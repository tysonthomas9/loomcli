# Per-package import-fanout allowances (see check-import-fanout.sh).
# Format: <package path relative to module> <max internal imports>
#
# These are grandfathered ceilings, not targets. The supervisor is the daemon's
# orchestration hub: the completion-hook, usage-accounting, watchdog and
# run-duration work each add a seam it must coordinate, which pushed it past
# the global threshold of 18; pre-turn hook management (hookcfg) is one more
# such seam. internal/cli grew one import for the same line of
# work. Ratchet these back down (or remove the entries) when coupling is paid
# down; do not raise them without recording why here.
internal/cli/daemon/supervisor 22
internal/cli 19
# The skills webui module adds the skills handler package to the app's module
# wiring and the domain skill path policy to svcimpl's file index. app then took
# one more (19 -> 20) for internal/webui/server/handler: the nested workspace mux
# has to answer unmatched requests with the same JSON error envelope as the outer
# catch-all, and that helper lives in server/handler because handlers/misc needs
# it too and cannot import app. Route registration is where the app package is
# supposed to reach for shared HTTP responders, so this is wiring, not new
# coupling.
#
# app (20 -> 21) and svcimpl (19 -> 20) then each took one more when route
# registration moved from *http.ServeMux to the internal/webui/route
# abstraction, so that the served routes can be enumerated from the built mux
# instead of grepped out of source. That package is a stdlib-only leaf
# (net/http + sort) with no onward module deps, so this is one more edge in the
# counter but no new coupling: it replaces a standard-library type in the same
# position. Shrink these back if the route abstraction is ever folded into a
# package these two already import.
internal/webui/app 21
internal/webui/svcimpl 20
# serve is where a process-wide Prometheus registration has to happen: the
# spawn/session collector is read at scrape time by the /metrics handler serve
# owns, so serve imports internal/metrics/agentmetrics directly. That is one
# import past the threshold, and the only one the collector costs.
internal/cli/serve 19
