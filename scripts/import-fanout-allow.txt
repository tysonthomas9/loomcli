# Per-package import-fanout allowances (see check-import-fanout.sh).
# Format: <package path relative to module> <max internal imports>
#
# These are grandfathered ceilings, not targets. The supervisor is the daemon's
# orchestration hub: the completion-hook, usage-accounting, watchdog and
# run-duration work each add a seam it must coordinate, which pushed it past
# the global threshold of 18. internal/cli grew one import for the same line of
# work. Ratchet these back down (or remove the entries) when coupling is paid
# down; do not raise them without recording why here.
#
# Lead-in-Daytona-sandbox raised three more. Each is a wiring seam, not a new
# concern leaking sideways:
#   internal/cli            +leadbackend, +leadoccupant (a sandboxed lead
#                           resolves its issue backend over serve instead of a
#                           local store) and +netbase (the shared IPv4/proxy
#                           transport the lock bridge dials through).
#   internal/cli/serve      +placement — serve holds the provider credential, so
#                           it is the process that constructs the broker. The
#                           provider construction itself was paid down as the
#                           note here asked: it now lives behind
#                           internal/cli/serve/placementwire, which absorbs
#                           placement/daytona, placement/exe, leadprovision and
#                           webui/terminal. Adding a THIRD provider must not
#                           move this number.
#   internal/webui/modbuilder  +leadapi, +health, +backend, +leadprovision.
#                           Constructing modules is this package's entire job,
#                           so its fanout tracks the module count.
internal/cli/daemon/supervisor 21
internal/cli 22
internal/cli/serve 21  # was 22 before placementwire; do not raise
internal/webui/modbuilder 20
