# Mock Slack App

Small dependency-free Slack-style app shell used by the epic runner demo.

## Commands

```bash
npm test
npm run build
```

The app is intentionally plain HTML, CSS, and JavaScript so worker agents can
extend it without installing packages.
