# Per-package file-count allowances (see check-package-size.sh).
# Format: <package dir> <max non-test files>
#
# Grandfathered ceilings, not targets. The supervisor package hosts the
# daemon's orchestration seams — the input-wait/watchdog, run-duration-cap and
# e2e-hardening work each added a file, crossing the global 25. The right end
# state is sub-packaging (inputwait / runcap / ...); until that split happens,
# this records the ceiling. Shrink or remove the entry when the package is
# split; do not raise it without recording why here.
# Raised 28 -> 29 for diffcapture.go: the checkpoint's git-diff capture was
# lifted out of concurrency.go (a concurrency tracker it never belonged in)
# when it grew to cover untracked files and multiple source trees. A
# sub-package was tried first and is the right end state, but it costs the
# package an import and pushes its recorded import-fanout ceiling (22) over
# instead — trading one ratchet for another. Take it in the sub-split.
# Raised 29 -> 30 for profile.go: the per-agent profile boot path (#563/#652,
# PUPPET-275/485) took spawn.go to 1059 lines, over the 1000-line LOC gate,
# and .loc-allowlist takes no new entries. The profile section moved out of
# spawn.go unchanged, as local/union did in 769403892 (PUPPET-243) - the
# alternative is trading the package gate for the LOC gate.
internal/cli/daemon/supervisor 30
# internal/cli gained the daemon input-await seam (agent_input_await.go) with
# the human answer path; 26th file, same sub-split end state applies. The
# hook-driven exit-code contract lives in exit.go rather than a file of its own,
# so this stays at 26.
internal/cli 26
