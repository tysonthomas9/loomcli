# Workspaces graph migration

This package preserves all three `workspaces.test.yaml` executions. Creation and
selector-switch paths share the exact primary-board open/readiness prefix; the direct
second-workspace link remains a sibling path. Graph-scoped setup provisions the common
secondary workspace once, while every path receives a fresh browser.

Added graph state proofs are the initial `about:blank` check, a visible board-toolbar
check at the shared trunk, and a terminal route proof after the selector scenario's final
API isolation read. Original mechanics remain ordered. No leaf depends on another leaf.
