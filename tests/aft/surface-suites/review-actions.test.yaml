# Surface tier: these review-action fixtures have no reviewable branch, commit,
# PR, or diff content on the gh-less stack (FINDINGS §1.19 and §3.9). They guard
# the wired buttons/status transitions without claiming a real review scenario.
# Promote them once seeded branch/commit review content exists.
suite: review-actions-surface
classification: { area: reviews, feature: review-actions, surface: mixed, purpose: contract }
baseUrl: "${AFT_BASE_URL:-http://127.0.0.1:3100}"
teardown: '"$AFT_TESTS_DIR/scripts/close-open-issues.sh"'

tests:
  # Surface-only because the row is a hollow status fixture; promote when the
  # reviewer can inspect seeded branch/commit content before approving.
  # covers: LCLI-RA-001 partial; PRReviewWorkspace approve wiring
  - name: approving a review issue closes it and clears the queue
    intent: An issue moved to review appears as a queue row, its review workspace opens in-app, and Approve closes the issue
    steps:
      - api:
          method: "POST"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues"
          body:
            title: "review-approve-$RUN_ID"
            issue_type: "task"
            priority: 2
          save:
            - from: "data.id"
              as: "reviewApproveId"
        intent: "An API client creates the review-approval fixture"
      - api:
          method: "PATCH"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues/${var:reviewApproveId}"
          body:
            status: "review"
        intent: "An API client moves the approval fixture into the review queue"
      - open: /ws/E2E-WS/prs
      - wait: { text: "review-approve-${RUN_ID:-local}" }
      # rows are li[role=button] aria-label="Review <title>"
      # rows re-render when GitHub enrichment settles, which can detach the node
      # between find and click — retry the click until the review param lands
      - run: >-
          for i in 1 2 3; do
          agent-browser --session "$AFT_SESSION" find role button click --name "review-approve-${RUN_ID:-local}" || true;
          sleep 1;
          agent-browser --session "$AFT_SESSION" eval "location.search" | grep -q "review=" && exit 0;
          done; echo "review row click never opened the workspace"; exit 1
        intent: "A reviewer retries the queue row until the review workspace opens"
      - wait: { url: "**review=**" }
      - wait:
          fn: >-
            !!document.querySelector('[data-testid=pr-review-workspace]')
        intent: "Wait until the review workspace is ready"
      - expect: { visible: { testid: pr-review-workspace } }
      # unassigned review issue: the assign-a-reviewer control renders
      - expect: { visible: { testid: review-agent-button } }
      # button text is "✓ Approve"; accessible-name matching is substring
      - click: { role: button, name: Approve }
      - wait:
          fn: >-
            !document.querySelector('[data-testid=pr-review-workspace]')
        intent: "Wait until the completed review workspace closes"
      - api:
          method: "GET"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues/${var:reviewApproveId}"
          assert:
            - path: "data.status"
              equals: "closed"
        intent: "A reviewer's readback confirms approval closed the issue"
      - expect: { notText: "review-approve-${RUN_ID:-local}" }

  # Surface-only because the row is a hollow status fixture; promote when the
  # reviewer can inspect seeded branch/commit content before requesting changes.
  # covers: LCLI-RA-001 partial; PRReviewWorkspace request-changes wiring
  - name: requesting changes sends the issue back to open
    intent: Request changes in the review workspace reopens the issue and removes it from the review queue
    steps:
      - api:
          method: "POST"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues"
          body:
            title: "review-changes-$RUN_ID"
            issue_type: "task"
            priority: 2
          save:
            - from: "data.id"
              as: "reviewChangesId"
        intent: "An API client creates the request-changes fixture"
      - api:
          method: "PATCH"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues/${var:reviewChangesId}"
          body:
            status: "review"
        intent: "An API client moves the changes fixture into the review queue"
      - open: /ws/E2E-WS/prs
      - wait: { text: "review-changes-${RUN_ID:-local}" }
      # same enrichment-settle re-render race as the approve test — retry the click
      - run: >-
          for i in 1 2 3; do
          agent-browser --session "$AFT_SESSION" find role button click --name "review-changes-${RUN_ID:-local}" || true;
          sleep 1;
          agent-browser --session "$AFT_SESSION" eval "location.search" | grep -q "review=" && exit 0;
          done; echo "review row click never opened the workspace"; exit 1
        intent: "A reviewer retries the queue row until the review workspace opens"
      - wait:
          fn: >-
            !!document.querySelector('[data-testid=pr-review-workspace]')
        intent: "Wait until the review workspace is ready"
      - click: { role: button, name: Request changes }
      - wait:
          fn: >-
            !document.querySelector('[data-testid=pr-review-workspace]')
        intent: "Wait until the completed review workspace closes"
      - api:
          method: "GET"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues/${var:reviewChangesId}"
          assert:
            - path: "data.status"
              equals: "open"
            - path: "data.labels"
              contains: "needs-revision"
            - path: "data.notes"
              contains: "requested changes"
        intent: "A reviewer's readback confirms requested changes reopened and annotated the issue"
      - expect: { notText: "review-changes-${RUN_ID:-local}" }
