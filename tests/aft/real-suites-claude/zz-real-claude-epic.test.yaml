# REAL claude tier — runs the operator's real `claude` CLI through the serve epic-runner.
# Gated: only runs under AFT_REAL_BACKEND=claude (run-aft.sh points AFT_SUITES at
# real-suites-claude/). Needs Claude Code OAuth credentials
# (${CLAUDE_CONFIG_DIR:-~/.claude}/.credentials.json); ANTHROPIC_API_KEY is unset by the
# harness so the run bills the subscription login, not the API. Every other agent CLI is
# stubbed (e2e/stubs-real-claude). Named zz- defensively. NOT part of the default suites/ dir.
suite: zz-real-claude-epic
classification: { area: agents, feature: real-agent-backends, surface: mixed, purpose: journey }
baseUrl: "${AFT_BASE_URL:-http://127.0.0.1:3100}"
teardown: >-
  "$AFT_TESTS_DIR/scripts/close-open-issues.sh";
  "$AFT_TESTS_DIR/scripts/real-backend-teardown.sh" claude || true

tests:
  - name: real claude epic-runner creates HELLO.md and closes the task
    intent: The real claude CLI, selected as the workspace default backend, claims the seeded child task via epic-runner, creates HELLO.md in a resolved task worktree, patches it back, records a real transcript+diff, and closes the task
    steps:
      # 0) select claude as this workspace's default backend. The stack is freshly
      #    booted per run, so this cannot leak into other suites or runs.
      - api:
          method: "PATCH"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/config/backend"
          body:
            backend: "claude"
        intent: "An operator selects claude as the workspace default backend"
      - api:
          method: "GET"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/config/backend"
          assert:
            - path: "data.backend"
              equals: "claude"
        intent: "An operator confirms claude is the persisted workspace backend"
      # 1) seed epic + one child task carrying a tiny, deterministic DESIGN (the claude prompt).
      - api:
          method: "POST"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues"
          body:
            title: "real-claude epic $RUN_ID"
            issue_type: "epic"
            priority: 2
          save:
            - from: "data.id"
              as: "rclEpicId"
        intent: "An operator seeds the real-claude epic at priority 2"
      - api:
          method: "POST"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/issues"
          body:
            title: "real-claude task $RUN_ID"
            issue_type: "task"
            priority: 2
            parent: "${var:rclEpicId}"
            design: "Create a file named HELLO.md at the repository root whose entire contents are exactly the line: hello world. Do not create any other files. Do not modify any other files."
          save:
            - from: "data.id"
              as: "rclTaskId"
        intent: "An operator seeds the real-claude child task with the HELLO.md design"
      # 2) confirm the card is on the board (SSE) before triggering.
      - open: /ws/E2E-WS/kanban
      - wait: { fn: "!!document.querySelector('[data-testid=board-toolbar]')" }
        intent: "Wait until the workspace board is ready"
      - wait: { text: real-claude task }
      # 3) trigger epic-runner (workspace backend = claude). POST carries the one-time flue
      #    bundle build; still well under the 120s run-step ceiling.
      - api:
          method: "POST"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/workflows/epic-runner"
          body:
            epicId: "${var:rclEpicId}"
        intent: "An operator starts epic-runner with the real claude backend"
      # 4) WAIT for completion by polling the API for status:closed (see real-codex suite for
      #    why the board is not a usable completion signal). Chained grace polls keep each
      #    run: step under aft's 120s ceiling — ~5.5 min total budget.
      - run: >-
          for i in $(seq 1 55); do curl -sf "$AFT_BASE_URL/api/workspaces/${AFT_WS:-E2E-WS}/issues/$(cat "$AFT_WORK_DIR/rclTaskId")" | grep -q '"status":"closed"' && exit 0; sleep 2; done; echo "not closed yet (grace window 1)"; exit 0
        intent: "Wait until the real backend task reaches closed state in the workspace API"
      - run: >-
          for i in $(seq 1 55); do curl -sf "$AFT_BASE_URL/api/workspaces/${AFT_WS:-E2E-WS}/issues/$(cat "$AFT_WORK_DIR/rclTaskId")" | grep -q '"status":"closed"' && exit 0; sleep 2; done; echo "not closed yet (grace window 2)"; exit 0
        intent: "Wait until the real backend task reaches closed state in the workspace API"
      - run: >-
          for i in $(seq 1 55); do curl -sf "$AFT_BASE_URL/api/workspaces/${AFT_WS:-E2E-WS}/issues/$(cat "$AFT_WORK_DIR/rclTaskId")" | grep -q '"status":"closed"' && exit 0; sleep 2; done; echo "task never closed"; exit 1
        intent: "Wait until the real backend task reaches closed state in the workspace API"
      # 5) board-level UX assert on the FLAT board, where closed cards do render in Done.
      - open: /ws/E2E-WS/kanban?groupBy=none
      - wait:
          fn: >-
            Array.from(document.querySelectorAll("section[data-status=done] article"))
              .some((el) => el.textContent.includes('real-claude task ${RUN_ID:-local}'))
        intent: "Wait until the flat board renders the completed real-backend task in Done"
      # 6) a session was recorded, and it is a claude/local run with a real diff
      #    (files_changed>=1). Also capture the session id for the transcript check.
      - api:
          method: "GET"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/tasks/${var:rclTaskId}/sessions"
          assert:
            - path: "data.sessions.0.backend"
              equals: "claude"
            - path: "data.sessions.0.exit_code"
              equals: 0
            - path: "data.sessions.0.evidence.status"
              equals: "ok"
            - path: "data.sessions.0.evidence.conflicts"
              contains: "[]"
          save:
            - from: "data.sessions.0"
              as: "rclSessionRecord"
            - from: "data.sessions.0.session_id"
              as: "rclSessionId"
        intent: "An operator reads back the completed real-claude session and captures its identity"
      - run: >-
          python3 -c 'import json,os; s=json.load(open(os.environ["AFT_WORK_DIR"]+"/rclSessionRecord")); assert s.get("backend")=="claude", s; assert s.get("exit_code")==0, s; assert int(s.get("files_changed") or 0) >= 1, s; ev=s.get("evidence") or {}; assert ev.get("status")=="ok" and not ev.get("conflicts"), ev; usage=ev.get("usage_status"); assert usage in ("reported","unavailable"), ev; fields=("input_tokens","output_tokens","cache_read_tokens","cache_write_tokens","estimated_cost_usd"); assert (all(s.get(f) is None for f in fields) if usage=="unavailable" else all(isinstance(s.get(f),(int,float)) for f in fields)), s'
        intent: "An operator confirms the real-claude session carries non-stub execution evidence"
      - api:
          method: "GET"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/tasks/${var:rclTaskId}/sessions/${var:rclSessionId}"
          assert:
            - path: "data.has_transcript"
              equals: true
            - path: "data.has_diff"
              equals: true
            - path: "data.evidence.status"
              equals: "ok"
        intent: "An operator confirms the real-claude session advertises healthy transcript and diff evidence"
      # 7) transcript is real: >=1 non-system entry AND references the file it created / tool
      #    activity (stub signature otherwise).
      - api:
          method: "GET"
          path: "/api/workspaces/${AFT_WS:-E2E-WS}/tasks/${var:rclTaskId}/sessions/${var:rclSessionId}/transcript"
          status: 200
          save:
            - from: "data.entries"
              as: "rclTranscriptEntries"
        intent: "An operator reads the real-claude session transcript"
      - run: >-
          python3 -c 'import json,os; ents=json.load(open(os.environ["AFT_WORK_DIR"]+"/rclTranscriptEntries")); nonsys=[e for e in ents if e.get("role")!="system"]; assert nonsys, "no non-system transcript entries"; blob=json.dumps(ents).lower(); assert "hello.md" in blob or any(e.get("role")=="tool" for e in ents) or "tool_use" in blob, "transcript lacks tool activity / HELLO.md reference (stub signature)"'
        intent: "An operator confirms the real-claude transcript contains non-system tool activity"
      # 7b) the session diff is readable — content assertion, not a flag check.
      - run: >-
          code=$(curl -s -o "$AFT_WORK_DIR/rcl-diff.json" -w "%{http_code}" "$AFT_BASE_URL/api/workspaces/${AFT_WS:-E2E-WS}/tasks/$(cat "$AFT_WORK_DIR/rclTaskId")/sessions/$(cat "$AFT_WORK_DIR/rclSessionId")/diff");
          [ "$code" = "200" ] || { echo "session diff returned $code (expected 200)"; cat "$AFT_WORK_DIR/rcl-diff.json"; exit 1; };
          grep -q 'HELLO.md' "$AFT_WORK_DIR/rcl-diff.json" || { echo "diff does not mention HELLO.md"; head -c 400 "$AFT_WORK_DIR/rcl-diff.json"; exit 1; }
        intent: "An operator confirms the recorded session diff contains HELLO.md"
      # 8) NEGATIVE GUARD via filesystem: the real claude physically created HELLO.md with the
      #    exact content. The stub creates NO file. This is the strongest real-vs-stub
      #    discriminator and is independent of any serve projection gap.
      - run: >-
          ws="$AFT_TESTS_DIR/../../tmp/e2e-workspace";
          f="$(find "$ws" -name HELLO.md -print 2>/dev/null | head -n1)";
          test -n "$f" || { echo "HELLO.md not found on disk — stub likely ran, not real claude"; exit 1; };
          grep -qx "hello world" "$f" || { echo "HELLO.md content wrong: $(cat "$f")"; exit 1; };
          echo "real claude wrote $f"
        intent: "An operator confirms the real backend wrote exactly HELLO.md with the expected content"
