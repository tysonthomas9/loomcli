{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace-ipc",
  "description": "App commands the loopback workspace web UI may invoke. Remote loopback origins only: `local: false` keeps these off the bundled launcher, and no non-loopback origin is listed. browser_operator_session additionally rejects any caller whose URL is not on the runtime origin the shell opened, under /ws/<workspace>.",
  "windows": ["main", "workspace-*"],
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["allow-pick-folder", "allow-browser-operator-session"]
}
