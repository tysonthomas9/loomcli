{
  "name": "@loom/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare-sidecar": "bash scripts/prepare-sidecar.sh",
    "refresh:app": "bash scripts/refresh-app.sh",
    "refresh:webui": "bash scripts/refresh-webui.sh",
    "dev": "tauri dev",
    "build": "tauri build --bundles app",
    "build:dmg": "tauri build --bundles app,dmg",
    "dev:frontend": "vite --host 127.0.0.1 --port 1420",
    "build:frontend": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-shell": "^2.3.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.2"
  }
}
