{
  "id": "crud_validation_errors",
  "title": "CRUD validation errors classify consistently",
  "description": "Exercises create/update/delete validation failures so the backend adapters agree on error class rather than treating bad input as internal failures.",
  "tags": ["crud", "validation", "error"],
  "steps": [
    {
      "id": "create_missing_title",
      "description": "Create without a title should fail validation",
      "method": "issue.create",
      "params": {
        "priority": 2,
        "type": "task",
        "owner": "parity-harness",
        "actor": "parity-harness"
      }
    },
    {
      "id": "update_missing_id",
      "description": "Update without an ID should fail validation or not-found consistently",
      "method": "issue.update",
      "params": {
        "title": "no id"
      }
    },
    {
      "id": "delete_missing_ids",
      "description": "Delete without any IDs should fail validation",
      "method": "issue.delete",
      "params": {
        "force": true
      }
    }
  ]
}
