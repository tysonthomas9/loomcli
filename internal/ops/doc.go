// Package ops defines cross-layer operation interfaces and their associated
// types. It is a leaf package in the SDK layer with zero imports of other
// internal packages. Both webui and cli import ops; neither defines nor owns
// the shared types.
package ops
