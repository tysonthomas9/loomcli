## What you must not do

Do not merge, do not push — to the trunk or your own branch — do not open a PR, and do not close the task. The tester judges the work and the integrator lands and publishes it, per the repo's contract entry (`push` differs per repo here, and one clone's `origin` is the owner's live working checkout — the contract, not you, decides what leaves the machine).
