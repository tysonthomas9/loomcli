<!-- Code generated by scripts/openapi-to-md. DO NOT EDIT. -->
<!-- Endpoint reference is generated from api/openapi.yaml. -->
<!-- Hand-written prose lives in docs/api.preamble.md and docs/api.appendix.md. -->
<!-- Regenerate with: make gen-api-docs -->

# Loom CLI API

**Generated file — do not edit by hand. Run `make gen-api-docs`.**

The endpoint reference below is generated from `api/openapi.yaml`, which is
staleness-gated against the committed Go types by `make check-go-api-staleness`.
Prose sections come from `docs/api.preamble.md` and `docs/api.appendix.md`;
edit those files, not this one.

Spec version: `0.1.0`.

REST API for the Loom CLI web UI and daemon. Covers workspace management,
issue tracking, agent lifecycle, terminal sessions, git operations, fleet
coordination, monitoring, and observability.

## About this document

This is the reference for the **Loom WebUI HTTP API** — the server started by
`loom serve` (`internal/cli/serve/serve.go`), which the web UI, the CLI, and
remote workers all talk to. "Loom" here is the agent-orchestration platform, not
any similarly named product; see `docs/loom-glossary.md` for the other
overloaded words used below (workspace, agent, lead, fleet, flue, driver).

The endpoint reference and schema tables are generated from `api/openapi.yaml`.
The sections in this preamble and in the appendix are hand-written and are the
only parts of this file you may edit — they live in `docs/api.preamble.md` and
`docs/api.appendix.md`.

`api/openapi.yaml` is not a complete description of what the server registers.
The **Spec Coverage vs Registered Routes** appendix at the end of this document
is regenerated on every run and lists the exact gaps in both directions. Read it
before trusting the absence of an endpoint here as evidence it does not exist.

## Overview

- **Base URL:** `http://127.0.0.1:8080`. Port and bind address come from
  `--port`/`LOOM_SERVER_PORT` and `--bind`/`LOOM_BIND_ADDR`
  (`internal/cli/serve/serve.go:151-164`).
- **Content-Type:** `application/json` for request and response bodies, except
  the WebSocket and Server-Sent Events endpoints and `GET /metrics`
  (Prometheus text format).
- **Request body limit:** 1 MB — `handler.MaxRequestBody`
  (`internal/webui/server/handler/request.go:15`), applied via
  `http.MaxBytesReader`. `POST /api/client-errors` uses its own smaller 16 KB
  limit (`internal/webui/handlers/misc/client_errors.go:34`).
- **Unregistered `/api/` paths** return `404` with the JSON body
  `{"error":"not found"}` (`internal/webui/app/routes.go:32-36`). Non-`/api`
  paths reach the SPA handler only when the server was started with a frontend
  directory; `registerFrontendRoutes` returns without registering `/` when
  `FrontendDir` is empty (`internal/webui/app/frontend.go:11-16`), and the
  frontend is normally served externally, so those paths get Go's default text
  `404`.

## Authentication

This document owns **per-endpoint** auth requirements. Auth-mode *policy* —
which modes exist, when each applies, credential storage, and the SSRF and
subprocess-env analysis — is owned by [`security.md`](security.md). When the two
disagree about policy, `security.md` wins.

### Bearer token

Protected endpoints require a bearer JWT:

```
Authorization: Bearer <token>
```

`extractBearerToken` reads the `Authorization` header **only**
(`internal/webui/server/middleware/auth.go:181-194`); there is no `?token=`
fallback on the generic auth middleware. Browser transports that cannot set
headers use a dedicated token exchange instead:

- **SSE:** `GET /api/workspaces/{ws}/events/token` mints a short-lived token the
  `EventSource` connection presents (`internal/webui/subscription/module.go:55`).
- **Terminal / agent-terminal WebSockets:** `GET .../terminal/token` mints a
  one-time HMAC token passed as `?token=` on the upgrade request (see
  *One-time terminal tokens* below).

### Public routes

`isPublicRoute` (`internal/webui/server/middleware/auth_routes.go:11`) decides
what bypasses the JWT middleware. Workspace-scoped paths are normalised by
stripping `/api/workspaces/{ws}` first, so the rules below apply to both the
global and the workspace-scoped form.

Public regardless of method:

- Any path under `/api/fleet/`, `/api/internal/workers/`, `/api/webhooks/`,
  `/api/driver/`, `/api/task-run/`, `/api/auth/` — these authenticate inside
  their own handlers (fleet API key / fleet JWT, `LOOM_WORKER_TOKEN`, per-binding
  webhook signature, `X-Loom-Driver-*` run credentials, task-run lease token,
  and the proxied BetterAuth service respectively).
- `POST /api/client-errors` — must work before auth bootstrap completes.
- `POST` to the session-notify path (`sessions.NotifyPath`) — uses its own
  mechanism.

Public for `GET` only:

- `/health`, `/api/health`, `/metrics`
- `/api/config` — auth discovery, needed before a token exists
- `/api/events` (i.e. `/api/workspaces/{ws}/events`) — SSE token exchange
- `/api/terminal/ws` (i.e. `/api/workspaces/{ws}/terminal/ws`) — one-time token
- `/api/agents/{name}/terminal/ws` — one-time token
- Any non-`/api/` path — frontend static files and SPA routes

### One-time terminal tokens

`realtime.TerminalAuth` (`internal/webui/server/realtime/terminal_auth.go`)
issues the tokens used by the terminal and agent-terminal WebSockets.

- Format: `base64url(JSON{session, workspace, uid, exp, nonce}) + "." +
  base64url(HMAC-SHA256)`.
- Lifetime: 60 seconds (`TerminalTokenExpiry`), single use — the nonce is
  recorded in memory and replays are rejected.
- The HMAC secret is 32 random bytes generated per process, so every server
  restart invalidates all outstanding tokens. This is deliberate.
- Agent-terminal tokens are scoped `agent:<name>:logs`
  (`internal/webui/handlers/terminal/agent.go:29`), which stops a token minted
  for one agent from being replayed against another or against the main
  terminal.

## Response envelopes

There is no single envelope. The three shapes in use are:

1. **Error envelope** — `dto.ErrorResponse`
   (`internal/webui/server/dto/common.go:5`):
   `{"success": false, "error": "...", "code": "...", "details": {...}}`.
   `code` and `details` are `omitempty`.
2. **List envelope** — `dto.ListResponse[T]`:
   `{"success": true, "data": [...], "total": N}`. `data` is always an array,
   never `null`.
3. **Bare JSON** — some handlers write the payload directly with no `success`
   wrapper, and middleware-level failures use `{"error": "..."}` with no
   `success` field (`internal/webui/app/respond.go:20`).

The generated per-endpoint tables below record what `api/openapi.yaml` declares
for each operation; where the spec says only `type: object`, the shape is not
pinned down and the handler is the authority.

## Workspace scoping

Most endpoints live under `/api/workspaces/{ws}/...` and are served by a nested
mux mounted behind workspace middleware
(`internal/webui/app/routes.go:140-199`). `middleware.WorkspaceResolved`
(`internal/webui/server/middleware/workspace.go:76`):

1. reads `{ws}` via `r.PathValue("ws")` and trims whitespace,
2. returns `400 {"error": "workspace ID is required"}` when it is empty,
3. resolves it to a canonical workspace ID, returning
   `404 {"error": "workspace not found: {ws}"}` when resolution fails,
4. injects the resolved `WorkspaceRef` into the request context, where handlers
   read it with `middleware.WorkspaceFromContext`.

So every workspace-scoped operation below can also return `400` or `404` from
the middleware, before its own handler runs. Authentication (`401`) is enforced
earlier still, by the server-level auth middleware.

Two deliberate exceptions to the mount:

- `GET /api/workspaces/{ws}/readyz` bypasses workspace middleware so it can
  report readiness for a workspace that is not registered yet
  (`internal/webui/app/routes.go:188-197`).
- `PATCH` routes are registered on the outer mux rather than the nested one,
  working around a `net/http` bug where reading the body of a `PATCH` routed
  through a nested mux via a wildcard subtree pattern hangs
  (`internal/webui/app/routes.go:151-153`).

## Shared validation

- **Agent names** must match `^[a-zA-Z0-9_-]+$`
  (`internal/webui/handlers/terminal/agent.go:25`,
  `internal/webui/handlers/misc/logs.go:26`). Git handlers reject a missing name
  with `400 "missing agent name"` and a malformed one with
  `400 "invalid agent name"`; an unknown worktree is
  `404 agent worktree "<name>" not found`
  (`internal/webui/handlers/git/diff_service.go:32-42`).
- **Git refs and branches** must match `^[a-zA-Z0-9][a-zA-Z0-9_./-]*$`
  (`internal/webui/handlers/git/git.go:25`,
  `internal/webui/svcimpl/validators.go:18`). The leading-character rule rejects
  refs starting with `-` (argument injection) and the character class excludes
  everything needed for `..` traversal.

## Conditional registration

A route existing in the source does not mean it is mounted in every process.
Handler modules skip registration when their dependency is nil — for example
`internal/webui/handlers/terminal/module.go:76-98` registers the agent-terminal
routes only when the agent service, tmux manager, or terminal auth is
configured, and `internal/webui/app/routes.go:23-25` registers the whole
workspace subtree only when a multi-workspace pool exists. A request to an
unmounted `/api/` route gets the JSON `404` described above, not a `501`.

## Servers

- `http://localhost:8080` — Local development server (post-consolidation single server)

## Security Schemes

The document-level default is `BearerAuth`.
An operation that declares `security: []` overrides that and is public.

### `BearerAuth`

- **Kind:** http, scheme `bearer`

API key authentication. The key is a 64-character hex string generated
from 32 random bytes. Configured via `LOOM_AUTH_API_KEY` env var or
loaded from `~/.loom/api-key`. Supports fallback to `?token=` query
parameter for WebSocket/EventSource connections.

### `WorkerToken`

- **Kind:** http, scheme `bearer`

Static token for internal worker API endpoints. Configured via
`LOOM_WORKER_TOKEN` env var. If not set, worker API returns 503.

### `FleetApiKey`

- **Kind:** apiKey in `header`, name `X-Fleet-API-Key`

API key for fleet registration. Configured via `LOOM_FLEET_API_KEY`
env var. Constant-time comparison with optional per-IP rate limiting.

### `FleetJWT`

- **Kind:** http, scheme `bearer`, format `JWT`

JWT token for fleet claim/heartbeat endpoints. HMAC-SHA256 signed.
Configured via `LOOM_FLEET_JWT_KEY` env var. Token expiry: 1 hour.

### `TaskRunLease`

- **Kind:** http, scheme `bearer`

Per-task-run lease-token Bearer auth for the task-run SDK transport,
verified in-handler (not by the user-JWT middleware) through fleet-db's
fenced task-run checks. The token is accompanied by `X-Loom-Task-Run-*`
identity headers (run id + fencing token, optional node/lease id); a
superseded lease or a terminal run is rejected. Not user-JWT, not
fleet-JWT.

## Endpoint Index

168 operations across 147 paths.

| Method | Path | Tag | Summary |
|--------|------|-----|---------|
| GET | `/health` | health | Health check for load balancers |
| GET | `/api/health` | health | API health check with daemon status |
| GET | `/api/metrics` | health | SSE hub metrics snapshot |
| POST | `/api/client-errors` | health | Client-side error reporting (rate-limited, 10 req/min/IP) |
| GET | `/api/workspaces/{ws}/readyz` | health | Workspace-scoped runtime readiness |
| GET | `/api/config` | config | Auth mode discovery (public, rate-limited) |
| GET | `/api/local/settings` | config | Get desktop-local runtime settings (secret-sanitized) |
| PATCH | `/api/local/settings` | config | Update desktop-local runtime settings |
| GET | `/api/backends` | config | Backend availability health check |
| GET | `/api/workspaces/{ws}/config/backend` | config | Get workspace-scoped backend configuration |
| PATCH | `/api/workspaces/{ws}/config/backend` | config | Set workspace backend override |
| PATCH | `/api/workspaces/{ws}/config/design-format` | config | Set the workspace planner design format |
| GET | `/api/auth/token` | config | Auth token endpoint (external auth mode) |
| GET | `/api/workspaces/active` | workspaces | Get full topology for the default workspace |
| GET | `/api/workspaces` | workspaces | List all workspaces |
| POST | `/api/workspaces` | workspaces | Create a new workspace |
| GET | `/api/workspaces/{ws}` | workspaces | Get workspace topology |
| DELETE | `/api/workspaces/{ws}` | workspaces | Delete a workspace |
| GET | `/api/workspaces/{ws}/repos` | workspaces | List a workspace's repos |
| POST | `/api/workspaces/{ws}/repos` | workspaces | Add repos to a workspace |
| GET | `/api/workspaces/jobs/{id}` | workspaces | Poll workspace creation job status |
| PUT | `/api/workspaces/order` | workspaces | Set workspace display order |
| PUT | `/api/workspaces/default` | workspaces | Set the default workspace |
| DELETE | `/api/workspaces/default` | workspaces | Clear the default workspace |
| PATCH | `/api/workspaces/{ws}/name` | workspaces | Rename a workspace |
| GET | `/api/workspaces/{ws}/issues` | issues | List issues with filtering and search |
| POST | `/api/workspaces/{ws}/issues` | issues | Create a new issue |
| GET | `/api/workspaces/{ws}/issues/search` | issues | Full-text search issues in a workspace |
| GET | `/api/workspaces/{ws}/issues/{id}` | issues | Get a single issue with full details |
| PATCH | `/api/workspaces/{ws}/issues/{id}` | issues | Partially update an issue |
| DELETE | `/api/workspaces/{ws}/issues/{id}` | issues | Delete an issue (tombstone) |
| POST | `/api/workspaces/{ws}/issues/{id}/close` | issues | Close an issue with optional reason |
| POST | `/api/workspaces/{ws}/issues/{id}/reopen` | issues | Reopen a closed issue |
| POST | `/api/workspaces/{ws}/issues/{id}/claim` | issues | Atomically claim an issue for the server-side actor |
| POST | `/api/workspaces/{ws}/issues/{id}/move` | issues | Move an issue to another workspace |
| GET | `/api/workspaces/{ws}/issues/{id}/comments` | comments | List comments for an issue |
| POST | `/api/workspaces/{ws}/issues/{id}/comments` | comments | Add a comment to an issue |
| GET | `/api/workspaces/{ws}/issues/{id}/dependencies` | dependencies | List an issue's dependencies |
| POST | `/api/workspaces/{ws}/issues/{id}/dependencies` | dependencies | Add a dependency to an issue |
| DELETE | `/api/workspaces/{ws}/issues/{id}/dependencies/{depId}` | dependencies | Remove a dependency from an issue |
| GET | `/api/workspaces/{ws}/ready` | ready | List ready (unblocked) issues |
| GET | `/api/workspaces/{ws}/blocked` | blocked | List blocked issues with blocker details |
| GET | `/api/workspaces/{ws}/issues/graph` | graph | Get dependency graph for visualization |
| GET | `/api/workspaces/{ws}/stats` | stats | Per-workspace statistics |
| GET | `/api/workspaces/{ws}/issues/{id}/events` | events | Get audit trail events for an issue |
| GET | `/api/workspaces/{ws}/events` | events | SSE mutation event stream |
| GET | `/api/workspaces/{ws}/events/token` | events | Get one-time SSE auth token status |
| GET | `/api/workspaces/{ws}/tasks/{taskId}/sessions` | sessions | List sessions for a task |
| GET | `/api/workspaces/{ws}/tasks/{taskId}/sessions/{sessionId}` | sessions | Get session metadata |
| GET | `/api/workspaces/{ws}/tasks/{taskId}/sessions/{sessionId}/transcript` | sessions | Get session transcript entries |
| GET | `/api/workspaces/{ws}/tasks/{taskId}/sessions/{sessionId}/diff` | sessions | Get session git diff |
| POST | `/api/sessions/notify` | sessions | Notify SSE hub of session status change (agent-to-server) |
| GET | `/api/workspaces/{ws}/issues/{issueId}/sessions` | session-history | List session history records for an issue |
| GET | `/api/workspaces/{ws}/issues/{issueId}/sessions/{recordId}/scrollback` | session-history | Get scrollback content for a session history record |
| GET | `/api/workspaces/{ws}/agents/{name}/logs` | logs | Get recent log lines for an agent |
| GET | `/api/workspaces/{ws}/tasks/{id}/logs` | logs | List available log phases for a task |
| GET | `/api/workspaces/{ws}/tasks/{id}/logs/{phase}` | logs | Get log content for a specific task phase |
| GET | `/api/config/terminal` | terminal | Terminal session lifecycle configuration |
| GET | `/api/workspaces/{ws}/terminal/token` | terminal | Generate one-time WebSocket auth token |
| GET | `/api/workspaces/{ws}/terminal/ws` | terminal | WebSocket terminal relay |
| GET | `/api/workspaces/{ws}/terminal/tabs` | terminal-tabs | List terminal tab metadata |
| GET | `/api/workspaces/{ws}/terminal/tabs/{session}` | terminal-tabs | Get metadata for a single terminal tab |
| PUT | `/api/workspaces/{ws}/terminal/tabs/{session}` | terminal-tabs | Create or replace terminal tab metadata |
| PATCH | `/api/workspaces/{ws}/terminal/tabs/{session}` | terminal-tabs | Partially update terminal tab metadata |
| DELETE | `/api/workspaces/{ws}/terminal/tabs/{session}` | terminal-tabs | Delete terminal tab metadata |
| GET | `/api/workspaces/{ws}/terminal/sessions/by-issue` | terminal-tabs | List terminal sessions grouped by issue |
| POST | `/api/workspaces/{ws}/terminal/setup` | terminal-tabs | Start a backend-owned setup command in a setup terminal |
| GET | `/api/workspaces/{ws}/terminal/state` | terminal-state | Get persisted terminal UI state |
| PATCH | `/api/workspaces/{ws}/terminal/state` | terminal-state | Update terminal UI state |
| GET | `/api/workspaces/{ws}/agents/{name}/terminal/info` | agent-terminal | Get terminal info for a specific agent |
| GET | `/api/workspaces/{ws}/agents/{name}/terminal/token` | agent-terminal | Generate one-time WebSocket token for agent terminal |
| GET | `/api/workspaces/{ws}/agents/{name}/terminal/ws` | agent-terminal | WebSocket terminal relay for agent |
| POST | `/api/workspaces/{ws}/agents/{name}/terminal/session` | agent-terminal | Ensure a persisted terminal session for an agent |
| GET | `/api/workspaces/{ws}/interactive-prompts` | agent-control | List built-in interactive terminal prompt options |
| GET | `/api/workspaces/{ws}/agents` | agent-control | List agents via daemon control socket |
| POST | `/api/workspaces/{ws}/agents` | agent-control | Create an agent assignment |
| PATCH | `/api/workspaces/{ws}/agents/{name}` | agent-control | Partial-update an agent assignment |
| DELETE | `/api/workspaces/{ws}/agents/{name}` | agent-control | Delete an agent assignment |
| GET | `/api/workspaces/{ws}/agents/{name}/queue` | agent-control | Ranked task queue for a single agent (daemon-backed) |
| POST | `/api/workspaces/{ws}/agents/{name}/stop` | agent-control | Stop an agent (yield by default, force with body) |
| POST | `/api/workspaces/{ws}/agents/{name}/start` | agent-control | Start an agent |
| POST | `/api/workspaces/{ws}/agents/{name}/restart` | agent-control | Restart an agent |
| POST | `/api/workspaces/{ws}/agents/{name}/yield` | agent-control | Yield an agent (graceful stop at next safe point) |
| GET | `/api/workspaces/{ws}/agents/{name}/git/status` | git | Get git status for an agent worktree |
| POST | `/api/workspaces/{ws}/agents/{name}/git/push` | git | Push agent worktree to remote |
| POST | `/api/workspaces/{ws}/agents/{name}/git/pull` | git | Pull remote changes into agent worktree |
| POST | `/api/workspaces/{ws}/agents/{name}/git/sync` | git | Sync agent worktree (pull + push) |
| POST | `/api/workspaces/{ws}/agents/{name}/git/pr` | git | Create a pull request from agent worktree |
| POST | `/api/workspaces/{ws}/agents/{name}/git/reset` | git | Reset agent worktree to integration branch |
| PATCH | `/api/workspaces/{ws}/agents/{name}/git/target` | git | Update agent git push target branch |
| POST | `/api/workspaces/{ws}/git/push-all` | git | Push all agent worktrees to remote |
| GET | `/api/workspaces/{ws}/pull-requests` | pull-requests | List pull requests across the workspace's repos (gh-backed fallback) |
| GET | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}` | pull-requests | Get pull request detail via the GitHub connector |
| GET | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/diff` | pull-requests | Get pull request diff via the GitHub connector |
| POST | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/review` | pull-requests | Post a pull request review via the GitHub connector |
| POST | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/reviewer` | pull-requests | Ensure a per-PR terminal reviewer agent |
| POST | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/messages` | pull-requests | Send a message to a per-PR terminal reviewer agent |
| GET | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/stream` | pull-requests | Stream the per-PR reviewer conversation |
| GET | `/api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/conversation` | pull-requests | Snapshot of the per-PR reviewer conversation (poll target) |
| GET | `/api/workspaces/{ws}/issues/{id}/git/diff-stat` | diff | Get git diff statistics for an issue |
| GET | `/api/workspaces/{ws}/agents/{name}/git/diff-stat` | diff | Get git diff statistics for an agent |
| GET | `/api/workspaces/{ws}/agents/{name}/diff/commits` | diff | List commits for an agent worktree |
| GET | `/api/workspaces/{ws}/agents/{name}/diff/files` | diff | List changed files in agent worktree |
| GET | `/api/workspaces/{ws}/agents/{name}/diff/file` | diff | Get diff for a specific file |
| GET | `/api/workspaces/{ws}/files/stat` | files | Get mutation metadata and a strong version for a scoped path |
| GET | `/api/workspaces/{ws}/files/capabilities` | files | Get effective workspace file-browser capabilities |
| GET | `/api/workspaces/{ws}/files/tree` | files | Get file tree for a scoped file browser root |
| GET | `/api/workspaces/{ws}/files/index` | files | Get quick-open file index for a scoped file browser root |
| POST | `/api/workspaces/{ws}/files/search` | files | Search files in a scoped file browser root |
| GET | `/api/workspaces/{ws}/files/git-status` | files | Get git status decorations for a scoped file browser root |
| GET | `/api/workspaces/{ws}/files/checkouts` | files | List workspace file checkouts and local change counts |
| POST | `/api/workspaces/{ws}/files/checkouts/repair` | files | Repair or provision a known workspace file checkout |
| GET | `/api/workspaces/{ws}/files/diff` | files | Get unified diff for a file in a scoped file browser root |
| GET | `/api/workspaces/{ws}/files/history` | files | Get bounded commit history for a file |
| GET | `/api/workspaces/{ws}/files/blame` | files | Get bounded git blame data for a file |
| GET | `/api/workspaces/{ws}/files` | files | Read a file from a scoped file browser root |
| PUT | `/api/workspaces/{ws}/files` | files | Create or update a file in a scoped file browser root |
| DELETE | `/api/workspaces/{ws}/files` | files | Delete a file or directory in a scoped file browser root |
| POST | `/api/workspaces/{ws}/files/mkdir` | files | Create a directory in a scoped file browser root |
| PATCH | `/api/workspaces/{ws}/files/move` | files | Move or rename a path in a scoped file browser root |
| GET | `/api/editors` | editors | List detected external editors |
| POST | `/api/editors/open` | editors | Open a file in an external editor |
| POST | `/api/workspaces/{ws}/fleet/register` | fleet | Register a fleet worker |
| POST | `/api/workspaces/{ws}/fleet/claim` | fleet | Claim a task from the fleet queue |
| POST | `/api/workspaces/{ws}/fleet/done/{id}` | fleet | Mark a fleet task as done |
| POST | `/api/workspaces/{ws}/fleet/heartbeat` | fleet | Fleet worker heartbeat |
| POST | `/api/internal/workers/register` | workers | Register a remote worker |
| DELETE | `/api/internal/workers/{id}` | workers | Deregister a remote worker |
| POST | `/api/internal/workers/{id}/state` | workers | Update worker state (action, task assignment) |
| POST | `/api/internal/workers/{id}/events` | workers | Push events from a remote worker |
| POST | `/api/internal/workers/{id}/logs` | workers | Push log lines from a remote worker |
| GET | `/api/workspaces/{ws}/issues/{issueId}/tabs` | issue-tabs | Get issue detail tab state |
| PUT | `/api/workspaces/{ws}/issues/{issueId}/tabs` | issue-tabs | Save issue detail tab state |
| DELETE | `/api/workspaces/{ws}/issues/{issueId}/tabs` | issue-tabs | Delete issue detail tab state |
| GET | `/api/workspaces/{ws}/monitor/status` | monitor | Full monitor dashboard data for a workspace |
| GET | `/api/daemon/config` | monitor | Effective resolved daemon config |
| GET | `/api/daemon/supervisor` | monitor | Daemon supervisor state and supervised agents |
| GET | `/api/monitor/status` | monitor | Full monitor dashboard data |
| GET | `/api/monitor/agents` | monitor | Agent list with optional workspace grouping |
| GET | `/api/monitor/tasks` | monitor | Task distribution by status |
| GET | `/api/monitor/stats` | monitor | Monitor statistics (distinct from /api/stats issue stats) |
| GET | `/api/monitor/sync` | monitor | Git sync status |
| GET | `/api/monitor/workspaces` | monitor | Workspace topology metadata |
| GET | `/api/monitor/stale-detector` | monitor | Stale detector status |
| GET | `/api/monitor/usage` | monitor | Token usage aggregates |
| GET | `/metrics` | monitor | Prometheus scrape endpoint |
| GET | `/api/observability/metrics` | observability | Event metrics snapshot |
| GET | `/api/observability/events` | observability | Paginated event log |
| POST | `/api/workspaces/{ws}/approvals` | approvals | Record an approval or rejection decision for a pending await |
| POST | `/api/workspaces/{ws}/driver/events/await` | driver | Register-and-check the run's next await (suspend or resolve inline) |
| GET | `/api/workspaces/{ws}/driver/events/awaits` | driver | List the calling run's awaits in index order |
| GET | `/api/workspaces/{ws}/driver/watch/epic` | driver | SSE stream of TaskRun journal events for an epic |
| POST | `/api/workspaces/{ws}/driver/workflows/await` | driver | Await a child workflow run's completion |
| POST | `/api/workspaces/{ws}/driver/workflows/start` | driver | Start (or idempotently re-read) a child workflow run |
| POST | `/api/workspaces/{ws}/driver/{op}` | driver | Driver-op dispatcher (opaque op path parameter) |
| POST | `/api/workspaces/{ws}/workflows/{name}` | workflows | Start a workflow run |
| POST | `/api/workspaces/{ws}/workflows/{name}/versions` | workflows | Register a workflow version from uploaded source |
| GET | `/api/workspaces/{ws}/runs/{runId}` | workflows | Get a workflow/driver run |
| GET | `/api/workspaces/{ws}/runs/{runId}/events` | workflows | List a run's events (paginated) |
| GET | `/api/workspaces/{ws}/runs/{runId}/stream` | workflows | Stream a run's events (SSE) |
| PUT | `/api/workspaces/{ws}/task-run/artifacts/{artifactId}/content` | task-run | Upload raw content for a task-run artifact |
| POST | `/api/workspaces/{ws}/task-run/{op}` | task-run | Task-run operation dispatcher (opaque op) |
| GET | `/api/workspaces/{ws}/trigger-events` | triggers | List persisted trigger events (webhook ingest audit trail) |
| GET | `/api/workspaces/{ws}/trigger-events/{eventId}` | triggers | Get a single persisted trigger event |
| GET | `/api/workspaces/{ws}/trigger-deliveries` | triggers | List persisted trigger deliveries (fan-out dispatch audit trail) |
| GET | `/api/workspaces/{ws}/trigger-deliveries/{deliveryId}` | triggers | Get a single persisted trigger delivery |
| POST | `/api/workspaces/{ws}/webhooks/{name}` | webhooks | Ingest an inbound webhook delivery for the named adapter |
| POST | `/api/workspaces/{ws}/onboarding/first-task` | onboarding | Create the onboarding first task and queue an agent start |

## Endpoints

### Tag: health

Health checks and metrics

#### `GET /health`

Health check for load balancers

- **Operation ID:** `getHealth`
- **Auth:** **None** (public route)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Server is healthy | `text/plain`: `string` |

#### `GET /api/health`

API health check with daemon status

- **Operation ID:** `getApiHealth`
- **Auth:** **None** (public route)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Healthy | `application/json`: `object` |

#### `GET /api/metrics`

SSE hub metrics snapshot

- **Operation ID:** `getMetrics`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Hub metrics | `application/json`: `object` |

#### `POST /api/client-errors`

Client-side error reporting (rate-limited, 10 req/min/IP)

- **Operation ID:** `reportClientError`
- **Auth:** **None** (public route)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Error recorded | — |
| `429` | Rate limited | — |

#### `GET /api/workspaces/{ws}/readyz`

Workspace-scoped runtime readiness

- **Operation ID:** `getWorkspaceRuntimeReady`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Runtime is ready to serve agent work for the workspace | `application/json`: [RuntimeReadyResponse](#schema-runtimereadyresponse) |
| `503` | Runtime is not ready to serve agent work for the workspace | `application/json`: [RuntimeReadyResponse](#schema-runtimereadyresponse) |

### Tag: config

Authentication and backend configuration

#### `GET /api/config`

Auth mode discovery (public, rate-limited)

- **Operation ID:** `getAuthConfig`
- **Auth:** **None** (public route)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Auth configuration | `application/json`: `object` |
| `429` | Rate limited | — |

Response headers (`429`):

- `Retry-After` — `integer`

#### `GET /api/local/settings`

Get desktop-local runtime settings (secret-sanitized)

- **Operation ID:** `getLocalSettings`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Sanitized local settings | `application/json`: [LocalSettingsResponse](#schema-localsettingsresponse) |
| `500` | Failed to load local settings | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `PATCH /api/local/settings`

Update desktop-local runtime settings

- **Operation ID:** `patchLocalSettings`
- **Auth:** `BearerAuth` (document default)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [LocalSettingsPatchRequest](#schema-localsettingspatchrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Settings saved (returns sanitized settings) | `application/json`: [LocalSettingsResponse](#schema-localsettingsresponse) |
| `400` | Invalid request body or validation error | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `413` | Request body too large | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Failed to load existing settings | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/backends`

Backend availability health check

- **Operation ID:** `getBackendsHealth`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Backend health statuses | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/config/backend`

Get workspace-scoped backend configuration

- **Operation ID:** `getWorkspaceBackendConfig`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Backend config | `application/json`: [BackendConfigResponse](#schema-backendconfigresponse) |

#### `PATCH /api/workspaces/{ws}/config/backend`

Set workspace backend override

- **Operation ID:** `patchWorkspaceBackend`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [WorkspaceBackendPatchRequest](#schema-workspacebackendpatchrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Updated | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `PATCH /api/workspaces/{ws}/config/design-format`

Set the workspace planner design format

- **Operation ID:** `patchWorkspaceDesignFormat`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [WorkspaceDesignFormatPatchRequest](#schema-workspacedesignformatpatchrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Updated | `application/json`: [MessageResponse](#schema-messageresponse) |
| `400` | Invalid design format | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/auth/token`

Auth token endpoint (external auth mode)

- **Operation ID:** `getAuthToken`
- **Auth:** **None** (public route)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Token info | `application/json`: `object` |

### Tag: workspaces

Workspace CRUD and topology

#### `GET /api/workspaces/active`

Get full topology for the default workspace

- **Operation ID:** `getActiveWorkspace`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Active workspace topology | `application/json`: [WorkspaceResponse](#schema-workspaceresponse) |
| `404` | No default workspace configured | — |

#### `GET /api/workspaces`

List all workspaces

- **Operation ID:** `listWorkspaces`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Workspace list | `application/json`: `object` |

#### `POST /api/workspaces`

Create a new workspace

- **Operation ID:** `createWorkspace`
- **Auth:** `BearerAuth` (document default)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `path` | `string` | yes | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `202` | Workspace creation started (async job) | `application/json`: `object` |
| `400` | Validation error | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}`

Get workspace topology

- **Operation ID:** `getWorkspace`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Workspace topology | `application/json`: [WorkspaceResponse](#schema-workspaceresponse) |
| `404` | Workspace not found | — |

#### `DELETE /api/workspaces/{ws}`

Delete a workspace

- **Operation ID:** `deleteWorkspace`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Workspace deleted | `application/json`: [MessageResponse](#schema-messageresponse) |
| `404` | Workspace not found | — |

#### `GET /api/workspaces/{ws}/repos`

List a workspace's repos

Lightweight endpoint returning only the workspace's repo list, for
clients that do not need the full workspace topology payload.

- **Operation ID:** `listWorkspaceRepos`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Repo list | `application/json`: `object` |
| `400` | Missing workspace ID | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Workspace not found | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/repos`

Add repos to a workspace

Attaches existing local git repos and/or clones remote git URLs into
the workspace, then returns the refreshed workspace topology.

- **Operation ID:** `addWorkspaceRepos`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [WorkspaceAddReposRequest](#schema-workspaceaddreposrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Repos added; refreshed workspace topology | `application/json`: `object` |
| `400` | Missing workspace ID or invalid request body | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `413` | Request body too large | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/jobs/{id}`

Poll workspace creation job status

- **Operation ID:** `getWorkspaceJob`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `id` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Job status | `application/json`: `object` |
| `404` | Job not found | — |

#### `PUT /api/workspaces/order`

Set workspace display order

- **Operation ID:** `reorderWorkspaces`
- **Auth:** `BearerAuth` (document default)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `order` | array of `string` | yes | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Order updated | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `PUT /api/workspaces/default`

Set the default workspace

- **Operation ID:** `setDefaultWorkspace`
- **Auth:** `BearerAuth` (document default)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace` | `string` | yes | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Default set | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `DELETE /api/workspaces/default`

Clear the default workspace

- **Operation ID:** `clearDefaultWorkspace`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Default cleared | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `PATCH /api/workspaces/{ws}/name`

Rename a workspace

- **Operation ID:** `renameWorkspace`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [WorkspaceRenameRequest](#schema-workspacerenamerequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Renamed | `application/json`: [MessageResponse](#schema-messageresponse) |
| `400` | Invalid name | `application/json`: [ErrorResponse](#schema-errorresponse) |

### Tag: issues

Issue CRUD, search, and lifecycle

#### `GET /api/workspaces/{ws}/issues`

List issues with filtering and search

- **Operation ID:** `listIssues`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `status` | query | no | `string` enum: `open`, `in_progress`, `blocked`, `deferred`, `review`, `closed` | — |
| `type` | query | no | `string` enum: `bug`, `feature`, `task`, `epic`, `chore` | — |
| `assignee` | query | no | `string` | — |
| `q` | query | no | `string` | Free-text search query |
| `priority` | query | no | `integer` | — |
| `labels` | query | no | `string` | Comma-separated label filters (all must match) |
| `source_repos` | query | no | `string` | Comma-separated source repo filters |
| `limit` | query | no | `integer` | — |
| `title_contains` | query | no | `string` | — |
| `description_contains` | query | no | `string` | — |
| `notes_contains` | query | no | `string` | — |
| `created_after` | query | no | `string` | RFC3339 or YYYY-MM-DD |
| `created_before` | query | no | `string` | RFC3339 or YYYY-MM-DD |
| `updated_after` | query | no | `string` | RFC3339 or YYYY-MM-DD |
| `updated_before` | query | no | `string` | RFC3339 or YYYY-MM-DD |
| `empty_description` | query | no | `boolean` | — |
| `no_assignee` | query | no | `boolean` | — |
| `no_labels` | query | no | `boolean` | — |
| `pinned` | query | no | `boolean` | — |
| `exclude_status` | query | no | `string` | Comma-separated statuses to exclude |
| `include_blocked` | query | no | `boolean` | Include blocked dependency info |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue list | `application/json`: `object` |
| `400` | Invalid filter parameters | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/issues`

Create a new issue

- **Operation ID:** `createIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [CreateIssueRequest](#schema-createissuerequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Issue created | `application/json`: `object` |
| `400` | Validation error | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/issues/search`

Full-text search issues in a workspace

- **Operation ID:** `searchIssues`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `q` | query | yes | `string` | Full-text search query, forwarded to the issue backend |
| `limit` | query | no | `integer` | Max results. Defaults to 100 and is clamped to a maximum of 500; invalid or non-positive values fall back to 100. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Matching issues | `application/json`: `object` |
| `400` | Missing search query 'q' | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/issues/{id}`

Get a single issue with full details

- **Operation ID:** `getIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue details | `application/json`: `object` |
| `404` | Issue not found | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `PATCH /api/workspaces/{ws}/issues/{id}`

Partially update an issue

- **Operation ID:** `patchIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [PatchIssueRequest](#schema-patchissuerequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue updated | `application/json`: `object` |
| `400` | Validation error | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Issue not found | — |

#### `DELETE /api/workspaces/{ws}/issues/{id}`

Delete an issue (tombstone)

- **Operation ID:** `deleteIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue deleted | `application/json`: [MessageResponse](#schema-messageresponse) |
| `404` | Issue not found | — |

#### `POST /api/workspaces/{ws}/issues/{id}/close`

Close an issue with optional reason

- **Operation ID:** `closeIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [CloseRequest](#schema-closerequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue closed | `application/json`: `object` |
| `404` | Issue not found | — |

#### `POST /api/workspaces/{ws}/issues/{id}/reopen`

Reopen a closed issue

Transitions a closed issue back to open status. The request body is
optional; an empty body or {} yields a status-only reopen. When `reason`
is set the backend records it as a comment on the issue (best-effort).

- **Operation ID:** `reopenIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `reason` | `string` | no | Optional reason, recorded as a comment (best-effort) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue reopened | `application/json`: `object` |
| `400` | Missing issue ID or invalid request body | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Issue not found | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/issues/{id}/claim`

Atomically claim an issue for the server-side actor

Claims an issue for the LOOM_AGENT_NAME configured on the server. Returns 409
Conflict if the issue is already claimed by another agent. The claim
operation is atomic — only one of N concurrent callers will succeed.
Blocked issues with open ready-work dependencies cannot be claimed.
Sets assignee and transitions status to in_progress in a single step.

- **Operation ID:** `claimIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue claimed | `application/json`: `object` |
| `400` | Missing issue ID | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Issue not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `409` | Issue already claimed by another agent or blocked by an open dependency | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/issues/{id}/move`

Move an issue to another workspace

- **Operation ID:** `moveIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [MoveIssueRequest](#schema-moveissuerequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue moved | `application/json`: `object` |
| `400` | Invalid target workspace | — |
| `404` | Issue not found | — |

### Tag: comments

Issue comments

#### `GET /api/workspaces/{ws}/issues/{id}/comments`

List comments for an issue

- **Operation ID:** `listComments`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue comments (empty array when the issue has none) | `application/json`: `object` |
| `400` | Missing issue ID | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/issues/{id}/comments`

Add a comment to an issue

- **Operation ID:** `addComment`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [CommentRequest](#schema-commentrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Comment added | `application/json`: `object` |
| `400` | Empty comment text | — |
| `404` | Issue not found | — |

### Tag: dependencies

Issue dependency management

#### `GET /api/workspaces/{ws}/issues/{id}/dependencies`

List an issue's dependencies

Returns the issue's dependencies projected as the *related* issue plus a
dependency_type discriminator (the FE IssueWithDependencyMetadata shape),
not the raw types.Dependency relation. Each entry's `id` is the issue on
the other side of the edge, not the issue being viewed.

- **Operation ID:** `listDependencies`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue dependencies (empty array when the issue has none) | `application/json`: `object` |
| `400` | Missing issue ID | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/issues/{id}/dependencies`

Add a dependency to an issue

- **Operation ID:** `addDependency`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [AddDependencyRequest](#schema-adddependencyrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Dependency added | `application/json`: `object` |
| `400` | Invalid dependency | — |
| `404` | Issue not found | — |

#### `DELETE /api/workspaces/{ws}/issues/{id}/dependencies/{depId}`

Remove a dependency from an issue

- **Operation ID:** `removeDependency`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |
| `depId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Dependency removed | `application/json`: `object` |
| `404` | Dependency not found | — |

### Tag: ready

Ready (unblocked) issue queue

#### `GET /api/workspaces/{ws}/ready`

List ready (unblocked) issues

Returns the canonical ready availability view from the issue backend:
open, non-epic work that is not canonically blocked and not currently
deferred. Filters may narrow this view, but they must not make
non-open statuses or epics ready.

- **Operation ID:** `listReady`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `assignee` | query | no | `string` | — |
| `type` | query | no | `string` enum: `bug`, `feature`, `task`, `epic`, `chore` | Filter by issue type. Epics are always excluded from the ready view. |
| `parent_id` | query | no | `string` | — |
| `mol_type` | query | no | `string` enum: `swarm`, `patrol`, `work` | — |
| `sort` | query | no | `string` enum: `hybrid`, `priority`, `oldest` | — |
| `unassigned` | query | no | `boolean` | — |
| `priority` | query | no | `integer` | — |
| `limit` | query | no | `integer` | — |
| `labels` | query | no | `string` | Comma-separated labels (all must match) |
| `labels_any` | query | no | `string` | Comma-separated labels (any match) |
| `source_repos` | query | no | `string` | Comma-separated source repo filters |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Ready issues | `application/json`: `object` |
| `400` | Invalid parameters | — |

### Tag: blocked

Blocked issues listing

#### `GET /api/workspaces/{ws}/blocked`

List blocked issues with blocker details

Returns canonical blocked work from the issue backend. For FleetDB-backed
workspaces this includes explicit status=blocked issues, issues blocked
by non-terminal dependencies, and descendants of blocked parents.
Dependency blocker IDs/details are returned when known; explicit
status-only and parent-only blocked issues may have no dependency
blocker IDs.

- **Operation ID:** `listBlocked`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `parent_id` | query | no | `string` | — |
| `assignee` | query | no | `string` | — |
| `type` | query | no | `string` enum: `bug`, `feature`, `task`, `epic`, `chore` | — |
| `priority` | query | no | `integer` | — |
| `limit` | query | no | `integer` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Blocked issues | `application/json`: `object` |

### Tag: graph

Dependency graph visualization

#### `GET /api/workspaces/{ws}/issues/graph`

Get dependency graph for visualization

- **Operation ID:** `getGraph`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `status` | query | no | `string` enum: `all`, `open`, `closed` | — |
| `include_closed` | query | no | `boolean` | — |
| `source_repos` | query | no | `string` | Comma-separated source repo filters |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Dependency graph | `application/json`: `object` |

### Tag: stats

Workspace and project statistics

#### `GET /api/workspaces/{ws}/stats`

Per-workspace statistics

- **Operation ID:** `getWorkspaceStats`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Statistics | `application/json`: [Statistics](#schema-statistics) |

### Tag: events

Issue audit events and SSE mutation stream

#### `GET /api/workspaces/{ws}/issues/{id}/events`

Get audit trail events for an issue

- **Operation ID:** `getIssueEvents`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Issue events | `application/json`: `object` |
| `404` | Issue not found | — |

#### `GET /api/workspaces/{ws}/events`

SSE mutation event stream

Server-Sent Events endpoint that streams real-time mutations.
Not expressible as a standard REST operation in OpenAPI.
The frontend's `useSSE` hook consumes this stream directly.

**Event format**: Each SSE `data:` line contains a JSON `MutationPayload`:
```json
{
  "type": "update",
  "entity_type": "issue",
  "entity_id": "proj-abc.5",
  "action": "issue.update",
  "issue_id": "proj-abc.5",
  "title": "Fix login bug",
  "assignee": "agent-1",
  "actor": "agent-1",
  "timestamp": "2026-04-06T12:00:00Z",
  "old_status": "open",
  "new_status": "in_progress",
  "parent_id": "proj-abc",
  "step_count": 3,
  "priority": 1,
  "source_repo": "my-repo",
  "workspace_id": "default"
}
```

**Mutation types**: `create`, `update`, `delete`, `comment`, `status`,
`bonded`, `squashed`, `burned`, `refresh`, `terminal_metadata`,
`terminal_session_change`, `issue_tabs`, `session_change`

**Authentication**: One-time token via the `token` query parameter
only. The user JWT middleware treats this as a public route and the
handler does its own check, which never reads an `Authorization`
header. In external auth mode, obtain the one-time SSE token from
`GET /api/workspaces/{ws}/events/token`; anything else — including a
valid SSE token sent as a Bearer header — returns `401`. In open auth
mode no credential is required.

- **Operation ID:** `subscribeEvents`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `token` | query | no | `string` | One-time SSE auth token (for EventSource clients) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | SSE event stream | `text/event-stream`: `string` |

#### `GET /api/workspaces/{ws}/events/token`

Get one-time SSE auth token status

- **Operation ID:** `getSSEToken`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | One-time token in external auth mode, or disabled marker in open mode | `application/json`: `object` |

### Tag: sessions

Session audit trail (task-scoped)

#### `GET /api/workspaces/{ws}/tasks/{taskId}/sessions`

List sessions for a task

- **Operation ID:** `listTaskSessions`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `taskId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Session list | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/tasks/{taskId}/sessions/{sessionId}`

Get session metadata

- **Operation ID:** `getSession`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `taskId` | path | yes | `string` | — |
| `sessionId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Session detail | `application/json`: `object` |
| `404` | Session not found | — |

#### `GET /api/workspaces/{ws}/tasks/{taskId}/sessions/{sessionId}/transcript`

Get session transcript entries

- **Operation ID:** `getSessionTranscript`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `taskId` | path | yes | `string` | — |
| `sessionId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Transcript | `application/json`: `object` |
| `404` | Session not found | — |

#### `GET /api/workspaces/{ws}/tasks/{taskId}/sessions/{sessionId}/diff`

Get session git diff

- **Operation ID:** `getSessionDiff`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `taskId` | path | yes | `string` | — |
| `sessionId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Diff content | `text/plain`: `string` |
| `404` | Session not found | — |

#### `POST /api/sessions/notify`

Notify SSE hub of session status change (agent-to-server)

- **Operation ID:** `notifySessionChange`
- **Auth:** **None** (public route)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `task_id` | `string` | yes | — |
| `session_id` | `string` | yes | — |
| `status` | `string` | yes | — |
| `workspace_id` | `string` | yes | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Notification received | — |

### Tag: session-history

Session history (issue-scoped, Redis-backed)

#### `GET /api/workspaces/{ws}/issues/{issueId}/sessions`

List session history records for an issue

- **Operation ID:** `listSessionHistory`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `issueId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Session history | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/issues/{issueId}/sessions/{recordId}/scrollback`

Get scrollback content for a session history record

- **Operation ID:** `getSessionScrollback`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `issueId` | path | yes | `string` | — |
| `recordId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Scrollback content | `text/plain`: `string` |
| `404` | Record not found | — |

### Tag: logs

Agent and task log retrieval

#### `GET /api/workspaces/{ws}/agents/{name}/logs`

Get recent log lines for an agent

- **Operation ID:** `getAgentLog`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |
| `lines` | query | no | `integer` | Number of lines to return |
| `before_line` | query | no | `integer` (`int64`) | Read lines before this line number (for pagination) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Log content | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/tasks/{id}/logs`

List available log phases for a task

- **Operation ID:** `listTaskPhases`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Available phases | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/tasks/{id}/logs/{phase}`

Get log content for a specific task phase

- **Operation ID:** `getTaskLog`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | — |
| `phase` | path | yes | `string` | — |
| `lines` | query | no | `integer` | Number of lines to return |
| `before_line` | query | no | `integer` (`int64`) | Read lines before this line number (for pagination) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Log content | `application/json`: `object` |

### Tag: terminal

Terminal session management, spawning, scrollback

#### `GET /api/config/terminal`

Terminal session lifecycle configuration

- **Operation ID:** `getTerminalConfig`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Terminal lifecycle config snapshot | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/terminal/token`

Generate one-time WebSocket auth token

- **Operation ID:** `getTerminalToken`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | One-time token | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/terminal/ws`

WebSocket terminal relay

WebSocket endpoint for terminal I/O relay. Not a standard REST
operation. Uses binary frames for terminal data and text frames
for control messages. The frontend's `terminalConnection.ts`
consumes this directly.

**Authentication**: One-time token via the `token` query parameter
only, generated by `GET /api/workspaces/{ws}/terminal/token`. The user
JWT middleware treats this as a public route and the handler does its
own check; an `Authorization` header is never read.

- **Operation ID:** `connectTerminalWS`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `token` | query | yes | `string` | — |
| `session` | query | no | `string` | Terminal session name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `101` | Switching Protocols (WebSocket upgrade) | — |
| `401` | Invalid or expired token | — |

### Tag: terminal-tabs

Terminal tab metadata (Redis-backed)

#### `GET /api/workspaces/{ws}/terminal/tabs`

List terminal tab metadata

- **Operation ID:** `listTerminalTabs`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tab list | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/terminal/tabs/{session}`

Get metadata for a single terminal tab

- **Operation ID:** `getTerminalTab`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `session` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tab metadata | `application/json`: `object` |
| `404` | Tab not found | — |

#### `PUT /api/workspaces/{ws}/terminal/tabs/{session}`

Create or replace terminal tab metadata

- **Operation ID:** `putTerminalTab`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `session` | path | yes | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [TabPutRequest](#schema-tabputrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tab saved | `application/json`: `object` |

#### `PATCH /api/workspaces/{ws}/terminal/tabs/{session}`

Partially update terminal tab metadata

- **Operation ID:** `patchTerminalTab`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `session` | path | yes | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [TabPatchRequest](#schema-tabpatchrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tab updated | `application/json`: `object` |

#### `DELETE /api/workspaces/{ws}/terminal/tabs/{session}`

Delete terminal tab metadata

- **Operation ID:** `deleteTerminalTab`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `session` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tab deleted | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/terminal/sessions/by-issue`

List terminal sessions grouped by issue

- **Operation ID:** `listSessionsByIssue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `issue_id` | query | no | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Sessions by issue | `application/json`: `object` |

#### `POST /api/workspaces/{ws}/terminal/setup`

Start a backend-owned setup command in a setup terminal

Opens or reuses the workspace's per-backend setup terminal and runs the
requested action (install / login / configure / test). For supported
backends (claude, codex, gemini, opencode, cursor) the resolved command
is run inside the PTY; for backends flagged as manual (cursor login)
the terminal shows credential guidance instead.

- **Operation ID:** `startTerminalSetup`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backend` | `string` | yes | AI backend name (claude, codex, gemini, opencode, cursor) |
| `action` | `string` enum: `install`, `login`, `configure`, `test` | yes | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Setup terminal created or reused | `application/json`: `object` |
| `400` | Invalid backend or action | — |
| `503` | Terminal manager not available | — |

### Tag: terminal-state

Terminal UI state persistence

#### `GET /api/workspaces/{ws}/terminal/state`

Get persisted terminal UI state

- **Operation ID:** `getTerminalState`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Terminal state | `application/json`: `object` |

#### `PATCH /api/workspaces/{ws}/terminal/state`

Update terminal UI state

- **Operation ID:** `patchTerminalState`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `active_tab` | `string` | no | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | State updated | `application/json`: `object` |

### Tag: agent-terminal

Per-agent terminal info and WebSocket

#### `GET /api/workspaces/{ws}/agents/{name}/terminal/info`

Get terminal info for a specific agent

- **Operation ID:** `getAgentTerminalInfo`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent terminal info | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents/{name}/terminal/token`

Generate one-time WebSocket token for agent terminal

- **Operation ID:** `getAgentTerminalToken`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | One-time token | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents/{name}/terminal/ws`

WebSocket terminal relay for agent

WebSocket endpoint for agent-specific terminal I/O. Uses the same
binary/text frame protocol as the main terminal WebSocket.
The frontend's `terminalConnection.ts` handles this directly.

**Authentication**: One-time token via the `token` query parameter
only, on the same terms as the main terminal WebSocket. The user JWT
middleware treats this as a public route and the handler does its own
check; an `Authorization` header is never read.

- **Operation ID:** `connectAgentTerminalWS`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |
| `token` | query | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `101` | Switching Protocols (WebSocket upgrade) | — |
| `401` | Invalid or expired token | — |

#### `POST /api/workspaces/{ws}/agents/{name}/terminal/session`

Ensure a persisted terminal session for an agent

Resolves an agent name to a persisted UUID terminal session and
returns its tab metadata, creating or rebuilding the tab's launch spec
when needed. Takes no request body. The response wraps the tab metadata
in a `{success, data}` envelope; error cases set `success:false` with an
`error` message.

- **Operation ID:** `ensureAgentTerminalSession`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Ensured agent terminal session | `application/json`: `object` |
| `400` | Invalid agent name or agent has no launch spec | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Agent not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Terminal service or agent store not initialized | `application/json`: `object` |

### Tag: agent-control

Agent lifecycle (start/stop/restart/yield)

#### `GET /api/workspaces/{ws}/interactive-prompts`

List built-in interactive terminal prompt options

- **Operation ID:** `listInteractivePrompts`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Built-in interactive prompts | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents`

List agents via daemon control socket

- **Operation ID:** `listAgents`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent list | `application/json`: `object` |
| `503` | Daemon unavailable | — |

#### `POST /api/workspaces/{ws}/agents`

Create an agent assignment

- **Operation ID:** `createAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace_key` | `string` | no | — |
| `name` | `string` | yes | — |
| `role_name` | `string` | yes | — |
| `kind` | `string` | no | Optional role kind for creating an interactive role. |
| `prompt` | `string` | no | Literal inline prompt text for interactive roles. |
| `prompt_file` | `string` | no | Custom or builtin prompt selector for interactive roles. |
| `auto` | `boolean` | no | — |
| `backend` | `string` | no | — |
| `fallback_backends` | array of `string` | no | — |
| `repos` | array of `string` | no | — |
| `repo_groups` | array of `string` | no | — |
| `cross_repo` | `boolean` | no | — |
| `parent` | `string` | no | — |
| `desired_state` | `string` | no | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Agent created | `application/json`: [WorkspaceAgentInfo](#schema-workspaceagentinfo) |
| `400` | Invalid agent create request | — |
| `409` | Agent already exists | — |
| `503` | Agent service unavailable | — |

#### `PATCH /api/workspaces/{ws}/agents/{name}`

Partial-update an agent assignment

Applies a partial update to an existing agent. Every field is
optional; only present fields are changed. `state` accepts
`idle`|`active`|`stopped` and `desired_state` accepts
`stopped`|`idle`|`running`|`draining` (an empty/omitted value leaves
the field unchanged). Broadcasts an `agent.refresh` mutation on success.

- **Operation ID:** `updateAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `role_name` | `string` | no | — |
| `auto` | `boolean` | no | — |
| `backend` | `string` | no | — |
| `fallback_backends` | array of `string` | no | — |
| `repos` | array of `string` | no | — |
| `repo_groups` | array of `string` | no | — |
| `cross_repo` | `boolean` | no | — |
| `parent` | `string` | no | — |
| `state` | `string` enum: `idle`, `active`, `stopped` | no | — |
| `desired_state` | `string` enum: `stopped`, `idle`, `running`, `draining` | no | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Updated agent | `application/json`: [Agent](#schema-agent) |
| `400` | Invalid update (bad state, desired_state, or body) | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Agent not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Internal error | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `DELETE /api/workspaces/{ws}/agents/{name}`

Delete an agent assignment

- **Operation ID:** `deleteAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent deleted | `application/json`: [MessageResponse](#schema-messageresponse) |
| `404` | Agent not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Internal error | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/agents/{name}/queue`

Ranked task queue for a single agent (daemon-backed)

Returns the daemon's ranked candidate task queue for one agent.
Served by the daemon-control-socket module; in fleet-db store mode the
same path is instead served by a stub that returns 501 Not Implemented
("agent-specific queue is not available in fleet-db store mode; use
monitor task queues").

- **Operation ID:** `getAgentQueue`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Ranked agent task queue | `application/json`: `object` |
| `404` | Agent not found in daemon config | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `501` | Not available in fleet-db store mode | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Daemon unavailable | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/agents/{name}/stop`

Stop an agent (yield by default, force with body)

Without `force` (or empty body): sends `agent_yield` to the daemon,
returns 202 Accepted. With `{"force": true}`: sends `agent_stop(force=true)`,
returns 200.

- **Operation ID:** `stopAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `force` | `boolean` | no | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent force-stopped | `application/json`: [MessageResponse](#schema-messageresponse) |
| `202` | Yield signal sent (graceful stop in progress) | `application/json`: [MessageResponse](#schema-messageresponse) |
| `503` | Daemon unavailable | — |

#### `POST /api/workspaces/{ws}/agents/{name}/start`

Start an agent

- **Operation ID:** `startAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent started | `application/json`: [MessageResponse](#schema-messageresponse) |
| `503` | Daemon unavailable | — |

#### `POST /api/workspaces/{ws}/agents/{name}/restart`

Restart an agent

- **Operation ID:** `restartAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent restarted | `application/json`: [MessageResponse](#schema-messageresponse) |
| `503` | Daemon unavailable | — |

#### `POST /api/workspaces/{ws}/agents/{name}/yield`

Yield an agent (graceful stop at next safe point)

- **Operation ID:** `yieldAgent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `202` | Yield signal sent | `application/json`: [MessageResponse](#schema-messageresponse) |
| `503` | Daemon unavailable | — |

### Tag: git

Git operations (status, push, pull, sync, PR, reset)

#### `GET /api/workspaces/{ws}/agents/{name}/git/status`

Get git status for an agent worktree

- **Operation ID:** `getGitStatus`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Git status | `application/json`: `object` |

#### `POST /api/workspaces/{ws}/agents/{name}/git/push`

Push agent worktree to remote

- **Operation ID:** `gitPush`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target` | `string` | no | Remote branch target |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Push result | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `POST /api/workspaces/{ws}/agents/{name}/git/pull`

Pull remote changes into agent worktree

- **Operation ID:** `gitPull`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source` | `string` | no | Remote branch source |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Pull result | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `POST /api/workspaces/{ws}/agents/{name}/git/sync`

Sync agent worktree (pull + push)

- **Operation ID:** `gitSync`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Sync result | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `POST /api/workspaces/{ws}/agents/{name}/git/pr`

Create a pull request from agent worktree

- **Operation ID:** `gitCreatePR`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | PR created | `application/json`: `object` |

#### `POST /api/workspaces/{ws}/agents/{name}/git/reset`

Reset agent worktree to integration branch

- **Operation ID:** `gitReset`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Reset complete | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `PATCH /api/workspaces/{ws}/agents/{name}/git/target`

Update agent git push target branch

- **Operation ID:** `updateGitTarget`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target` | `string` | no | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Target updated | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `POST /api/workspaces/{ws}/git/push-all`

Push all agent worktrees to remote

- **Operation ID:** `gitPushAll`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Push results | `application/json`: `object` |

### Tag: pull-requests

Pull request review read APIs

#### `GET /api/workspaces/{ws}/pull-requests`

List pull requests across the workspace's repos (gh-backed fallback)

Aggregates pull requests across every repo in the workspace via the
gh CLI. Registered only in serve modes without a store; store-backed
serve exposes the richer connector-primary listing at the same path.
Per-repo failures (non-GitHub remote, auth) are reported in warnings
without failing the whole listing.

- **Operation ID:** `listWorkspacePullRequests`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `state` | query | no | `string` enum: `all`, `open`, `merged`, `review` | Filter by PR state; defaults to all when omitted. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Pull request list | `application/json`: `object` |
| `502` | Upstream git/gh failure while listing pull requests | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}`

Get pull request detail via the GitHub connector

- **Operation ID:** `getPullRequestDetail`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Pull request detail | `application/json`: `object` |
| `400` | Invalid path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered in the workspace | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Connector egress unavailable | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/diff`

Get pull request diff via the GitHub connector

- **Operation ID:** `getPullRequestDiff`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Pull request diff | `application/json`: `object` |
| `400` | Invalid path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered in the workspace | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Connector egress unavailable | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/review`

Post a pull request review via the GitHub connector

- **Operation ID:** `postPullRequestReview`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [PullRequestReviewRequest](#schema-pullrequestreviewrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Pull request review result | `application/json`: `object` |
| `400` | Invalid request body or path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `403` | Connector grant denied | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered in the workspace | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `409` | Pull request head changed before review was posted | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `428` | Expected head SHA precondition is required | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Connector egress unavailable | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/reviewer`

Ensure a per-PR terminal reviewer agent

- **Operation ID:** `ensurePullRequestReviewer`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Reviewer agent and checkout | `application/json`: `object` |
| `400` | Invalid path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered or not checked out locally | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `409` | Pull request head changed while preparing the reviewer | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Worktree or reviewer setup failed | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `502` | Upstream pull request metadata was incomplete | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Connector egress unavailable | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/messages`

Send a message to a per-PR terminal reviewer agent

- **Operation ID:** `postPullRequestReviewerMessage`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [ReviewerMessageRequest](#schema-reviewermessagerequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Reviewer message delivery result | `application/json`: `object` |
| `400` | Invalid request body or path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered or reviewer was not started | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Reviewer message delivery failed | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/stream`

Stream the per-PR reviewer conversation

- **Operation ID:** `streamPullRequestReviewer`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | SSE event stream of reviewer status and messages | `text/event-stream`: `string` |
| `400` | Invalid path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered or reviewer was not started | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/pull-requests/{owner}/{repo}/{number}/conversation`

Snapshot of the per-PR reviewer conversation (poll target)

- **Operation ID:** `getPullRequestReviewerConversation`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `owner` | path | yes | `string` | — |
| `repo` | path | yes | `string` | — |
| `number` | path | yes | `integer` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Reviewer conversation snapshot | `application/json`: `object` |
| `400` | Invalid path parameter | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Repository is not registered or reviewer was not started | `application/json`: [ErrorResponse](#schema-errorresponse) |

### Tag: diff

Diff and commit inspection

#### `GET /api/workspaces/{ws}/issues/{id}/git/diff-stat`

Get git diff statistics for an issue

- **Operation ID:** `getIssueDiffStat`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | Issue identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Diff statistics | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents/{name}/git/diff-stat`

Get git diff statistics for an agent

- **Operation ID:** `getAgentDiffStat`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Diff statistics | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents/{name}/diff/commits`

List commits for an agent worktree

- **Operation ID:** `getDiffCommits`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Commit list | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents/{name}/diff/files`

List changed files in agent worktree

- **Operation ID:** `getDiffFiles`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Changed files | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/agents/{name}/diff/file`

Get diff for a specific file

- **Operation ID:** `getDiffFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Agent worktree name |
| `path` | query | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File diff | `application/json`: `object` |

### Tag: files

File tree browsing and editing

#### `GET /api/workspaces/{ws}/files/stat`

Get mutation metadata and a strong version for a scoped path

- **Operation ID:** `statScopedFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | — |
| `path` | query | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Path metadata | `application/json`: [FileStatResponse](#schema-filestatresponse) |

Response headers (`200`):

- `ETag` — `string` — Strong current path version.

#### `GET /api/workspaces/{ws}/files/capabilities`

Get effective workspace file-browser capabilities

- **Operation ID:** `getFileCapabilities`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Effective file-browser capabilities | `application/json`: [FileCapabilitiesResponse](#schema-filecapabilitiesresponse) |

#### `GET /api/workspaces/{ws}/files/tree`

Get file tree for a scoped file browser root

- **Operation ID:** `getScopedFileTree`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | no | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File tree | `application/json`: [FileTreeResponse](#schema-filetreeresponse) |

#### `GET /api/workspaces/{ws}/files/index`

Get quick-open file index for a scoped file browser root

- **Operation ID:** `getScopedFileIndex`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Quick-open file index | `application/json`: [FileIndexResponse](#schema-fileindexresponse) |

#### `POST /api/workspaces/{ws}/files/search`

Search files in a scoped file browser root

- **Operation ID:** `searchScopedFiles`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [FileSearchRequest](#schema-filesearchrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File search results | `application/json`: [FileSearchResponse](#schema-filesearchresponse) |

#### `GET /api/workspaces/{ws}/files/git-status`

Get git status decorations for a scoped file browser root

- **Operation ID:** `getScopedFileGitStatus`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Root-relative file paths mapped to raw git porcelain XY codes | `application/json`: [FileGitStatusResponse](#schema-filegitstatusresponse) |

#### `GET /api/workspaces/{ws}/files/checkouts`

List workspace file checkouts and local change counts

- **Operation ID:** `getFileCheckouts`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Known file checkouts | `application/json`: [FileCheckoutsResponse](#schema-filecheckoutsresponse) |

#### `POST /api/workspaces/{ws}/files/checkouts/repair`

Repair or provision a known workspace file checkout

- **Operation ID:** `repairFileCheckout`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [FileCheckoutRepairRequest](#schema-filecheckoutrepairrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Checkout repair result | `application/json`: [FileCheckoutRepairResponse](#schema-filecheckoutrepairresponse) |
| `400` | Unknown or disallowed checkout target | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/files/diff`

Get unified diff for a file in a scoped file browser root

- **Operation ID:** `getScopedFileDiff`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | yes | `string` | — |
| `from` | query | no | `string` | — |
| `to` | query | no | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Unified file diff | `application/json`: [FileDiffResponse](#schema-filediffresponse) |

#### `GET /api/workspaces/{ws}/files/history`

Get bounded commit history for a file

- **Operation ID:** `getScopedFileHistory`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File timeline | `application/json`: [FileHistoryResponse](#schema-filehistoryresponse) |

#### `GET /api/workspaces/{ws}/files/blame`

Get bounded git blame data for a file

- **Operation ID:** `getScopedFileBlame`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File blame data or skip signal | `application/json`: [FileBlameResponse](#schema-fileblameresponse) |

#### `GET /api/workspaces/{ws}/files`

Read a file from a scoped file browser root

- **Operation ID:** `readScopedFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | yes | `string` | — |
| `rev` | query | no | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File content | `application/json`: [FileReadResponse](#schema-filereadresponse) |

Response headers (`200`):

- `ETag` — `string` — Strong SHA-256 version for the current file (not revision reads).

#### `PUT /api/workspaces/{ws}/files`

Create or update a file in a scoped file browser root

- **Operation ID:** `writeScopedFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `path` | query | yes | `string` | — |
| `If-Match` | header | no | `string` | Optional strong version for replace/restore callers. Ordinary editor saves omit it and remain last-write-wins. |
| `If-None-Match` | header | no | `string` | Use * for create-only writes. |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [FileWriteRequest](#schema-filewriterequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File written | `application/json`: [FileMutationResponse](#schema-filemutationresponse) |
| `412` | A supplied file precondition no longer matches | — |

#### `DELETE /api/workspaces/{ws}/files`

Delete a file or directory in a scoped file browser root

- **Operation ID:** `deleteScopedFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | yes | `string` | — |
| `recursive` | query | no | `boolean` | — |
| `If-Match` | header | yes | `string` | Strong current source version from file stat/read. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | File or directory deleted | `application/json`: [MessageResponse](#schema-messageresponse) |
| `412` | Source version no longer matches | — |
| `428` | Source version is required | — |

#### `POST /api/workspaces/{ws}/files/mkdir`

Create a directory in a scoped file browser root

- **Operation ID:** `mkdirScopedFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |
| `repo` | query | no | `string` | Optional repo qualifier, valid only when scope=agent. |
| `path` | query | yes | `string` | — |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [FileRepoQualifierRequest](#schema-filerepoqualifierrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Directory created | `application/json`: [MessageResponse](#schema-messageresponse) |

#### `PATCH /api/workspaces/{ws}/files/move`

Move or rename a path in a scoped file browser root

- **Operation ID:** `moveScopedFile`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `scope` | query | no | `string` enum: `workspace`, `repo`, `agent` | — |
| `target` | query | no | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [FileMoveRequest](#schema-filemoverequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Path moved | `application/json`: [FileMutationResponse](#schema-filemutationresponse) |
| `412` | Source or destination version no longer matches | — |
| `428` | A required source or destination version is missing | — |

### Tag: editors

External editor detection and launch

#### `GET /api/editors`

List detected external editors

- **Operation ID:** `listEditors`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Editor list | `application/json`: `object` |

#### `POST /api/editors/open`

Open a file in an external editor

- **Operation ID:** `openEditor`
- **Auth:** `BearerAuth` (document default)

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [EditorOpenRequest](#schema-editoropenrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Editor launched | `application/json`: `object` |

### Tag: fleet

Fleet worker coordination

#### `POST /api/workspaces/{ws}/fleet/register`

Register a fleet worker

- **Operation ID:** `fleetRegister`
- **Auth:** `FleetApiKey`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Worker registered | `application/json`: `object` |
| `401` | Invalid API key | — |

#### `POST /api/workspaces/{ws}/fleet/claim`

Claim a task from the fleet queue

- **Operation ID:** `fleetClaim`
- **Auth:** `FleetJWT`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Task claimed or no work available | `application/json`: `object` |

#### `POST /api/workspaces/{ws}/fleet/done/{id}`

Mark a fleet task as done

- **Operation ID:** `fleetDone`
- **Auth:** `FleetJWT`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `id` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Task marked done | `application/json`: `object` |

#### `POST /api/workspaces/{ws}/fleet/heartbeat`

Fleet worker heartbeat

- **Operation ID:** `fleetHeartbeat`
- **Auth:** `FleetJWT`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Heartbeat acknowledged | `application/json`: `object` |

### Tag: workers

Internal worker API

#### `POST /api/internal/workers/register`

Register a remote worker

- **Operation ID:** `registerWorker`
- **Auth:** `WorkerToken`

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [WorkerRegisterRequest](#schema-workerregisterrequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Worker registered | `application/json`: `object` |
| `503` | Worker token not configured | — |

#### `DELETE /api/internal/workers/{id}`

Deregister a remote worker

- **Operation ID:** `deregisterWorker`
- **Auth:** `WorkerToken`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `id` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `204` | Worker deregistered | — |

#### `POST /api/internal/workers/{id}/state`

Update worker state (action, task assignment)

- **Operation ID:** `updateWorkerState`
- **Auth:** `WorkerToken`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `id` | path | yes | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | [WorkerStateRequest](#schema-workerstaterequest) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | State updated | — |

#### `POST /api/internal/workers/{id}/events`

Push events from a remote worker

- **Operation ID:** `pushWorkerEvents`
- **Auth:** `WorkerToken`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `id` | path | yes | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Events received | — |

#### `POST /api/internal/workers/{id}/logs`

Push log lines from a remote worker

- **Operation ID:** `pushWorkerLogs`
- **Auth:** `WorkerToken`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `id` | path | yes | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Logs received | — |

### Tag: issue-tabs

Issue detail tab state (Redis-backed)

#### `GET /api/workspaces/{ws}/issues/{issueId}/tabs`

Get issue detail tab state

- **Operation ID:** `getIssueTabs`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `issueId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tab state | `application/json`: `object` |

#### `PUT /api/workspaces/{ws}/issues/{issueId}/tabs`

Save issue detail tab state

- **Operation ID:** `saveIssueTabs`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `issueId` | path | yes | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `tabs` | array of [IssueTab](#schema-issuetab) | yes | — |
| `active_tab_id` | `string` | yes | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tabs saved | `application/json`: `object` |

#### `DELETE /api/workspaces/{ws}/issues/{issueId}/tabs`

Delete issue detail tab state

- **Operation ID:** `deleteIssueTabs`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `issueId` | path | yes | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Tabs deleted | `application/json`: `object` |

### Tag: monitor

Daemon monitor dashboard and metrics

#### `GET /api/workspaces/{ws}/monitor/status`

Full monitor dashboard data for a workspace

- **Operation ID:** `getWorkspaceMonitorStatus`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Dashboard status | `application/json`: [MonitorStatusResponse](#schema-monitorstatusresponse) |
| `404` | Workspace not found | — |
| `503` | Data collection unavailable | — |

#### `GET /api/daemon/config`

Effective resolved daemon config

- **Operation ID:** `getDaemonConfig`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Resolved daemon config | `application/json`: `object` |
| `503` | Daemon config could not be loaded | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/daemon/supervisor`

Daemon supervisor state and supervised agents

- **Operation ID:** `getDaemonSupervisor`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Supervisor state | `application/json`: `object` |
| `500` | Failed to read daemon state | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `503` | Daemon is not running | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/monitor/status`

Full monitor dashboard data

- **Operation ID:** `getMonitorStatus`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Dashboard status | `application/json`: [MonitorStatusResponse](#schema-monitorstatusresponse) |
| `503` | Data collection unavailable | — |

#### `GET /api/monitor/agents`

Agent list with optional workspace grouping

- **Operation ID:** `getMonitorAgents`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Agent list | `application/json`: [MonitorAgentsResponse](#schema-monitoragentsresponse) |
| `503` | Data collection unavailable | — |

#### `GET /api/monitor/tasks`

Task distribution by status

- **Operation ID:** `getMonitorTasks`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Task breakdown | `application/json`: [MonitorTasksResponse](#schema-monitortasksresponse) |
| `503` | Data collection unavailable | — |

#### `GET /api/monitor/stats`

Monitor statistics (distinct from /api/stats issue stats)

- **Operation ID:** `getMonitorStats`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Monitor stats | `application/json`: [MonitorStatsResponse](#schema-monitorstatsresponse) |
| `503` | Data collection unavailable | — |

#### `GET /api/monitor/sync`

Git sync status

- **Operation ID:** `getMonitorSync`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Sync status | `application/json`: [MonitorSyncResponse](#schema-monitorsyncresponse) |
| `503` | Data collection unavailable | — |

#### `GET /api/monitor/workspaces`

Workspace topology metadata

- **Operation ID:** `getMonitorWorkspaces`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Workspace info | `application/json`: [MonitorWorkspacesResponse](#schema-monitorworkspacesresponse) |

#### `GET /api/monitor/stale-detector`

Stale detector status

- **Operation ID:** `getStaleDetector`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Stale detector status | `application/json`: [StaleDetectorStatus](#schema-staledetectorstatus) |

#### `GET /api/monitor/usage`

Token usage aggregates

- **Operation ID:** `getMonitorUsage`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `agent` | query | no | `string` | — |
| `backend` | query | no | `string` | — |
| `epic` | query | no | `string` | — |
| `since` | query | no | `string` | YYYY-MM-DD |
| `until` | query | no | `string` | YYYY-MM-DD |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Usage data | `application/json`: [UsageResponse](#schema-usageresponse) |
| `400` | Invalid date parameter | — |
| `503` | Usage store not initialized | — |

#### `GET /metrics`

Prometheus scrape endpoint

- **Operation ID:** `getPrometheusMetrics`
- **Auth:** **None** (public route)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Prometheus metrics | `text/plain`: `string` |

### Tag: observability

Event metrics and paginated event log

#### `GET /api/observability/metrics`

Event metrics snapshot

- **Operation ID:** `getObservabilityMetrics`
- **Auth:** `BearerAuth` (document default)

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Metrics snapshot | `application/json`: [ObservabilityMetricsResponse](#schema-observabilitymetricsresponse) |
| `503` | Observability not configured | — |

#### `GET /api/observability/events`

Paginated event log

- **Operation ID:** `getObservabilityEvents`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `page` | query | no | `integer` | — |
| `per_page` | query | no | `integer` | — |
| `type` | query | no | `string` | Event type filter |
| `agent` | query | no | `string` | Agent name filter |
| `since` | query | no | `string` (`date-time`) | RFC3339 timestamp filter |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Paginated events | `application/json`: [ObservabilityEventsResponse](#schema-observabilityeventsresponse) |
| `400` | Invalid parameters | — |

### Tag: approvals

Await-approval resolution

#### `POST /api/workspaces/{ws}/approvals`

Record an approval or rejection decision for a pending await

Records one approval/rejection by the VERIFIED session user and feeds it to the await machinery (journal-first, then dispatch-time matching). The actor is always the authenticated session identity, never request data.

- **Operation ID:** `postWorkspaceApproval`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `subjectRef` | `string` | yes | Rendered subject the approval targets, e.g. "acme/widgets#7@shaA". |
| `eventType` | `string` | no | Await event type. Defaults to "approval". |
| `decision` | `string` enum: `approved`, `rejected` | no | Approval decision. Defaults to "approved". |
| `note` | `string` | no | Optional free-form reviewer note carried on the payload. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Decision recorded (and any pending awaits resolved) | `application/json`: `object` |
| `400` | Invalid body, or subjectRef/eventType do not render a subject-scoped key | `application/json`: [ApprovalError](#schema-approvalerror) |
| `401` | Missing verified session identity (fail closed) | `application/json`: [ApprovalError](#schema-approvalerror) |
| `403` | Session actor is not an eligible approver for the subject key | `application/json`: [ApprovalError](#schema-approvalerror) |
| `500` | Internal error | `application/json`: [ApprovalError](#schema-approvalerror) |
| `501` | Await store unavailable for this backend | `application/json`: [ApprovalError](#schema-approvalerror) |

### Tag: driver

Driver run SDK transport (run-scoped auth)

#### `POST /api/workspaces/{ws}/driver/events/await`

Register-and-check the run's next await (suspend or resolve inline)

Register-and-check the calling run's next durable await. Responds `satisfied`/`timed_out` inline (including idempotent replay of an already-finished awaitIndex, with the recorded event) or suspends the run and responds `suspended` (the runner then exits; resume re-runs from the top). Authentication is run-scoped and enforced in-handler, not by the user-JWT middleware: present a run-scoped Bearer token (identity derived from its claims) OR the legacy X-Loom-Driver-* header quad, optionally with a shared static Bearer token. The await instance key is derived server-side from the verified run id, never from the body.

- **Operation ID:** `awaitDriverEvent`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `X-Loom-Driver-Run-Id` | header | no | `string` | Parent DriverRun id. Required on the legacy header-quad transport; omitted when authenticating with a run-scoped Bearer token. A value conflicting with the token is rejected 401 identity_mismatch. |
| `X-Loom-Driver-Node-Id` | header | no | `string` | Parent DriverRun node id (legacy header transport only). |
| `X-Loom-Driver-Lease-Id` | header | no | `string` | Parent DriverRun lease id (legacy header transport only). |
| `X-Loom-Driver-Fencing-Token` | header | no | `string` | Positive int64 fencing token as a decimal string (legacy header transport only). |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `pattern` | `string` | no | Fully rendered subject-scoped key to wait for (eventType:subject, exact equality, no glob). |
| `actor` | — | no | Optional eligible-resolver allow-list; accepts a single string or an array of strings. |
| `timeoutMs` | `integer` (`int64`) | yes | Mandatory await timeout in milliseconds. |
| `awaitIndex` | `integer` | yes | 1-based ordinal of this await within the run. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Await outcome — suspended, or satisfied/timed_out with the recorded event inline. | `application/json`: `object` |
| `400` | Invalid request (e.g. unscoped pattern, missing timeout, malformed instance key). | `application/json`: [DriverOpError](#schema-driveroperror) |
| `401` | Unauthenticated, expired run token, or identity_mismatch. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `403` | Caller does not own the parent run, or actor not permitted to resolve. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `404` | Parent DriverRun not found or no longer running. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `409` | Conflict (e.g. run already resumed). | `application/json`: [DriverOpError](#schema-driveroperror) |
| `500` | Internal error. | `application/json`: [DriverOpError](#schema-driveroperror) |

#### `GET /api/workspaces/{ws}/driver/events/awaits`

List the calling run's awaits in index order

Returns the verified run's await registrations in instance-index order (terminal rows carry their recorded event and inline resume payload; pending rows are included) so a resumed workflow can rebuild context. Run-scoped auth is enforced in-handler (run token or legacy X-Loom-Driver-* header quad), not by the user-JWT middleware.

- **Operation ID:** `listDriverEventAwaits`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `X-Loom-Driver-Run-Id` | header | no | `string` | Parent DriverRun id (legacy header transport; omitted with a run-scoped Bearer token). |
| `X-Loom-Driver-Node-Id` | header | no | `string` | — |
| `X-Loom-Driver-Lease-Id` | header | no | `string` | — |
| `X-Loom-Driver-Fencing-Token` | header | no | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | The run's awaits in index order. | `application/json`: `object` |
| `401` | Unauthenticated, expired run token, or identity_mismatch. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `404` | Parent DriverRun not found or no longer running. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `500` | Internal error. | `application/json`: [DriverOpError](#schema-driveroperror) |

#### `GET /api/workspaces/{ws}/driver/watch/epic`

SSE stream of TaskRun journal events for an epic

Server-Sent Events stream of TaskRun journal events scoped to an epic. Handshake frame `event: snapshot` carries the epic scheduling snapshot plus the parent run's active task runs (id = cursor); each subsequent `event: taskRun` frame carries one journal event (id = the event Seq). Resume with the `Last-Event-ID` header or the `afterSeq` query param as an exclusive int64 Seq cursor. Every reconciliation tick re-verifies the parent run and re-sends a snapshot; when the parent is no longer running the stream ends with `event: closed` {code:"parent_not_running"}. Run-scoped auth is enforced in-handler; errors that occur before the stream commits to text/event-stream use the driver JSON error envelope. The epic id comes from the epicId query param, falling back to the parent run's epic.

- **Operation ID:** `watchDriverEpic`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `epicId` | query | no | `string` | Epic to scope the stream to. Falls back to the parent DriverRun's epic id; required if neither is present (400). |
| `afterSeq` | query | no | `integer` (`int64`) | Exclusive int64 Seq resume cursor (alternative to the Last-Event-ID header). 0/absent streams from the start of the journal. |
| `Last-Event-ID` | header | no | `string` | SSE reconnect cursor (exclusive int64 Seq); takes precedence over afterSeq. |
| `X-Loom-Driver-Run-Id` | header | no | `string` | Parent DriverRun id (legacy header transport; omitted with a run-scoped Bearer token). |
| `X-Loom-Driver-Node-Id` | header | no | `string` | — |
| `X-Loom-Driver-Lease-Id` | header | no | `string` | — |
| `X-Loom-Driver-Fencing-Token` | header | no | `string` | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | SSE stream. Frames: `snapshot` (epic + active task runs), `taskRun` (one journal event, id = Seq), `closed` (terminal, e.g. {code:"parent_not_running"}). | `text/event-stream`: `string` |
| `400` | Missing epic id or malformed resume cursor. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `401` | Unauthenticated, expired run token, or identity_mismatch. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `404` | Parent DriverRun not found or no longer running. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `500` | Internal error or streaming unsupported. | `application/json`: [DriverOpError](#schema-driveroperror) |

#### `POST /api/workspaces/{ws}/driver/workflows/await`

Await a child workflow run's completion

Await sugar over the pattern `run.finished:{childRunId}`: validates the child belongs to the caller, then runs the standard events/await flow (same suspended / satisfied / timed_out wire shapes), consuming a normal awaitIndex slot. A non-suspended response additionally carries the child's terminal status, summary and errorClass so a resumed parent branches without a second fetch. Run-scoped auth enforced in-handler.

- **Operation ID:** `awaitDriverWorkflow`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `X-Loom-Driver-Run-Id` | header | no | `string` | Parent DriverRun id (legacy header transport; omitted with a run-scoped Bearer token). |
| `X-Loom-Driver-Node-Id` | header | no | `string` | — |
| `X-Loom-Driver-Lease-Id` | header | no | `string` | — |
| `X-Loom-Driver-Fencing-Token` | header | no | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `childRunId` | `string` | yes | — |
| `timeoutMs` | `integer` (`int64`) | yes | Mandatory await timeout in milliseconds. |
| `awaitIndex` | `integer` | yes | 1-based await ordinal (consumes a normal await slot). |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Await outcome plus the awaited child's state (child absent when suspended). | `application/json`: `object` |
| `400` | Invalid request (missing timeout, bad await index, malformed child reference). | `application/json`: [DriverOpError](#schema-driveroperror) |
| `401` | Unauthenticated, expired run token, or identity_mismatch. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `403` | Caller does not own the parent run or the referenced child. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `404` | Parent DriverRun or child run not found. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `500` | Internal error. | `application/json`: [DriverOpError](#schema-driveroperror) |

#### `POST /api/workspaces/{ws}/driver/workflows/start`

Start (or idempotently re-read) a child workflow run

Create — or idempotently re-read — the deterministic child run of the verified caller: re-entry re-issuing the same start returns the same childRunId, never a duplicate. Provide `idempotencyKey`, or omit it and supply the SDK's 1-based per-run `startIndex`. Run-scoped auth enforced in-handler.

- **Operation ID:** `startDriverWorkflow`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `X-Loom-Driver-Run-Id` | header | no | `string` | Parent DriverRun id (legacy header transport; omitted with a run-scoped Bearer token). |
| `X-Loom-Driver-Node-Id` | header | no | `string` | — |
| `X-Loom-Driver-Lease-Id` | header | no | `string` | — |
| `X-Loom-Driver-Fencing-Token` | header | no | `string` | — |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workflowName` | `string` | yes | Registered workflow (driver) to run. |
| `input` | — | no | Child's initial payload (raw JSON); empty means {}. |
| `idempotencyKey` | `string` | no | Keys the deterministic child identity. When absent, startIndex is required. |
| `startIndex` | `integer` | no | SDK 1-based per-run start counter; required when idempotencyKey is absent. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | The created (or replayed) child run. | `application/json`: `object` |
| `400` | Invalid request (missing workflowName, or neither idempotencyKey nor startIndex). | `application/json`: [DriverOpError](#schema-driveroperror) |
| `401` | Unauthenticated, expired run token, or identity_mismatch. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `404` | Parent DriverRun not found or no longer running. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `409` | Conflict on deterministic child identity. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `500` | Internal error. | `application/json`: [DriverOpError](#schema-driveroperror) |

#### `POST /api/workspaces/{ws}/driver/{op}`

Driver-op dispatcher (opaque op path parameter)

Single-segment driver-op dispatcher (SDK v2 transport). The `{op}` path segment selects the operation; the request body is an op-specific camelCase JSON object and the 200 response is an op-specific JSON object. An empty body is treated as `{}`; bodies are capped at 8 MiB. Run-scoped auth is enforced in-handler (run token or legacy X-Loom-Driver-* header quad + optional shared static token); most ops additionally prove the caller owns a running parent DriverRun. Errors use the driver envelope {error:{code,message,retryable,details?}}; an unknown op returns 404 unknown_op. The op set is defined as the keys of the ops map in driverapi.NewModule (internal/webui/handlers/driverapi/module.go). NOTE: the two-segment paths events/await, events/awaits, workflows/start, workflows/await and watch/epic are registered as their own routes and are not dispatched here.

- **Operation ID:** `dispatchDriverOp`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `op` | path | yes | `string` enum: `claim-ready`, `epic-get`, `epic-snapshot`, `list-agents`, `agent-orchestration-session`, `update-agent-parent`, `deliver-lead-assignment`, `deliver-agent-message`, `exec-task`, `task-run-get`, `active-task-runs`, `recover-stale-tasks`, `complete-task`, `release-task`, `connector-dispatch`, `emit-event` | The driver operation to invoke. |
| `X-Loom-Driver-Run-Id` | header | no | `string` | Parent DriverRun id (legacy header transport; omitted with a run-scoped Bearer token). |
| `X-Loom-Driver-Node-Id` | header | no | `string` | — |
| `X-Loom-Driver-Lease-Id` | header | no | `string` | — |
| `X-Loom-Driver-Fencing-Token` | header | no | `string` | — |

**Request body** (optional)

Op-specific camelCase JSON parameters (decoded per op). Empty body is treated as {}.

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Op-specific result (each op returns its own JSON object). | `application/json`: `object` |
| `400` | Invalid request payload or op parameters. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `401` | Unauthenticated, expired run token, or identity_mismatch. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `403` | Caller does not own the parent run. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `404` | Unknown op, or a referenced resource / parent run not found. | `application/json`: [DriverOpError](#schema-driveroperror) |
| `409` | Conflict (e.g. already claimed, invalid transition, unschedulable). | `application/json`: [DriverOpError](#schema-driveroperror) |
| `500` | Internal error. | `application/json`: [DriverOpError](#schema-driveroperror) |

### Tag: workflows

Workflow and driver run management

#### `POST /api/workspaces/{ws}/workflows/{name}`

Start a workflow run

Resolves the named workflow to a driver (self-healing builtins on
demand), runs an AI-CLI-backend preflight when the run would execute
locally, then creates and enqueues a driver run. The request body is
the opaque driver run payload passed through verbatim; an empty body
is treated as {}. A recognised convention is an optional epic_id.

- **Operation ID:** `createWorkflowRun`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Workflow name |
| `Idempotency-Key` | header | no | `string` | Optional idempotency key for run creation. |

**Request body** (optional)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `202` | Run accepted and enqueued | `application/json`: [DriverRun](#schema-driverrun) |
| `400` | Invalid payload or failed runner preflight | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Workflow not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Run creation failed | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `POST /api/workspaces/{ws}/workflows/{name}/versions`

Register a workflow version from uploaded source

Builds the uploaded TypeScript source tree and registers it as a
driver version for the named workflow. Request body is limited to
4 MiB. When activate is omitted it defaults to true.

- **Operation ID:** `createWorkflowVersion`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Workflow name |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `files` | `object` | yes | Map of workspace-relative file path to file source. |
| `entrypoint` | `string` | no | Entry file path; defaults to workflows/{name}.ts. |
| `activate` | `boolean` | no | Activate this version after registering. Defaults to true. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Version registered (or reused) | `application/json`: `object` |
| `400` | Missing/invalid files, entrypoint, or build failure | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/runs/{runId}`

Get a workflow/driver run

- **Operation ID:** `getWorkflowRun`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `runId` | path | yes | `string` | Driver run identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Driver run | `application/json`: [DriverRun](#schema-driverrun) |
| `404` | Run not found | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/runs/{runId}/events`

List a run's events (paginated)

- **Operation ID:** `getWorkflowRunEvents`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `runId` | path | yes | `string` | Driver run identifier |
| `after` | query | no | `string` | Opaque cursor; return events after this position. |
| `limit` | query | no | `integer` | Page size; defaults to 100. |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Event page | `application/json`: [PlatformEventsPage](#schema-platformeventspage) |
| `400` | Invalid limit | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `404` | Run not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `501` | Run event stream unavailable for this store | `application/json`: [ErrorResponse](#schema-errorresponse) |

#### `GET /api/workspaces/{ws}/runs/{runId}/stream`

Stream a run's events (SSE)

Server-Sent Events stream of a run's events. Each new event is sent as
an SSE frame `event: event` whose data is a PlatformEvent JSON object;
reader errors are sent as `event: error` with a {error} payload. The
server polls roughly once per second until the client disconnects.

- **Operation ID:** `streamWorkflowRunEvents`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `runId` | path | yes | `string` | Driver run identifier |
| `after` | query | no | `string` | Opaque cursor; stream events after this position. Defaults to "0". |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | SSE stream of run events | `text/event-stream`: `string` |
| `404` | Run not found | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `500` | Streaming unavailable (response writer cannot flush) | `application/json`: [ErrorResponse](#schema-errorresponse) |
| `501` | Run event stream unavailable for this store | `application/json`: [ErrorResponse](#schema-errorresponse) |

### Tag: task-run

Task-run SDK transport (lease-scoped auth)

#### `PUT /api/workspaces/{ws}/task-run/artifacts/{artifactId}/content`

Upload raw content for a task-run artifact

Uploads the raw bytes of a task-run artifact declared earlier via the
`artifact-declare` op. The request body is the content itself (which is
why this cannot ride the JSON `{op}` route), and the request
`Content-Type` header is stored as the artifact's MIME type. The
artifact must belong to the caller's verified task run; a foreign or
unknown artifact is reported as `not_found`. Max body size: 64 MiB.

- **Operation ID:** `uploadTaskRunArtifactContent`
- **Auth:** `TaskRunLease`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `artifactId` | path | yes | `string` | Artifact identifier, scoped to the caller's task run |
| `X-Loom-Task-Run-Id` | header | yes | `string` | Task-run identity (mirrors LOOM_TASK_RUN_ID) |
| `X-Loom-Task-Run-Fencing-Token` | header | yes | `integer` (`int64`) | Monotonic lease fencing token; must be a positive integer |
| `X-Loom-Task-Run-Node-Id` | header | no | `string` | Node identity for the fenced ownership check (mirrors LOOM_TASK_RUN_NODE_ID) |
| `X-Loom-Task-Run-Lease-Id` | header | no | `string` | Lease identity for the fenced ownership check (mirrors LOOM_TASK_RUN_LEASE_ID) |

**Request body** (required)

Raw artifact content. The request Content-Type is stored as the artifact MIME type.

| Content-Type | Schema |
|--------------|--------|
| `application/octet-stream` | `string` (`binary`) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Content uploaded; the updated artifact record | `application/json`: [TaskRunArtifact](#schema-taskrunartifact) |
| `400` | Invalid request (unreadable body or over the size cap) | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `401` | Missing/invalid lease credentials, or the lease is superseded or the run is terminal | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `404` | Artifact does not exist or does not belong to the caller's task run | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `500` | Internal error | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |

#### `POST /api/workspaces/{ws}/task-run/{op}`

Task-run operation dispatcher (opaque op)

Opaque operation dispatcher for the task-runner API — the same transport
shape as the driver `{op}` route. `{op}` selects one of a fixed set of
operations; the request and response bodies are op-specific camelCase
JSON. The op set is defined in
`internal/webui/handlers/taskrunapi/module.go` (`NewModule`):
`get`, `task-get`, `heartbeat`, `log-append`, `complete`,
`runtime-credential`, `artifact-declare`, `artifact-get`,
`artifact-list`, `artifact-finalize`. An unknown op returns 404 with
code `unknown_op`. An empty request body is treated as `{}`. Read-only
and artifact ops first prove lease ownership via a fenced no-op
heartbeat; mutating ops (`heartbeat`, `log-append`, `complete`) pass the
lease credentials straight through to fleet-db's fenced task-run
methods. Errors use the structured envelope
`{error:{code,message,retryable,details}}`.

- **Operation ID:** `taskRunOp`
- **Auth:** `TaskRunLease`

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `op` | path | yes | `string` enum: `get`, `task-get`, `heartbeat`, `log-append`, `complete`, `runtime-credential`, `artifact-declare`, `artifact-get`, `artifact-list`, `artifact-finalize` | Task-run operation name |
| `X-Loom-Task-Run-Id` | header | yes | `string` | Task-run identity (mirrors LOOM_TASK_RUN_ID) |
| `X-Loom-Task-Run-Fencing-Token` | header | yes | `integer` (`int64`) | Monotonic lease fencing token; must be a positive integer |
| `X-Loom-Task-Run-Node-Id` | header | no | `string` | Node identity for the fenced ownership check (mirrors LOOM_TASK_RUN_NODE_ID) |
| `X-Loom-Task-Run-Lease-Id` | header | no | `string` | Lease identity for the fenced ownership check (mirrors LOOM_TASK_RUN_LEASE_ID) |

**Request body** (optional)

Op-specific camelCase JSON parameters. The shape depends on `{op}`;
see the op handlers in
`internal/webui/handlers/taskrunapi/module.go` and `artifacts.go`.
An empty body is treated as `{}`. Max size: 8 MiB.

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Op-specific result. Depending on `{op}` this is a task-run view (`get`, `heartbeat`), a `{taskRun, task}` pair (`task-get`), a log entry (`log-append`), a `{completion, taskRun}` pair (`complete`), an artifact view or `{artifacts:[...]}` (artifact ops), or a runtime-credential payload (`runtime-credential`). | `application/json`: `object` |
| `400` | Invalid op payload (code `invalid`) | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `401` | Missing/invalid lease credentials, or the lease is superseded, or the run is terminal (code `unauthenticated` or `lease_denied`) | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `403` | Fenced pass-through op rejected ownership (code `not_owner`) | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `404` | Unknown op or referenced resource not found (code `unknown_op` or `not_found`) | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `409` | Op-level conflict or invalid state transition (code `conflict` or `invalid_transition`) | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |
| `500` | Internal error | `application/json`: [TaskRunOpError](#schema-taskrunoperror) |

### Tag: triggers

Persisted trigger events and deliveries (webhook audit trail)

#### `GET /api/workspaces/{ws}/trigger-events`

List persisted trigger events (webhook ingest audit trail)

- **Operation ID:** `listTriggerEvents`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `source_kind` | query | no | `string` | Filter by event source kind (e.g. github) |
| `trigger_binding_id` | query | no | `string` | Filter by the trigger binding that matched the event |
| `limit` | query | no | `integer` | Max rows to return (1-1000) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Trigger event list | `application/json`: `object` |
| `400` | Invalid limit parameter (bare {error} body, not the standard ErrorResponse envelope) | `application/json`: `object` |
| `500` | List failed (bare {error} body) | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/trigger-events/{eventId}`

Get a single persisted trigger event

- **Operation ID:** `getTriggerEvent`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `eventId` | path | yes | `string` | Trigger event identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Trigger event | `application/json`: [TriggerEvent](#schema-triggerevent) |
| `404` | Trigger event not found (bare {error} body, not the standard ErrorResponse envelope) | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/trigger-deliveries`

List persisted trigger deliveries (fan-out dispatch audit trail)

- **Operation ID:** `listTriggerDeliveries`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `trigger_event_id` | query | no | `string` | Filter by the originating trigger event |
| `trigger_binding_id` | query | no | `string` | Filter by the trigger binding that produced the delivery |
| `status` | query | no | `string` enum: `accepted`, `rejected`, `duplicate`, `queued`, `dispatched`, `failed`, `replayed`, `superseded`, `held` | Filter by delivery lifecycle status |
| `limit` | query | no | `integer` | Max rows to return (1-1000) |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Trigger delivery list | `application/json`: `object` |
| `400` | Invalid limit parameter (bare {error} body, not the standard ErrorResponse envelope) | `application/json`: `object` |
| `500` | List failed (bare {error} body) | `application/json`: `object` |

#### `GET /api/workspaces/{ws}/trigger-deliveries/{deliveryId}`

Get a single persisted trigger delivery

- **Operation ID:** `getTriggerDelivery`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `deliveryId` | path | yes | `string` | Trigger delivery identifier |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `200` | Trigger delivery | `application/json`: [TriggerDelivery](#schema-triggerdelivery) |
| `404` | Trigger delivery not found (bare {error} body, not the standard ErrorResponse envelope) | `application/json`: `object` |

### Tag: webhooks

Inbound webhook ingestion

#### `POST /api/workspaces/{ws}/webhooks/{name}`

Ingest an inbound webhook delivery for the named adapter

Public endpoint (no user-JWT). The request is authenticated inside the
handler by the named adapter's per-binding signature verification
(e.g. GitHub `X-Hub-Signature-256`). The adapter normalizes
adapter-specific HTTP headers (delivery id, event type, subject/actor)
into routing metadata; the JSON request body is the raw provider
payload and is not otherwise interpreted. On success the delivery is
durably dispatched (fan-out) and 202 is returned with one entry per
matched binding.

- **Operation ID:** `receiveWebhook`
- **Auth:** **None** (public route)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |
| `name` | path | yes | `string` | Registered webhook adapter name (e.g. github) |

**Request body** (required)

Raw provider webhook payload. Must be valid JSON (an empty body is
treated as {}). Not decoded into a fixed schema; the concrete shape
depends on the adapter and provider event.

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `202` | Delivery accepted and durably dispatched | `application/json`: `object` |
| `400` | Malformed payload, empty/invalid JSON, or missing adapter delivery id (bare {error} body) | `application/json`: `object` |
| `401` | Signature verification failed (bare {error} body) | `application/json`: `object` |
| `404` | No adapter registered for {name}, or no enabled trigger binding for the resolved route (bare {error} body) | `application/json`: `object` |
| `501` | Trigger dispatch is unavailable for this store (bare {error} body) | `application/json`: `object` |

### Tag: onboarding

#### `POST /api/workspaces/{ws}/onboarding/first-task`

Create the onboarding first task and queue an agent start

Creates an open, unassigned issue and requests the named agent start
working on it via the lifecycle backend. If the lifecycle request
fails the just-created issue is deleted in a bounded cleanup so the
kanban does not accumulate orphans on disconnect.

- **Operation ID:** `runOnboardingFirstTask`
- **Auth:** `BearerAuth` (document default)

**Parameters**

| Name | In | Required | Type | Description |
|------|----|----------|------|-------------|
| `ws` | path | yes | `string` | Workspace identifier |

**Request body** (required)

| Content-Type | Schema |
|--------------|--------|
| `application/json` | `object` |

Body fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `agent_name` | `string` | yes | — |
| `title` | `string` | yes | — |
| `description` | `string` | no | — |
| `issue_type` | `string` | no | — |
| `priority` | `integer` | no | — |
| `source_repo` | `string` | no | — |

**Responses**

| Status | Description | Schema |
|--------|-------------|--------|
| `201` | Issue created and agent start queued | `application/json`: `object` |
| `400` | Validation error (missing agent_name or title) | — |
| `404` | Agent not found | — |
| `503` | Agent lifecycle backend unavailable | — |

## Schemas

Reusable schemas from `api/openapi.yaml` (`components.schemas`), sorted by name.

### Schema: AddDependencyRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `depends_on_id` | `string` | yes | — |
| `dep_type` | `string` | no | — |

### Schema: Agent

Full agent assignment (domain.Agent). Richer than WorkspaceAgentInfo. live_status/active_task_id/active_phase/last_error_class are DERIVED, read-only fields carried from fleet-db.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace_key` | `string` | yes | — |
| `name` | `string` | yes | — |
| `role_name` | `string` | yes | — |
| `auto` | `boolean` | no | — |
| `backend` | `string` | no | — |
| `fallback_backends` | array of `string` | no | — |
| `repos` | array of `string` | no | — |
| `repo_groups` | array of `string` | no | — |
| `cross_repo` | `boolean` | no | — |
| `parent` | `string` | no | — |
| `state` | `string` enum: `idle`, `active`, `stopped`, `backend_unavailable` | no | — |
| `mode` | `string` enum: `ephemeral`, `service` | no | — |
| `task_filter` | `string` | no | — |
| `max_concurrency` | `integer` | no | — |
| `budget_policy` | `string` | no | — |
| `desired_state` | `string` enum: `stopped`, `idle`, `running`, `draining` | no | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `updated_at` | `string` (`date-time`) | yes | — |
| `live_status` | `string` enum: `working`, `idle` | no | Derived read-only liveness from fleet-db. |
| `active_task_id` | `string` | no | Derived; set only when live_status is working. |
| `active_phase` | `string` | no | Derived; set only when live_status is working. |
| `last_error_class` | `string` | no | Derived; error_class of the most recent failed terminal session, surfaced while idle. |

### Schema: AgentBackendOverride

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `worktree` | `string` | yes | — |
| `role` | `string` | yes | — |
| `backend` | `string` | yes | — |

### Schema: AgentControlEntry

Agent entry from daemon control socket list

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `role` | `string` | yes | — |
| `status` | `string` | yes | — |

### Schema: AgentQueueEntry

A single scored candidate issue in an agent's ranked queue.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `issue_id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `priority` | `integer` | yes | — |
| `score` | `integer` | yes | — |
| `reason` | `string` | yes | — |
| `labels` | array of `string` | yes | — |
| `parent` | `string` | no | — |

### Schema: AgentStatusResponse

Agent entity from dto.AgentStatusResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `description` | `string` | no | — |
| `status` | `string` | no | — |
| `agent_state` | `string` enum: `idle`, `spawning`, `running`, `working`, `stuck`, `done`, `stopped`, `dead` | no | — |
| `role_type` | `string` | no | — |
| `rig` | `string` | no | — |
| `last_activity` | `string` (`date-time`), nullable | no | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `updated_at` | `string` (`date-time`) | yes | — |
| `labels` | array of `string` | yes | — |

### Schema: ApprovalError

Error envelope for the approvals endpoint (distinct from ErrorResponse).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `error` | `object` | yes | — |

### Schema: BackendConfigResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` | yes | — |
| `data` | `object` | no | — |
| `error` | `string` | no | — |

### Schema: BlockedIssue

Canonically blocked issue with blocking information when available.

- **Type:** allOf([Issue](#schema-issue) + `object`)

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `blocked_by_count` | `integer` | yes | — |
| `blocked_by` | array of `string` | yes | Dependency blocker IDs when known. May be empty for explicit status-only or parent-propagated blocking. |
| `blocked_by_details` | array of [BlockerRef](#schema-blockerref) | no | — |

### Schema: BlockerRef

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `priority` | `integer` | yes | — |

### Schema: CloseRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `reason` | `string` | no | — |
| `session` | `string` | no | — |
| `suggest_next` | `boolean` | no | — |
| `force` | `boolean` | no | — |

### Schema: Comment

Comment from types.Comment

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `integer` (`int64`) | yes | — |
| `issue_id` | `string` | yes | — |
| `author` | `string` | yes | — |
| `text` | `string` | yes | — |
| `created_at` | `string` (`date-time`) | yes | — |

### Schema: CommentRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `text` | `string` | yes | — |

### Schema: CommentResponse

Comment in IssueResponse (dto.CommentResponse)

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `integer` (`int64`) | yes | — |
| `author` | `string` | yes | — |
| `text` | `string` | yes | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `parent_id` | `integer` (`int64`), nullable | no | — |
| `edited_at` | `string` (`date-time`), nullable | no | — |

### Schema: CreateIssueRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | `string` | yes | — |
| `issue_type` | `string` enum: `bug`, `feature`, `task`, `epic`, `chore` | yes | — |
| `priority` | `integer` | yes | — |
| `id` | `string` | no | — |
| `parent` | `string` | no | — |
| `description` | `string` | no | — |
| `status` | `string` enum: `open`, `deferred` | no | — |
| `design` | `string` | no | — |
| `acceptance_criteria` | `string` | no | — |
| `notes` | `string` | no | — |
| `assignee` | `string` | no | — |
| `owner` | `string` | no | — |
| `created_by` | `string` | no | — |
| `external_ref` | `string` | no | — |
| `estimated_minutes` | `integer`, nullable | no | — |
| `labels` | array of `string` | no | — |
| `dependencies` | array of `string` | no | — |
| `due_at` | `string` | no | — |
| `defer_until` | `string` | no | — |

### Schema: Dependency

Full dependency relation from types.Dependency

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `issue_id` | `string` | yes | — |
| `depends_on_id` | `string` | yes | — |
| `type` | `string` | yes | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `created_by` | `string` | no | — |
| `metadata` | `string` | no | — |
| `thread_id` | `string` | no | — |

### Schema: DependencyRef

Slim reference to a dependency/dependent issue

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `status` | `string` | yes | — |
| `priority` | `integer` | yes | — |
| `type` | `string` | yes | Dependency type (e.g. "blocks") |
| `issue_type` | `string` | yes | — |

### Schema: DriverOpError

Structured v2 error envelope for the driver SDK transport ({error:{code,message,retryable,details?}}). Distinct from ErrorResponse.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `error` | `object` | yes | — |

### Schema: DriverRun

One execution request for a driver version (domain.DriverRun).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace_key` | `string` | yes | — |
| `run_id` | `string` | yes | — |
| `driver_id` | `string` | yes | — |
| `driver_version_id` | `string` | yes | — |
| `entrypoint` | `string` | no | — |
| `source_kind` | `string` | no | — |
| `source_ref` | `string` | no | — |
| `epic_id` | `string` | no | — |
| `status` | `string` | yes | — |
| `node_id` | `string` | no | — |
| `lease_id` | `string` | no | — |
| `fencing_token` | `integer` (`int64`) | no | — |
| `idempotency_key` | `string` | no | — |
| `payload` | `object` | no | Opaque driver run payload (raw JSON). |
| `output` | `object` | no | — |
| `summary` | `string` | no | — |
| `error_class` | `string` | no | — |
| `started_at` | `string` (`date-time`) | no | — |
| `last_heartbeat` | `string` (`date-time`) | no | — |
| `finished_at` | `string` (`date-time`), nullable | no | — |
| `parent_run_id` | `string` | no | — |

### Schema: EditorInfo

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `display_name` | `string` | yes | — |
| `icon_name` | `string` | yes | — |
| `detected` | `boolean` | yes | — |

### Schema: EditorOpenRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `editor_id` | `string` | yes | — |
| `path` | `string` | yes | — |

### Schema: ErrorResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` const `false` | yes | — |
| `error` | `string` | yes | — |
| `code` | `string` | no | — |
| `retryable` | `boolean` | no | — |
| `details` | `object` | no | — |

### Schema: FileBlameLine

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `line` | `integer` | yes | — |
| `lines` | `integer` | yes | — |
| `sha` | `string` | yes | — |
| `author` | `string` | yes | — |
| `time` | `string` | yes | — |
| `summary` | `string` | yes | — |

### Schema: FileBlameResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `skipped` | `boolean` | yes | — |
| `reason` | `string` | no | — |
| `message` | `string` | no | — |
| `lines` | array of [FileBlameLine](#schema-fileblameline) | yes | — |
| `partial` | `boolean` | yes | — |
| `limit_hit` | `boolean` | yes | — |

### Schema: FileCapabilitiesResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `read` | `boolean` | yes | — |
| `write` | `boolean` | yes | — |
| `sensitive` | `boolean` | yes | — |

### Schema: FileCheckout

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | `string` enum: `agent`, `repo` | yes | — |
| `agent` | `string` | no | — |
| `repo` | `string` | yes | — |
| `exists` | `boolean` | yes | — |
| `branch` | `string` | no | — |
| `change_count` | `integer` | yes | — |
| `status_error` | `boolean` | no | — |
| `error` | `string` | no | — |
| `partial` | `boolean` | no | — |
| `limit_hit` | `boolean` | no | — |

### Schema: FileCheckoutError

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | `string` enum: `agent`, `repo` | yes | — |
| `agent` | `string` | no | — |
| `repo` | `string` | yes | — |
| `error` | `string` | yes | — |

### Schema: FileCheckoutRepairRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `scope` | `string` enum: `agent`, `repo` | yes | — |
| `target` | `string` | yes | — |
| `repo` | `string` | no | Repo qualifier, valid when scope=agent. |
| `force` | `boolean` | no | — |

### Schema: FileCheckoutRepairResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `repaired` | `boolean` | yes | — |
| `method` | `string` enum: `repair`, `recreate`, `provision`, `none` | yes | — |
| `requires_force` | `boolean` | no | — |
| `backup_path` | `string` | no | — |
| `message` | `string` | yes | — |

### Schema: FileCheckoutsResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `checkouts` | array of [FileCheckout](#schema-filecheckout) | yes | — |
| `partial` | `boolean` | yes | — |
| `limit_hit` | `boolean` | yes | — |
| `errors` | array of [FileCheckoutError](#schema-filecheckouterror) | yes | — |

### Schema: FileDiffResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `patch` | `string` | yes | — |
| `partial` | `boolean` | yes | — |
| `limit_hit` | `boolean` | yes | — |

### Schema: FileGitStatusResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `status` | `object` | yes | — |
| `partial` | `boolean` | yes | — |
| `limit_hit` | `boolean` | yes | — |
| `errors` | array of [FileCheckoutError](#schema-filecheckouterror) | yes | — |

### Schema: FileHistoryEntry

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `kind` | `string` enum: `commit` | yes | — |
| `sha` | `string` | yes | — |
| `author` | `string` | yes | — |
| `time` | `string` | yes | — |
| `summary` | `string` | yes | — |

### Schema: FileHistoryResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `entries` | array of [FileHistoryEntry](#schema-filehistoryentry) | yes | — |
| `partial` | `boolean` | yes | — |
| `limit_hit` | `boolean` | yes | — |

### Schema: FileIndexResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `paths` | array of `string` | yes | — |
| `truncated` | `boolean` | yes | — |
| `partial_reasons` | array of [FilePartialReason](#schema-filepartialreason) | yes | — |

### Schema: FileMoveRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `from` | `string` | yes | — |
| `to` | `string` | yes | — |
| `repo` | `string` | no | Optional repo qualifier, valid only when scope=agent. |
| `overwrite` | `boolean` | no | — |
| `source_version` | `string` | no | Required by the server; current strong version of the source path. |
| `destination_version` | `string` | no | Required when overwriting an existing regular file. |

### Schema: FileMutationResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` | yes | — |
| `version` | `string` | yes | — |

### Schema: FilePartialReason

- **Type:** `string` enum: `file_count`, `result_count`, `byte_limit`, `file_size`, `deadline`, `canceled`

### Schema: FileReadResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `content` | `string` | no | — |
| `size` | `integer` (`int64`) | yes | — |
| `binary` | `boolean` | yes | — |
| `truncated` | `boolean` | yes | — |
| `version` | `string` | yes | — |

### Schema: FileRepoQualifierRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `repo` | `string` | no | Optional repo qualifier, valid only when scope=agent. |

### Schema: FileSearchFileResult

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `matches` | array of [FileSearchMatch](#schema-filesearchmatch) | yes | — |

### Schema: FileSearchMatch

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `line` | `integer` | yes | — |
| `col` | `integer` | yes | — |
| `preview` | `string` | yes | — |

### Schema: FileSearchRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `query` | `string` | yes | — |
| `repo` | `string` | no | Optional repo qualifier, valid only when scope=agent. |
| `regex` | `boolean` | no | — |
| `include` | array of `string` | no | — |
| `exclude` | array of `string`, nullable | no | — |
| `caseSensitive` | `boolean` | no | — |

### Schema: FileSearchResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `results` | array of [FileSearchFileResult](#schema-filesearchfileresult) | yes | — |
| `limitHit` | `boolean` | yes | — |
| `partial_reasons` | array of [FilePartialReason](#schema-filepartialreason) | yes | — |

### Schema: FileStatResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `is_dir` | `boolean` | yes | — |
| `size` | `integer` (`int64`) | yes | — |
| `mod_time` | `string` (`date-time`) | yes | — |
| `version` | `string` | yes | — |

### Schema: FileTreeEntry

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `is_dir` | `boolean` | yes | — |
| `size` | `integer` (`int64`) | yes | — |
| `mod_time` | `string` (`date-time`) | yes | — |

### Schema: FileTreeResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `entries` | array of [FileTreeEntry](#schema-filetreeentry) | yes | — |

### Schema: FileWriteRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `content` | `string` | yes | — |
| `repo` | `string` | no | Optional repo qualifier, valid only when scope=agent. |

### Schema: GitPullRequest

A pull request aggregated across a workspace's repos (gh-backed).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `number` | `integer` | yes | — |
| `title` | `string` | yes | — |
| `url` | `string` | yes | — |
| `state` | `string` | yes | — |
| `is_draft` | `boolean` | yes | — |
| `head_ref_name` | `string` | yes | — |
| `base_ref_name` | `string` | yes | — |
| `author_login` | `string` | no | — |
| `created_at` | `string` | no | — |
| `updated_at` | `string` | no | — |
| `review_decision` | `string` | no | — |
| `repo_name` | `string` | yes | — |
| `source_repo` | `string` | no | — |
| `additions` | `integer` | no | — |
| `deletions` | `integer` | no | — |
| `changed_files` | `integer` | no | — |

### Schema: HourlyBucket

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `hour` | `string` (`date-time`) | yes | — |
| `completed` | `integer` | yes | — |
| `failed` | `integer` | yes | — |
| `avg_duration` | `number` (`double`) | yes | — |

### Schema: Issue

Core issue type used in list endpoints. Maps to `types.Issue` json
serialization. Some list endpoints return this shape directly (not
the richer IssueResponse). Fields marked optional may be absent
depending on issue state.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `description` | `string` | no | — |
| `design` | `string` | no | Hydrated design body when available; collection responses may omit it. |
| `design_artifact_id` | `string` | no | Managed FleetDB artifact reference for the design body. |
| `design_format` | `string` enum: `markdown`, `html` | no | Durable format of the hydrated design body. |
| `has_design` | `boolean` | no | True for either a legacy inline or artifact-backed design. |
| `acceptance_criteria` | `string` | no | — |
| `notes` | `string` | no | — |
| `status` | `string` enum: `open`, `in_progress`, `blocked`, `deferred`, `review`, `closed` | no | User-facing statuses. Internal statuses (tombstone, pinned, hooked) are not settable via the API and excluded from this enum. |
| `priority` | `integer` | yes | — |
| `issue_type` | `string` enum: `bug`, `feature`, `task`, `epic`, `chore` | no | — |
| `assignee` | `string` | no | — |
| `owner` | `string` | no | — |
| `estimated_minutes` | `integer`, nullable | no | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `created_by` | `string` | no | — |
| `updated_at` | `string` (`date-time`) | yes | — |
| `closed_at` | `string` (`date-time`), nullable | no | — |
| `close_reason` | `string` | no | — |
| `due_at` | `string` (`date-time`), nullable | no | — |
| `defer_until` | `string` (`date-time`), nullable | no | — |
| `external_ref` | `string`, nullable | no | — |
| `source_system` | `string` | no | — |
| `source_repo` | `string` | no | — |
| `labels` | array of `string` | no | — |
| `dependencies` | array of [Dependency](#schema-dependency) | no | — |
| `comments` | array of [Comment](#schema-comment) | no | — |
| `pinned` | `boolean` | no | — |
| `agent_state` | `string` enum: `idle`, `spawning`, `running`, `working`, `stuck`, `done`, `stopped`, `dead` | no | — |
| `last_activity` | `string` (`date-time`), nullable | no | — |
| `role_type` | `string` | no | — |
| `rig` | `string` | no | — |
| `mol_type` | `string` | no | — |
| `parent` | `string` | no | — |

### Schema: IssueEvent

Audit trail entry for an issue

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `integer` (`int64`) | yes | — |
| `issue_id` | `string` | yes | — |
| `event_type` | `string` | yes | — |
| `actor` | `string` | yes | — |
| `old_value` | `string`, nullable | no | — |
| `new_value` | `string`, nullable | no | — |
| `comment` | `string`, nullable | no | — |
| `created_at` | `string` (`date-time`) | yes | — |

### Schema: IssueResponse

Full issue detail returned by get-single-issue endpoint. Includes dependency/dependent refs, comments, and counts.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `description` | `string` | no | — |
| `design` | `string` | no | — |
| `design_artifact_id` | `string` | no | Managed FleetDB artifact reference for the design body. |
| `design_format` | `string` enum: `markdown`, `html` | no | Durable format of the hydrated design body. |
| `has_design` | `boolean` | no | True for either a legacy inline or artifact-backed design. |
| `acceptance_criteria` | `string` | no | — |
| `notes` | `string` | no | — |
| `status` | `string` enum: `open`, `in_progress`, `blocked`, `deferred`, `review`, `closed` | yes | — |
| `priority` | `integer` | yes | — |
| `issue_type` | `string` enum: `bug`, `feature`, `task`, `epic`, `chore` | yes | — |
| `assignee` | `string` | no | — |
| `owner` | `string` | no | — |
| `estimated_minutes` | `integer`, nullable | no | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `updated_at` | `string` (`date-time`) | yes | — |
| `closed_at` | `string` (`date-time`), nullable | no | — |
| `close_reason` | `string` | no | — |
| `due_at` | `string` (`date-time`), nullable | no | — |
| `defer_until` | `string` (`date-time`), nullable | no | — |
| `external_ref` | `string`, nullable | no | — |
| `source_repo` | `string` | no | — |
| `labels` | array of `string` | yes | — |
| `dependencies` | array of [DependencyRef](#schema-dependencyref) | yes | — |
| `dependents` | array of [DependencyRef](#schema-dependencyref) | yes | — |
| `comments` | array of [CommentResponse](#schema-commentresponse) | yes | — |
| `parent` | `string`, nullable | no | — |
| `parent_title` | `string`, nullable | no | — |
| `dependency_count` | `integer` | yes | — |
| `dependent_count` | `integer` | yes | — |
| `pinned` | `boolean` | yes | — |

### Schema: IssueTab

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | "details", "sessions", "logs", or "terminal-{session}" |
| `type` | `string` enum: `details`, `sessions`, `logs`, `terminal` | yes | — |
| `label` | `string` | yes | — |
| `session_name` | `string` | no | For terminal tabs only |
| `backend` | `string` | no | For terminal tabs only |
| `sort_order` | `integer` | yes | — |

### Schema: IssueTabState

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `issue_id` | `string` | yes | — |
| `tabs` | array of [IssueTab](#schema-issuetab) | yes | — |
| `active_tab_id` | `string` | yes | — |
| `updated_at` | `string` (`date-time`) | yes | — |

### Schema: LocalSettingsPatchRequest

All top-level fields are optional partial patches (nil = leave unchanged).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `fleetdb_redis` | `object` | no | — |
| `agent_runtime` | `object` | no | — |
| `local_task_runner` | `object` | no | — |
| `runtime_credentials` | `object` | no | — |

### Schema: LocalSettingsResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` | yes | — |
| `data` | [SanitizedLocalSettings](#schema-sanitizedlocalsettings) | no | — |
| `message` | `string` | no | — |
| `error` | `string` | no | — |

### Schema: MessageResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` const `true` | yes | — |
| `message` | `string` | yes | — |

### Schema: MetricsSnapshot

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `timestamp` | `string` (`date-time`) | yes | — |
| `tasks_completed_last_hour` | `integer` | yes | — |
| `tasks_completed_24h` | `integer` | yes | — |
| `avg_task_duration_sec` | `number` (`double`) | yes | — |
| `lines_changed_last_hour` | `integer` | yes | — |
| `error_rate_pct` | `number` (`double`) | yes | — |
| `restart_count_24h` | `integer` | yes | — |
| `restarts_by_agent` | `object` | yes | — |
| `agent_utilization` | `object` | yes | — |
| `tasks_by_role` | `object` | yes | — |
| `tasks_by_epic` | `object` | yes | — |
| `tasks_by_agent` | `object` | yes | — |
| `hourly_completions` | array of [HourlyBucket](#schema-hourlybucket) | yes | — |
| `total_tasks_completed` | `integer` (`int64`) | yes | — |
| `total_tasks_failed` | `integer` (`int64`) | yes | — |
| `total_restarts` | `integer` (`int64`) | yes | — |

### Schema: MonitorAgentStatus

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `branch` | `string` | yes | — |
| `status` | `string` | yes | — |
| `ahead` | `integer` | yes | — |
| `behind` | `integer` | yes | — |
| `role` | `string` | no | — |
| `role_kind` | `string` enum: `interactive`, `worker` | no | — |
| `repo` | `string` | no | — |
| `workspace` | `string` | yes | — |
| `daemon_managed` | `boolean` | no | — |
| `parent` | `string` | no | Active epic assignment for lead/workers. |
| `orchestrator_session_id` | `string` | no | Lead/orchestrator session that spawned or owns the agent. |
| `task_id` | `string` | no | Latest task session associated with this agent. |
| `session_id` | `string` | no | Latest control-plane session associated with this agent. |
| `mode` | `string` | no | Assignment mode, such as persistent or ephemeral. |
| `desired_state` | `string` | no | Requested daemon state for the agent. |
| `commits` | array of [MonitorCommitDetail](#schema-monitorcommitdetail) | no | — |
| `changes` | array of [MonitorFileChange](#schema-monitorfilechange) | no | — |
| `current_task_id` | `string` | no | Task this daemon-managed agent has currently claimed. Empty between tasks (just spawned, polling, finished). UI joins this against issue.id to render which agent is working each card. |
| `last_activity_at` | `string` (`date-time`) | no | Most recent PTY-output observation from the agent's supervised backend (claude/codex/gemini), forwarded over IPC. Compare to "now" to detect stuck agents. Zero/absent when no observation yet or agent isn't daemon-managed. |
| `live_status` | `string` | no | Fleet-db's DERIVED liveness signal ("working" or "idle"), computed server-side from the running-session + fresh-lease join (never re-derived by loom). "working" means the agent has a live session right now. Absent when liveness was not computed (no fleet-db store). The UI prefers this over the lock-derived status, which stays "idle" for a provably-working agent on serve-only deployments. |
| `active_task_id` | `string` | no | Task id of the agent's live session, set only when live_status == "working". |
| `active_phase` | `string` | no | Phase of the agent's live session (e.g. "planning", "implementation"), set only when live_status == "working". |
| `last_error_class` | `string` | no | Fleet-db's DERIVED error_class of the agent's most recent terminal session, set only when that latest run failed and the agent is currently idle (a newer success clears it). Lets the UI explain a stalled idle agent (e.g. "SpawnFailure") instead of a bare "agent missing". Absent when liveness was not computed or the last run succeeded. |

### Schema: MonitorAgentsResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace` | [MonitorWorkspaceInfo](#schema-monitorworkspaceinfo) | yes | — |
| `agents` | array of [MonitorAgentStatus](#schema-monitoragentstatus) | yes | — |
| `by_workspace` | `object` | no | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: MonitorCommitDetail

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `hash` | `string` | yes | — |
| `message` | `string` | yes | — |
| `url` | `string` | no | — |

### Schema: MonitorFileChange

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `status` | `string` | yes | "M", "A", "D", "??", "R" |
| `path` | `string` | yes | — |

### Schema: MonitorStats

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `open` | `integer` | yes | — |
| `closed` | `integer` | yes | — |
| `total` | `integer` | yes | — |
| `completion` | `number` (`double`) | yes | — |
| `remaining` | `integer` | yes | — |
| `in_progress` | `integer` | yes | — |
| `review` | `integer` | yes | — |
| `blocked` | `integer` | yes | — |

### Schema: MonitorStatsResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `stats` | [MonitorStats](#schema-monitorstats) | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: MonitorStatusResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace` | [MonitorWorkspaceInfo](#schema-monitorworkspaceinfo) | yes | — |
| `agents` | array of [MonitorAgentStatus](#schema-monitoragentstatus) | yes | — |
| `tasks` | [MonitorTaskSummary](#schema-monitortasksummary) | yes | — |
| `needs_planning` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `ready_to_implement` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `needs_review` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `in_progress` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `in_progress_list` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `backlog` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `closed` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `agent_tasks` | `object` of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `stats` | [MonitorStats](#schema-monitorstats) | yes | — |
| `sync` | [MonitorSyncInfo](#schema-monitorsyncinfo) | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: MonitorSyncInfo

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `db_synced` | `boolean` | yes | — |
| `db_last_sync` | `string` | yes | — |
| `db_error` | `string` | no | — |
| `git_needs_push` | `integer` | yes | — |
| `git_needs_pull` | `integer` | yes | — |
| `git_push_details` | array of [MonitorWorktreeSyncDetail](#schema-monitorworktreesyncdetail) | no | — |
| `git_pull_details` | array of [MonitorWorktreeSyncDetail](#schema-monitorworktreesyncdetail) | no | — |

### Schema: MonitorSyncResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `sync` | [MonitorSyncInfo](#schema-monitorsyncinfo) | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: MonitorTaskInfo

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `title` | `string` | yes | — |
| `priority` | `integer` | yes | — |
| `status` | `string` | yes | — |

### Schema: MonitorTaskSummary

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `needs_planning` | `integer` | yes | — |
| `ready_to_implement` | `integer` | yes | — |
| `in_progress` | `integer` | yes | — |
| `need_review` | `integer` | yes | — |
| `backlog` | `integer` | yes | — |
| `epics` | `integer` | yes | — |

### Schema: MonitorTasksResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `summary` | [MonitorTaskSummary](#schema-monitortasksummary) | yes | — |
| `needs_planning` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `ready_to_implement` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `needs_review` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `in_progress` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `backlog` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `closed` | array of [MonitorTaskInfo](#schema-monitortaskinfo) | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: MonitorWorkspaceDetail

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `repos` | array of `string` | yes | — |

### Schema: MonitorWorkspaceInfo

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `mode` | `string` enum: `workspace` | yes | — |
| `name` | `string` | no | — |
| `workspaces` | array of `string` | no | — |

### Schema: MonitorWorkspacesResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `mode` | `string` enum: `workspace` | yes | — |
| `default` | `string` | no | — |
| `workspaces` | `object` of [MonitorWorkspaceDetail](#schema-monitorworkspacedetail) | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: MonitorWorktreeSyncDetail

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `count` | `integer` | yes | — |

### Schema: MoveIssueRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `target_workspace` | `string` | yes | — |

### Schema: MoveResult

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `source_id` | `string` | yes | — |
| `target_id` | `string` | yes | — |
| `warnings` | array of `string` | no | — |

### Schema: MutationPayload

SSE mutation event payload. `type` is the legacy coarse mutation kind.
New consumers should prefer the generic `entity_type`, `entity_id`, and
`action` envelope fields when deciding which local state to invalidate.
`issue_id` is retained for backward-compatible issue-scoped consumers
and may be omitted for non-issue entities. For status events: old_status
and new_status are present. For bonded events: parent_id and step_count
are present. This is documented as a flat schema (no discriminator)
because the SSE stream is not validated by generated clients.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | `string` enum: `create`, `update`, `delete`, `comment`, `status`, `bonded`, `squashed`, `burned`, `refresh`, `terminal_metadata`, `terminal_session_change`, `issue_tabs`, `session_change` | yes | — |
| `entity_type` | `string` | no | Generic changed entity type, for example issue, dependency, comment, label, agent, terminal, session, or workspace. |
| `entity_id` | `string` | no | Generic changed entity identifier. |
| `action` | `string` | no | Source action for the mutation, usually the fleet-db action such as issue.update or dep.add. |
| `issue_id` | `string` | no | Legacy issue identifier for issue-scoped consumers; omitted for non-issue entities. |
| `title` | `string` | no | — |
| `assignee` | `string` | no | — |
| `actor` | `string` | no | — |
| `timestamp` | `string` | yes | — |
| `old_status` | `string` | no | Present for status mutation events |
| `new_status` | `string` | no | Present for status mutation events |
| `parent_id` | `string` | no | Present for bonded mutation events |
| `step_count` | `integer` | no | Present for bonded mutation events |
| `priority` | `integer`, nullable | no | — |
| `source_repo` | `string` | no | — |
| `workspace_id` | `string` | no | — |

### Schema: ObservabilityEvent

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `type` | `string` enum: `task.claimed`, `task.started`, `task.completed`, `task.failed`, `agent.started`, `agent.restarted`, `agent.stopped`, `epic.assigned`, `epic.exhausted`, `pr.created`, `conflict.resolved`, `system.health_check`, `system.config_reloaded` | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |
| `agent` | `string` | no | — |
| `role` | `string` | no | — |
| `epic_id` | `string` | no | — |
| `data` | `object` | no | Event-type-specific data (JSON object) |

### Schema: ObservabilityEventsResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` | yes | — |
| `data` | array of [ObservabilityEvent](#schema-observabilityevent) | yes | — |
| `total` | `integer` | yes | — |
| `page` | `integer` | yes | — |
| `per_page` | `integer` | yes | — |
| `error` | `string` | no | — |

### Schema: ObservabilityMetricsResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `success` | `boolean` | yes | — |
| `data` | [MetricsSnapshot](#schema-metricssnapshot) | no | — |
| `error` | `string` | no | — |

### Schema: PatchIssueRequest

All fields are optional for partial update. Pointer types in Go map to nullable here.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | `string`, nullable | no | — |
| `description` | `string`, nullable | no | — |
| `status` | `string` enum: `open`, `in_progress`, `blocked`, `deferred`, `review`, `closed`, `<nil>`, nullable | no | — |
| `priority` | `integer`, nullable | no | — |
| `assignee` | `string`, nullable | no | — |
| `owner` | `string`, nullable | no | — |
| `design` | `string`, nullable | no | — |
| `design_format` | `string` enum: `markdown`, `html`, `<nil>`, nullable | no | — |
| `acceptance_criteria` | `string`, nullable | no | — |
| `notes` | `string`, nullable | no | — |
| `external_ref` | `string`, nullable | no | — |
| `estimated_minutes` | `integer`, nullable | no | — |
| `issue_type` | `string`, nullable | no | — |
| `add_labels` | array of `string` | no | — |
| `remove_labels` | array of `string` | no | — |
| `set_labels` | array of `string` | no | — |
| `pinned` | `boolean`, nullable | no | — |
| `parent` | `string`, nullable | no | — |
| `due_at` | `string`, nullable | no | — |
| `defer_until` | `string`, nullable | no | — |
| `agent_state` | `string` enum: `idle`, `spawning`, `running`, `working`, `stuck`, `done`, `stopped`, `dead`, `<nil>`, nullable | no | — |

### Schema: PlatformEvent

One control-plane platform event (domain.PlatformEvent).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |
| `actor` | `string` | yes | — |
| `action` | `string` | yes | — |
| `entity_type` | `string` | yes | — |
| `entity_id` | `string` | yes | — |
| `workspace_id` | `string` | yes | — |
| `before` | `string` | no | — |
| `after` | `string` | no | — |
| `metadata` | `object` | no | — |

### Schema: PlatformEventsPage

A paginated page of platform events (domain.PlatformEventsPage).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `events` | array of [PlatformEvent](#schema-platformevent) | yes | — |
| `cursor` | `string` | yes | — |

### Schema: PullRequestDetail

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `number` | `integer` | yes | — |
| `state` | `string` | yes | — |
| `title` | `string` | yes | — |
| `is_draft` | `boolean` | yes | — |
| `head_ref_name` | `string` | yes | — |
| `base_ref_name` | `string` | yes | — |
| `head_sha` | `string` | yes | — |
| `merged` | `boolean` | yes | — |

### Schema: PullRequestDiff

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `files` | array of [PullRequestDiffFile](#schema-pullrequestdifffile) | yes | — |
| `diff` | `string` | yes | — |

### Schema: PullRequestDiffFile

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `path` | `string` | yes | — |
| `status` | `string` | yes | — |
| `additions` | `integer` | yes | — |
| `deletions` | `integer` | yes | — |
| `patch` | `string` | yes | — |

### Schema: PullRequestReviewRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `event` | `string` enum: `approve`, `request_changes`, `comment` | yes | — |
| `body` | `string` | no | — |
| `expected_head_sha` | `string` | yes | — |

### Schema: PullRequestReviewResult

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `review_id` | `integer` | no | — |
| `state` | `string` | no | — |

### Schema: ReviewerConversation

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `state` | `string` | yes | starting \| reconnecting \| idle \| running \| failed \| unsupported. failed: the reviewer runtime died or its conversation is unreadable. unsupported: the reviewer's backend has no readable conversation (the terminal tab still works); detail says why. |
| `detail` | `string` | no | Human-readable context for failed/unsupported states. |
| `messages` | array of [ReviewerMessage](#schema-reviewermessage) | yes | — |

### Schema: ReviewerEnsureResult

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `agent_name` | `string` | yes | — |
| `checked_out_sha` | `string` | yes | — |
| `seeded` | `boolean` | yes | — |

### Schema: ReviewerMessage

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `turn_id` | `string` | yes | — |
| `item_id` | `string` | yes | — |
| `role` | `string` | yes | user \| assistant |
| `text` | `string` | yes | — |
| `phase` | `string` | no | — |

### Schema: ReviewerMessageRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `text` | `string` | yes | — |

### Schema: ReviewerMessageResult

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `state` | `string` | yes | — |
| `reason` | `string` | yes | — |

### Schema: RuntimeCredentialPatch

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `api_key` | `string` | no | — |
| `token` | `string` | no | — |
| `clear` | `boolean` | no | — |

### Schema: RuntimeReadyResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `ready` | `boolean` | yes | — |
| `mode` | `string` enum: `daemon`, `fleet` | yes | — |
| `workspace` | `string` | yes | — |
| `reason` | `string` | no | — |

### Schema: SanitizedLocalSettings

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `version` | `integer` | yes | — |
| `fleetdb_redis` | `object` | yes | — |
| `agent_runtime` | `object` | yes | — |
| `local_task_runner` | `object` | yes | — |
| `runtime_credentials` | `object` | yes | — |

### Schema: SanitizedRuntimeCredential

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `configured` | `boolean` | yes | — |
| `updated_at` | `string` | no | — |

### Schema: SessionHistoryRecord

Session history record (Redis-backed, per-issue)

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `session_name` | `string` | yes | — |
| `issue_id` | `string` | yes | — |
| `backend` | `string` | yes | — |
| `status` | `string` enum: `active`, `completed` | yes | — |
| `launcher` | `string` enum: `user`, `start-work` | yes | — |
| `started_at` | `string` (`date-time`) | yes | — |
| `ended_at` | `string` (`date-time`), nullable | no | — |
| `scrollback_path` | `string` | no | — |

### Schema: SessionResponse

Session audit record from dto.SessionResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `session_id` | `string` | yes | — |
| `task_id` | `string` | yes | — |
| `epic_id` | `string` | no | — |
| `agent_name` | `string` | yes | — |
| `backend` | `string` | yes | — |
| `model` | `string` | no | — |
| `phase` | `string` | no | — |
| `started_at` | `string` (`date-time`) | yes | — |
| `ended_at` | `string` (`date-time`), nullable | no | — |
| `duration_s` | `number` (`double`) | no | — |
| `status` | `string` | yes | — |
| `exit_code` | `integer` | yes | — |
| `input_tokens` | `integer` (`int64`) | yes | — |
| `output_tokens` | `integer` (`int64`) | yes | — |
| `cache_read_tokens` | `integer` (`int64`) | yes | — |
| `cache_write_tokens` | `integer` (`int64`) | yes | — |
| `estimated_cost_usd` | `number` (`double`) | yes | — |
| `files_changed` | `integer` | yes | — |
| `lines_added` | `integer` | yes | — |
| `lines_removed` | `integer` | yes | — |
| `files_touched` | array of `string` | no | — |
| `attempt_num` | `integer` | yes | — |
| `error_class` | `string` | no | — |
| `is_active` | `boolean` | yes | — |
| `has_transcript` | `boolean` | yes | — |
| `has_diff` | `boolean` | yes | — |
| `last_error` | `string` | no | — |

### Schema: StaleDetectorStatus

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `enabled` | `boolean` | yes | — |
| `is_leader` | `boolean` | yes | — |
| `last_check` | `string` (`date-time`) | no | — |
| `stale_workers_found` | `integer` | yes | — |
| `tasks_reconciled` | `integer` | yes | — |

### Schema: Statistics

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `total_issues` | `integer` | yes | — |
| `open_issues` | `integer` | yes | — |
| `in_progress_issues` | `integer` | yes | — |
| `closed_issues` | `integer` | yes | — |
| `blocked_issues` | `integer` | yes | — |
| `deferred_issues` | `integer` | yes | — |
| `ready_issues` | `integer` | yes | — |
| `tombstone_issues` | `integer` | yes | — |
| `pinned_issues` | `integer` | yes | — |
| `epics_eligible_for_closure` | `integer` | yes | — |
| `average_lead_time_hours` | `number` (`double`) | yes | — |

### Schema: TabMetadata

Terminal tab metadata (Redis-backed)

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `session_name` | `string` | yes | — |
| `workspace` | `string` | no | — |
| `label` | `string` | yes | — |
| `notes` | `string` | yes | — |
| `sort_order` | `integer` | yes | — |
| `pinned` | `boolean` | yes | — |
| `issue_id` | `string` | no | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `updated_at` | `string` (`date-time`) | yes | — |
| `pty_alive` | `boolean` | yes | Whether the backend PTY for this tab is currently alive in the server process. False means the tab metadata survived (e.g. a server restart) but the PTY did not; clients should render the tab as "session ended" and prompt before reconnecting (which will spawn a fresh session). |
| `attached_clients` | `integer` | yes | Count of concurrent WebSocket clients currently viewing this session. 0 means no one is attached (but the PTY may still be live, within its grace window). Values ≥2 can be surfaced as a "multiple viewers" indicator so users can be warned before destructive tab-close actions. |

### Schema: TabPatchRequest

All fields optional for partial update

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `label` | `string`, nullable | no | — |
| `notes` | `string`, nullable | no | — |
| `sort_order` | `integer`, nullable | no | — |
| `pinned` | `boolean`, nullable | no | — |
| `issue_id` | `string`, nullable | no | — |

### Schema: TabPutRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `label` | `string` | yes | — |
| `sort_order` | `integer` | yes | — |
| `notes` | `string` | yes | — |
| `pinned` | `boolean` | yes | — |

### Schema: TaskRunArtifact

camelCase wire view of a task-run artifact (artifactResult).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspaceKey` | `string` | no | — |
| `artifactId` | `string` | yes | — |
| `taskId` | `string` | no | — |
| `ownerType` | `string` | no | — |
| `ownerId` | `string` | no | — |
| `type` | `string` | yes | — |
| `uri` | `string` | no | — |
| `summary` | `string` | no | — |
| `mimeType` | `string` | no | — |
| `sizeBytes` | `integer` (`int64`) | no | — |
| `checksum` | `string` | no | — |
| `contentHash` | `string` | no | — |
| `visibility` | `string` | no | — |
| `redactionStatus` | `string` | no | — |
| `durableStatus` | `string` | no | — |
| `metadata` | `object` | no | — |
| `finalizedAt` | `string` (`date-time`), nullable | no | — |
| `createdAt` | `string` (`date-time`) | no | — |
| `updatedAt` | `string` (`date-time`) | no | — |

### Schema: TaskRunOpError

Structured error envelope for the task-run SDK transport ({error:{code,message,retryable,details?}}). Distinct from ErrorResponse.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `error` | `object` | yes | — |

### Schema: TranscriptEntry

Single transcript entry from a session

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `seq` | `integer` | yes | — |
| `ts` | `string` (`date-time`) | yes | — |
| `role` | `string` enum: `user`, `assistant`, `system`, `tool` | yes | — |
| `type` | `string` enum: `text`, `tool_use`, `tool_result` | yes | — |
| `content` | `string` | no | — |
| `tool_name` | `string` | no | — |
| `tool_input` | `string` | no | — |
| `raw` | `string` | no | — |

### Schema: TreeNode

Node in a dependency tree for graph visualization

- **Type:** allOf([Issue](#schema-issue) + `object`)

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `depth` | `integer` | yes | — |
| `parent_id` | `string` | yes | — |
| `truncated` | `boolean` | yes | — |

### Schema: TriggerDelivery

Links a TriggerEvent to the binding that matched it and the DriverRun it enqueued (domain.TriggerDelivery).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace_key` | `string` | yes | — |
| `delivery_id` | `string` | yes | — |
| `trigger_event_id` | `string` | yes | — |
| `trigger_binding_id` | `string` | yes | — |
| `status` | `string` enum: `accepted`, `rejected`, `duplicate`, `queued`, `dispatched`, `failed`, `replayed`, `superseded`, `held` | yes | — |
| `subject_key` | `string` | no | — |
| `rejection_reason` | `string` | no | — |
| `driver_run_id` | `string` | no | — |
| `attempt` | `integer` | yes | — |
| `next_retry_at` | `string` (`date-time`), nullable | no | — |
| `error_class` | `string` | no | — |
| `created_at` | `string` (`date-time`) | yes | — |
| `updated_at` | `string` (`date-time`) | yes | — |

### Schema: TriggerEvent

A persisted inbound trigger event (domain.TriggerEvent).

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace_key` | `string` | yes | — |
| `event_id` | `string` | yes | — |
| `trigger_binding_id` | `string` | no | — |
| `source_kind` | `string` | yes | — |
| `source_event_id` | `string` | no | — |
| `event_type` | `string` | yes | — |
| `subject_ref` | `string` | no | — |
| `actor_ref` | `string` | no | — |
| `origin` | `string` enum: `external`, `workflow`, `system` | no | Server-stamped provenance; empty normalizes to external on read. |
| `hop_depth` | `integer` | no | — |
| `occurred_at` | `string` (`date-time`) | yes | — |
| `received_at` | `string` (`date-time`) | yes | — |
| `idempotency_key` | `string` | no | — |
| `raw_payload_ref` | `string` | no | — |
| `raw_payload_digest` | `string` | no | — |
| `signature_status` | `string` | no | — |
| `replay_of_event_id` | `string` | no | — |

### Schema: TriggerRouteDelivery

One fan-out leg of a trigger-route dispatch (store.TriggerRouteDelivery), as returned inline by the webhook receive endpoint.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `delivery_id` | `string` | yes | — |
| `trigger_binding_id` | `string` | yes | — |
| `driver_run_id` | `string` | yes | — |
| `status` | `string` enum: `accepted`, `rejected`, `duplicate`, `queued`, `dispatched`, `failed`, `replayed`, `superseded`, `held` | yes | — |
| `rejection_reason` | `string` | no | — |

### Schema: UsageAgentSummary

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `sessions` | `integer` | yes | — |
| `input_tokens` | `integer` (`int64`) | yes | — |
| `output_tokens` | `integer` (`int64`) | yes | — |
| `total_cost` | `number` (`double`) | yes | — |

### Schema: UsageBackendSummary

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `sessions` | `integer` | yes | — |
| `total_cost` | `number` (`double`) | yes | — |

### Schema: UsageDailyCost

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `date` | `string` | yes | — |
| `cost` | `number` (`double`) | yes | — |
| `sessions` | `integer` | yes | — |

### Schema: UsageResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `total_input_tokens` | `integer` (`int64`) | yes | — |
| `total_output_tokens` | `integer` (`int64`) | yes | — |
| `total_cache_read_tokens` | `integer` (`int64`) | yes | — |
| `total_cache_write_tokens` | `integer` (`int64`) | yes | — |
| `total_cost` | `number` (`double`) | yes | — |
| `session_count` | `integer` | yes | — |
| `by_agent` | array of [UsageAgentSummary](#schema-usageagentsummary) | yes | — |
| `by_backend` | array of [UsageBackendSummary](#schema-usagebackendsummary) | yes | — |
| `daily_costs` | array of [UsageDailyCost](#schema-usagedailycost) | yes | — |
| `sessions` | array of [UsageSessionRecord](#schema-usagesessionrecord) | yes | — |
| `timestamp` | `string` (`date-time`) | yes | — |

### Schema: UsageSessionRecord

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `agent_name` | `string` | yes | — |
| `backend` | `string` | yes | — |
| `task_id` | `string` | no | — |
| `epic_id` | `string` | no | — |
| `input_tokens` | `integer` (`int64`) | yes | — |
| `output_tokens` | `integer` (`int64`) | yes | — |
| `cache_read_tokens` | `integer` (`int64`) | yes | — |
| `cache_write_tokens` | `integer` (`int64`) | yes | — |
| `estimated_cost_usd` | `number` (`double`) | yes | — |
| `started_at` | `string` (`date-time`) | yes | — |
| `ended_at` | `string` (`date-time`) | yes | — |
| `exit_code` | `integer` | yes | — |
| `model` | `string` | no | — |
| `session_id` | `string` | no | — |

### Schema: WorkerRegisterRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `workspace` | `string` | yes | — |
| `agent` | `string` | yes | — |
| `backend` | `string` | no | — |

### Schema: WorkerStateRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `action` | `string` | yes | — |
| `state` | `string` | no | — |
| `agent_name` | `string` | no | — |
| `task_id` | `string` | no | — |
| `task_title` | `string` | no | — |

### Schema: WorkspaceAddReposRequest

Body for POST /api/workspaces/{ws}/repos. The ws path param supplies the workspace; no workspace id field is read from the body.

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `repos` | array of `string` | no | Existing local repo paths to attach. |
| `clone_urls` | array of `string` | no | Remote git URLs to clone into the workspace. |
| `branch` | `string` | no | — |

### Schema: WorkspaceAgentInfo

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `repos` | array of `string` | yes | — |
| `repo_groups` | array of `string` | yes | — |
| `cross_repo` | `boolean` | yes | — |

### Schema: WorkspaceBackendPatchRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `backend` | `string` | yes | — |

### Schema: WorkspaceDesignFormatPatchRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `design_format` | `string` enum: `markdown`, `html` | yes | — |

### Schema: WorkspaceRenameRequest

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `new_name` | `string` | yes | — |

### Schema: WorkspaceRepo

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `name` | `string` | yes | — |
| `path` | `string` | yes | — |
| `default_branch` | `string` | yes | — |
| `remote` | `string` | yes | — |
| `remote_url` | `string` | no | — |
| `source_repo_id` | `string` | no | — |
| `groups` | array of `string` | yes | — |

### Schema: WorkspaceResponse

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `name` | `string` | yes | — |
| `path` | `string` | yes | — |
| `repos` | array of [WorkspaceRepo](#schema-workspacerepo) | yes | — |
| `groups` | array of `string` | yes | — |
| `agents` | array of [WorkspaceAgentInfo](#schema-workspaceagentinfo) | yes | — |
| `workspaces` | array of [WorkspaceSummary](#schema-workspacesummary) | yes | — |
| `workspace_order` | array of `string` | no | — |
| `default_workspace` | `string` | yes | — |
| `design_format` | `string` enum: `markdown`, `html` | no | — |

### Schema: WorkspaceSummary

- **Type:** `object`

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `id` | `string` | yes | — |
| `name` | `string` | yes | — |
| `path` | `string` | yes | — |
| `active` | `boolean` | yes | — |
| `repo_count` | `integer` | yes | — |
| `is_default` | `boolean` | yes | — |
| `backend` | `string` | no | — |

## Rate limiting

A per-IP token bucket is applied to API requests
(`internal/webui/server/middleware/ratelimit.go`). Defaults from
`DefaultRateLimitConfig` (line 26):

| Operation | Rate | Burst |
|-----------|------|-------|
| Read (`GET`/`HEAD`/`OPTIONS`) | 100 req/s | 200 |
| Mutating (`POST`/`PUT`/`PATCH`/`DELETE`) | 20 req/s | 40 |

- Buckets are keyed on the client IP; each IP is independent.
- Stale entries are evicted after 10 minutes of inactivity, swept every 5
  minutes (`EntryTTL`, `CleanupInterval`).
- Exceeding a bucket returns `429` with a `Retry-After` header.
- `/health`, `/api/health` and `/api/client-errors` are exempt from this
  limiter (`ratelimit.go:149`).

`POST /api/client-errors` has its own limiter instead: 10 requests per minute
per IP with a burst of 10, 5-minute cleanup, 10-minute entry TTL
(`internal/webui/handlermux/handlers.go:77`).

## Error codes

The `code` field of `dto.ErrorResponse` is populated by the issue handlers.
These are the values currently emitted anywhere in `internal/webui`
(`internal/webui/handlers/issues/`):

| Code | Meaning |
|------|---------|
| `ENCODE_ERROR` | Failed to encode the HTTP response |
| `INVALID_JSON` | Malformed request body |
| `INVALID_PARAMS` | Invalid query parameters |
| `MISSING_QUERY` | Required query parameter absent |
| `REQUEST_TOO_LARGE` | Request body exceeds 1 MB |

Endpoints outside the issue handlers return `error` without a `code`.

## Common HTTP status codes

| Code | Meaning |
|------|---------|
| `200` | Success |
| `201` | Resource created |
| `202` | Accepted — async job started (workspace creation) |
| `204` | No content (e.g. no task available for a fleet claim) |
| `400` | Invalid input, or empty `{ws}` path parameter |
| `401` | Authentication required or invalid |
| `403` | Cross-origin request rejected |
| `404` | Resource not found, unknown workspace, or unregistered route |
| `409` | Conflict (circular dependency, already claimed) |
| `413` | Request body too large |
| `429` | Rate limit exceeded |
| `503` | Service unavailable (daemon down, fleet not configured) |
| `504` | Gateway timeout |

## Fleet coordination reference

Fleet endpoints let remote workers register with a pre-shared API key, receive a
JWT, atomically claim tasks, report completion, and heartbeat. They are
registered whenever a fleet Redis store initializes — `config.FleetRedis != nil`
sets the registry (`internal/webui/app/server_app.go:243`) and a non-nil registry
mounts the module (`internal/webui/app/server_modules.go:106-110`). The fleet API
key is *not* part of that gate: with Redis up and no `LOOM_FLEET_API_KEY` the
routes exist and `POST .../fleet/register` answers `503` with `"fleet
authentication not configured"` (`internal/webui/fleet/handlers.go:62-68`). The
standard JSON `404` only happens when no fleet Redis is configured at all — and
note that `loom serve` starts an in-process miniredis when no external Redis
address is set (`internal/cli/serve/serve.go:207-211`), so in the common local
case the routes are mounted.

### Two-layer auth

1. **Registration** — `POST .../fleet/register` validates the
   `X-Fleet-API-Key` header with `subtle.ConstantTimeCompare`
   (`internal/webui/fleet/handlers.go:70-80`).
2. **Everything else** — registration returns an HMAC-SHA256 JWT
   (`internal/webui/fleet/jwt.go:24-41`) carrying `worker_id`, `repos`, `iat`
   and `exp`. Claim, done, and heartbeat validate it and inject `WorkerClaims`
   into the request context.

The JWT signing key lives in Redis and supports rotation with a grace period for
the previous version (`internal/webui/fleet/signing_key.go`). When no signing
key is configured, the JWT middleware is not applied.

### Redis keys

All keys use the `fleet:` prefix. A workspace-scoped store inserts the workspace
ID, giving `fleet:{wsID}:...` (`internal/webui/fleet/store.go:41-72`).

| Key pattern | TTL | Contents |
|-------------|-----|----------|
| `fleet:workers:{workerID}` | 2 hours | Worker registration |
| `fleet:tasks:claimed:{taskID}` | 5 minutes | Claimed-task ownership |
| `fleet:worker:claim:{workerID}` | 5 minutes | Cached claim response |
| `fleet:task:result:{taskID}` | 24 hours | Task completion result |
| `fleet:ratelimit:{ip}` | sliding window | Per-IP registration rate limit |

TTL sources: `store.go:28-34`, `result.go:14`.

### Timeouts

Per-request context deadlines:

| Endpoint | Timeout | Source |
|----------|---------|--------|
| Register | 30s | `handlers.go:138` |
| Claim | 5s | `handlers_claim.go:115` |
| Done | 5s, plus 2s for cleanup | `handlers_done.go:87,162` |
| Heartbeat | 2s | `handlers_heartbeat.go:87` |

`TimeoutEnforcer` runs a background sweep that releases claims for tasks that
overrun. Defaults: 30-minute task timeout, checked once a minute
(`internal/webui/fleet/timeout.go:105-110`).

### SSE and fleet counters

`GET /api/metrics` returns the SSE hub gauges plus fleet counters
(`internal/webui/handlers/health/health.go:65-75`). The `loom_fleet_*` fields
are `omitempty` on `int64`, so they are absent both when fleet coordination is
disabled **and** when every counter happens to be zero. `connected_clients`,
`retry_queue_depth` and `uptime_seconds` are gauges; the rest are monotonic
counters.

### `fleetDone` bypasses the user JWT, not authentication

`POST /api/workspaces/{ws}/fleet/done/{id}` skips the *user* JWT middleware —
which is what `isPublicRoute` means by public — but it is not unauthenticated.
`internal/webui/fleet/module.go:64-65` wraps it in exactly the same fleet-JWT
middleware as claim and heartbeat whenever a signing key is configured
(`module.go:54-57`). The spec matches: `api/openapi.yaml:5836-5837` declares
`FleetJWT` for `fleetDone`, so the endpoint reference above renders it as
**Auth:** `FleetJWT` rather than as a public route.

## Endpoints removed from the terminal surface

The tmux-era terminal session lifecycle endpoints (`spawn`, `restart`, `kill`,
`session-status`, `sessions`, `sessions/{name}/seed`, `sessions/close-all`,
`sessions/{session}/scrollback`, `/export`, `/scrollback-info`) were deleted
during the terminal simplification — see the module comment at
`internal/webui/handlers/terminal/module.go:75` ("the surviving terminal
routes") and `internal/webui/frontend/src/api/terminal/terminal.ts:1-8`. What
remains is WebSocket auth, tab metadata, terminal UI state, and the
cross-workspace sessions-by-issue lookup.

They were dropped from `api/openapi.yaml` in the same change, so they no longer
appear in the endpoint reference above or in the generated TypeScript types
(`internal/webui/frontend/src/types/generated/openapi.ts`); the spec now
declares only the seven surviving `/terminal/*` paths. The **Documented but not
registered** table below is the authoritative check that spec and server agree.

`POST /api/csp-report`, documented in the pre-generator version of this file, no
longer exists anywhere in the Go source.

## Appendix: Spec Coverage vs Registered Routes

Generated by comparing `api/openapi.yaml` against the `net/http` route
patterns registered under `internal/webui/`. See `scripts/openapi-to-md/routes.go`
for the detection rule and its limits — it is a syntactic scan, so a route
built from a non-literal pattern would not appear here, and conditionally
registered routes are listed even when their dependency is nil at runtime.

Provenance is file-level on purpose. This document is staleness-gated, and
citing `file:line` would make every unrelated edit above a registration
(a new import, a doc comment) fail the gate without any API change.

| Metric | Count |
|--------|-------|
| Spec operations | 168 |
| Registered routes found in Go | 167 |
| Matched | 167 |
| Registered but undocumented | 0 |
| Documented, served by a subtree mount | 1 |
| Documented but not registered | 0 |

### Registered but missing from `api/openapi.yaml`

None.

### Documented, served by a subtree mount

No route of their own, but an opaque `mux.Handle` subtree forwards every
path beneath it — so these are served, not missing.

| Method | Path | Mount |
|--------|------|-------|
| GET | `/api/auth/token` | `/api/auth/` in `internal/webui/app/routes.go` |

### Documented but not registered

None.
