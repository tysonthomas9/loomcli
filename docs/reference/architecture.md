<!-- Code generated by scripts/loomdoc. DO NOT EDIT. -->
<!-- package-layer architecture, from the depguard rules in .golangci.yml and the module import graph. -->
<!-- Regenerate with: make docs-gen -->

## About this document

This is the map of Loom's **internal package layers**: how `internal/…` is
partitioned and which imports may cross which boundary. Everything below this
preamble is generated — the rules come from the depguard section of
`.golangci.yml`, the package purposes from package doc comments, and the
cross-layer edges from the real compiled import graph. Regenerate with
`make docs-gen`; edit this preamble, not the generated body.

These boundaries are enforced, not advisory: `depguard` runs under `make check`
(the Go lint step) and fails the build when a package imports across a forbidden
edge. If the generated sections below ever disagreed with the code, CI would
already be red.

## Why the layers exist

The rules keep the dependency arrow pointing one way, `sdk → infra → web → cli`,
so the reusable core never reaches back up into whatever process embeds it. The
SDK layer — domain types, the issue-**backend** interface, the auth-aware HTTP
client — is a leaf that must not import infra, web, or CLI packages, so it can be
linked into a remote worker or a workflow bundle without pulling in the daemon,
the web server, or cobra. Infra may build on the SDK but not on web or CLI; the
web layer may use both but must not import CLI code.

"Backend" here means the **issue backend** — the pluggable issue-tracking data
layer (`internal/backend` interface, with `internal/backend/{api,fleet,agentipc,
mapping}` implementations). It is not the **AI CLI backend**
(`internal/cli/backends`, the `claude`/`codex`/… agent processes), which lives in
the CLI layer. See `docs/loom-glossary.md` for the other overloaded terms.

## Reading the generated sections

**Layer boundaries** and the per-rule detail are the enforced intent. **Actual
cross-layer imports** is the real graph; its forbidden-import check only
re-derives what depguard already gates, so it should always report agreement.
**Gaps and drift** is where to look for trouble — deny entries that no longer
match a package, globs that match nothing, packages that lint excludes, and
layer definitions referenced from outside the repository. Read that section as a
to-do list, not as settled architecture. Note too that each rule governs exactly
the packages its file globs match, which is not always every package its name
might suggest; the per-rule package list is the ground truth.

## Architecture: Package Layers

Loom's internal packages are grouped into enforced layers by the depguard
linter (`.golangci.yml`, `linters.settings.depguard.rules`). This document is generated
from those rules and cross-checked against the module's real import graph, so
it always reflects what the build enforces.

The intended dependency direction, from the config: **sdk → infra → web → cli**.

### Layer boundaries

Each depguard rule isolates a set of packages (matched by file globs) and
forbids them from importing named packages. `lax` mode denies only the listed
packages; `strict` mode additionally restricts imports to an allow-list. The
*Governed* count is the tracked packages whose files the rule's globs match.

| Rule | Mode | Governed packages | Denied imports |
|------|------|-------------------|----------------|
| `data-isolation` | lax | 1 | 18 |
| `handler-layer-isolation` | lax | 1 | 5 |
| `infra-isolation` | lax | 26 | 2 |
| `sdk-leaf` | lax | 10 | 19 |
| `webui-isolation` | lax | 43 | 1 |

80 of 153 first-party `internal/` packages are governed by a layer rule; the
remaining 73 are not constrained by these rules (depguard isolates only the
enumerated packages).

### Rules

#### `data-isolation` (list-mode: lax)

Applies to files matching:

- `**/internal/cli/data/**`

Forbids importing:

- `internal/atomicfile` — cli/data must not import infra packages
- `internal/backend/agentipc` — cli/data must not use the agentipc backend (HTTP only)
- `internal/backend/beads` — cli/data must not use the beads backend (HTTP only)
- `internal/backend/fleet` — cli/data must not use the fleet backend (HTTP only)
- `internal/backend/mapping` — cli/data must not import infra backend mapping
- `internal/circuitbreaker` — cli/data must not import infra packages
- `internal/cli` — cli/data must not import cli root or cli sub-packages (sdk-only; flat layout prevents prefix-match self-collision)
- `internal/configlock` — cli/data must not import infra packages
- `internal/debug` — cli/data must not import infra packages
- `internal/events/otelexport` — cli/data must not import infra packages
- `internal/kv` — cli/data must not import infra packages
- `internal/lockfile` — cli/data must not import infra packages
- `internal/logrouter` — cli/data must not import infra packages
- `internal/rpc` — cli/data must not import infra packages
- `internal/sessions` — cli/data must not import infra packages
- `internal/testutil` — cli/data must not import infra packages
- `internal/usage` — cli/data must not import infra packages
- `internal/webui` — cli/data must not import webui

Governs 1 tracked package(s):

- `internal/cli/data` — Package data contains the `loom data ...` command subtree — thin CLI commands that interact with local and remote loom issue backends.

#### `handler-layer-isolation` (list-mode: lax)

Applies to files matching:

- `**/handler/**`

Forbids importing:

- `internal/backend` — Handlers must not import backend directly; use the service layer (internal/webui/service)
- `internal/notify` — Handlers must not import notify directly; use the service layer (internal/webui/service)
- `internal/store` — Handlers must not import store directly; use the service layer (internal/webui/service)
- `internal/webui/daemon` — Handlers must not import daemon directly; use the service layer (internal/webui/service)
- `internal/webui/store` — Handlers must not import store directly; use the service layer (internal/webui/service)

Governs 1 tracked package(s):

- `internal/webui/server/handler` — Package handler provides HTTP request/response helpers for the webui handler layer.

#### `infra-isolation` (list-mode: lax)

Applies to files matching:

- `**/internal/rpc/**`
- `**/internal/sessions/**`
- `**/internal/lockfile/**`
- `**/internal/circuitbreaker/**`
- `**/internal/kv/**`
- `**/internal/configlock/**`
- `**/internal/usage/**`
- `**/internal/logrouter/**`
- `**/internal/atomicfile/**`
- `**/internal/gitbranch/**`
- `**/internal/workspace/**`
- `**/internal/debug/**`
- `**/internal/testutil/**`
- `**/internal/backend/mapping/**`
- `**/internal/backend/beads/**`
- `**/internal/backend/fleet/**`
- `**/internal/backend/agentipc/**`
- `**/internal/backend/api/**`
- `**/internal/events/otelexport/**`

Forbids importing:

- `internal/cli` — Infra packages must not import cli
- `internal/webui` — Infra packages must not import webui

Governs 26 tracked package(s):

- `internal/atomicfile` — Package atomicfile persists a file via write-temp-then-rename in the target directory, so a concurrent reader never observes a half-written file.
- `internal/backend/agentipc` — Package agentipc implements backend.IssueBackend by proxying the three IPC-supported mutation operations (ClaimIssue, Update, Close) to the loom daemon's agent IPC Unix socket.
- `internal/backend/api` — Package api implements backend.IssueBackend as an HTTP REST client against a loom server's workspace-scoped API endpoints.
- `internal/backend/api/gen` — Package gen provides primitives to interact with the openapi HTTP API.
- `internal/backend/fleet` — Package fleet implements backend.IssueBackend as an HTTP REST client against the fleet server's workspace-scoped API endpoints.
- `internal/backend/mapping` — Package mapping converts between backend wire types and entity domain types.
- `internal/circuitbreaker` — Package circuitbreaker implements the circuit breaker pattern for protecting against cascading failures when calling unreliable services.
- `internal/configlock` — Package configlock serializes load-mutate-save sequences over a loom config directory by holding a blocking exclusive advisory lock on its config.lock file, so two loom processes cannot interleave and clobber each other's writes.
- `internal/debug` — Package debug holds the loom CLI's process-global output-verbosity switches: LOOM_DEBUG (or SetVerbose) gates Logf/Printf diagnostics, and quiet mode suppresses PrintNormal.
- `internal/events/otelexport` — Package otelexport subscribes to an events.Bus and turns loom lifecycle events into OpenTelemetry spans and metrics pushed to an OTLP collector, re-rooting each span under the W3C trace context the event carried so event-driven spans join the originating request's trace.
- `internal/gitbranch` — Package gitbranch contains shared branch-ref inspection and recovery helpers.
- `internal/kv` — Package kv provides atomic Redis operations for distributed task management.
- `internal/lockfile` — Package lockfile provides loom's advisory file-locking primitives with per-platform implementations (unix/windows/wasm), a liveness check on a PID, and the daemon.lock protocol: its JSON metadata (PID, database, version) and a cheap "is a daemon already running" probe callers use before paying for an RPC timeout.
- `internal/logrouter` — Package logrouter tees an agent process's piped stdout into two size-rotating log files at once: the agent's own log and the log for whichever task/phase the agent's .agent.lock currently names, re-targeting as an fsnotify watcher sees that lock change.
- `internal/rpc` — Package rpc is the client half and wire protocol of the local loom daemon's unix-socket RPC — the issue-tracker operation family (create/close/list, dependencies, gates, mutation streams), the request/response payload types, socket-path resolution, token auth, and the daemon's request metrics.
- `internal/sessions` — Package sessions owns the local on-disk session record: one directory per agent run under <runtimeDir>/sessions/ (metadata, prompt, transcript) plus the queryable index.jsonl, and mirrors the agent CLI's own rollout/transcript file into it.
- `internal/sessions/eventstore` — Package eventstore is loom's durable, authoritative record of a run's transcript: ONE events.jsonl per logical run (the dir the caller supplies), holding ALL of the run's EventEnvelopes — the parent conversation AND its subagents, distinguished within the file by (HarnessSessionID, ParentSessionID).
- `internal/sessions/redact` — Package redact scrubs secrets out of captured agent transcripts before they hit disk.
- `internal/sessions/transcript` — Package transcript provides shared types and utilities for parsing JSONL transcripts written by AI coding agents (Claude Code, Cursor).
- `internal/sessions/transcript/backends` — Package backends is the backend-dispatching entry point for transcript parsing.
- `internal/sessions/transcript/claude` — Package claude parses Claude Code's native JSONL transcript format into the canonical transcript.Event stream.
- `internal/sessions/transcript/codex` — Package codex parses OpenAI Codex CLI's native rollout JSONL into the canonical transcript.Event stream by DELEGATING to harness-wrapper's codex parser — the per-harness Codex parsing knowledge now lives in one place (the wrapper).
- `internal/sessions/transcript/opencode` — Package opencode parses OpenCode's native `opencode export` JSON transcript into the canonical transcript.Event stream.
- `internal/testutil` — Package testutil provides shared test utilities for use across packages.
- `internal/usage` — Package usage tallies token consumption and estimated cost for one agent run: a Collector that de-duplicates the agent CLI's streamed usage events, per-agent-backend pricing tiers, and a flock-serialized append-only .loom/usage.jsonl store with filtered queries and purge.
- `internal/workspace` — Package workspace resolves a workspace key (falling back to LOOM_WORKSPACE_ID) and shortens it to the 8-character prefix that namespaces tmux session names, so the CLI (internal/cli/automode) and the web UI (internal/webui/terminal, appinfra) compute the same session name for the same workspace.

#### `sdk-leaf` (list-mode: lax)

Applies to files matching:

- `**/internal/types/**`
- `**/internal/entity/**`
- `**/internal/backend/*.go`
- `**/internal/notify/**`
- `**/internal/agenterr/**`
- `**/internal/authmode/**`
- `**/internal/workspaceerrors/**`
- `**/internal/events/*.go`
- `**/internal/ops/**`
- `**/internal/httpclient/**`

Forbids importing:

- `internal/atomicfile` — SDK-layer packages must not import infra packages
- `internal/backend/agentipc` — SDK-layer packages must not import infra packages
- `internal/backend/api` — SDK-layer packages must not import infra packages
- `internal/backend/beads` — SDK-layer packages must not import infra packages
- `internal/backend/fleet` — SDK-layer packages must not import infra packages
- `internal/backend/mapping` — SDK-layer packages must not import infra packages
- `internal/circuitbreaker` — SDK-layer packages must not import infra packages
- `internal/cli` — SDK-layer packages must not import cli
- `internal/configlock` — SDK-layer packages must not import infra packages
- `internal/debug` — SDK-layer packages must not import infra packages
- `internal/events/otelexport` — SDK-layer packages must not import infra packages
- `internal/kv` — SDK-layer packages must not import infra packages
- `internal/lockfile` — SDK-layer packages must not import infra packages
- `internal/logrouter` — SDK-layer packages must not import infra packages
- `internal/rpc` — SDK-layer packages must not import infra packages
- `internal/sessions` — SDK-layer packages must not import infra packages
- `internal/testutil` — SDK-layer packages must not import infra packages
- `internal/usage` — SDK-layer packages must not import infra packages
- `internal/webui` — SDK-layer packages must not import webui

Governs 10 tracked package(s):

- `internal/agenterr` — Package agenterr classifies a failed agent-backend run into a single Outcome: either a harness-wrapper error class parsed from the subprocess log/output, or a loom-domain outcome (no work, lock conflict, spawn failure, agent CLI missing).
- `internal/authmode` — Package authmode defines the two authentication modes a loom server advertises — "open" (none) and "oidc" — plus validation of the wire string.
- `internal/backend` — Package backend defines the pluggable data access interface for issue tracking.
- `internal/entity` — Package entity defines the canonical domain types for loomcli's V2 architecture.
- `internal/events` — Package events defines loom's telemetry event envelope (task, agent, epic and system lifecycle records), a Bus that appends them to rotating per-day JSONL files while fanning them out to in-process listeners, and a MetricsStore that replays those files back into aggregate snapshots.
- `internal/httpclient` — Package httpclient provides an authenticated HTTP client for CLI-to-server communication.
- `internal/notify` — Package notify provides an in-process pub/sub bus with workspace-scoped subscriptions.
- `internal/ops` — Package ops defines cross-layer operation interfaces and their associated types.
- `internal/types` — Package types defines core issue-tracking data structures.
- `internal/workspaceerrors` — Package workspaceerrors defines the classified failure type for workspace creation — a Code (AlreadyExists, PathNotFound, NotGitRepo, GitFailed, ConfigFailed, SecurityViolation) plus message and wrapped cause — so callers branch on the category instead of matching error strings.

#### `webui-isolation` (list-mode: lax)

Applies to files matching:

- `**/internal/webui/**`

Forbids importing:

- `internal/cli` — WebUI must not import cli

Governs 43 tracked package(s):

- `internal/webui` — Package webui holds the web UI server's wiring contract and its cross-cutting HTTP infrastructure: ServerConfig (assembled by internal/cli/serve, consumed by internal/webui/app), Prometheus/OTel/Sentry middleware, the /api/auth reverse proxy to the external auth service, SSRF-guarded dialing, and the daemon circuit-breaker HealthDoctor.
- `internal/webui/app` — Package app provides the web UI server assembly for loomcli.
- `internal/webui/appinfra` — Package appinfra consolidates infrastructure initialization for the webui/app composition root.
- `internal/webui/appstores` — Package appstores consolidates Redis-backed store initialization for the webui/app composition root.
- `internal/webui/coordinator` — Package coordinator provides the LifecycleHook interface and WorkspaceRegistry for managing per-workspace subsystem lifecycle in the webui layer.
- `internal/webui/daemon` — Package daemon provides RPC client connection infrastructure for the loomcli web UI server.
- `internal/webui/editor` — Package editor holds the registry of external editors and git clients (VS Code, Cursor, Zed, JetBrains, Sourcetree, ...), detects which are installed on the machine running loom, and launches one against a workspace path via a per-GOOS implementation.
- `internal/webui/fileaccess` — Package fileaccess carries the per-request file capabilities (read, write, sensitive) for the web UI's file browser and classifies credential-like paths (.env, .pem, .netrc, SSH keys) as sensitive.
- `internal/webui/fleet` — Package fleet provides Redis-based coordination for multi-server Fleet API.
- `internal/webui/handlermux` — Package handlermux consolidates handler-package imports for building workspace-scoped HTTP route modules and workspace route handlers.
- `internal/webui/handlers/agentcontrol` — Package agentcontrol serves the workspace-scoped agent lifecycle HTTP routes (start/stop/restart/yield, plus a bare agent list) by forwarding each one to the local daemon's control socket through an injected callback.
- `internal/webui/handlers/agents` — Package agents serves the workspace-scoped agent-assignment HTTP routes — list/create/update/delete plus the start/stop/restart/yield lifecycle commands — over service.AgentService (fleet-db), broadcasting each mutation to the SSE hub.
- `internal/webui/handlers/approvals` — Package approvals serves the minimal session-authenticated approval endpoint (ARCHITECTURE-PROPOSAL §7 step 8; the Phase D locked decision closing vet A2).
- `internal/webui/handlers/driverapi` — Package driverapi serves the driver-op HTTP API (SDK v2 transport): the same operations the hidden `loom driver` CLI subcommands expose, but over loom serve so workflow bundles talk HTTP instead of spawning CLI subprocesses.
- `internal/webui/handlers/git` — Package git serves the workspace-scoped git HTTP routes for agent worktrees (push/pull/sync/PR/reset/status/target), the diff-stat, commit and file diff endpoints, and the gh-backed pull-request list, over service.AgentService and service.DiffService.
- `internal/webui/handlers/health` — Package health serves the web UI's liveness, stats, metrics, and readiness endpoints: /api/health (with a daemon-free variant), issue-count statistics, SSE-hub and fleet claim metrics, and per-workspace runtime readiness — whether a daemon or fleet runtime can actually run agent work right now.
- `internal/webui/handlers/issues` — Package issues serves the workspace-scoped issue-tracker HTTP routes over service.IssueService (fleet-db): issue CRUD, close/reopen/claim/move, comments, events, dependencies, search, the ready-issue queue, per-issue terminal tab persistence, and session history.
- `internal/webui/handlers/localsettings` — Package localsettings serves the GET/PATCH HTTP endpoints for this machine's desktop-local runtime settings — embedded fleet-db Redis connection, default agent runtime, local task-runner model, and Daytona/GitHub runtime credentials — persisting them via internal/localsettings and returning secret-sanitized responses.
- `internal/webui/handlers/misc` — Package misc is a grab-bag of webui HTTP handlers that never earned their own package: its Module registers the scope-rooted workspace file browser, and it also exports standalone handlers for auth config, AI CLI backend health, client error reports, editor launching, agent/task logs, session transcripts, and the worker registration API.
- `internal/webui/handlers/onboarding` — Package onboarding serves the web UI's one-shot "first task" route, which creates a starter issue and immediately starts an agent on it, compensating by deleting the issue again if that start fails.
- `internal/webui/handlers/prreview` — Package prreview serves the web UI's pull request review routes: listing and reading PRs and diffs via internal/connector's GitHub provider, posting reviews, and provisioning the per-PR "pr-reviewer" agent — a detached git worktree at the PR head plus a codex conversation streamed back to the client over SSE.
- `internal/webui/handlers/taskrunapi` — Package taskrunapi serves the task-runner HTTP API on loom serve: the task-run operations sdk/runner.js (TaskRunClient) used to perform against fleet-db directly (logs.append, heartbeat, completeRun, getTaskRun, artifacts declare/get/list/upload/finalize), re-hosted behind serve so task harnesses — which execute model-generated code — never hold fleet-db credentials (§9.1: only loom serve touches fleet-db; closes the runner leg of vet A4).
- `internal/webui/handlers/terminal` — Package terminal serves the web UI's workspace-scoped terminal HTTP routes: the PTY-backed terminal WebSocket and its one-time auth token, the tmux-backed attach path for CLI auto-mode agent sessions, and the terminal tab-metadata, UI-state, sessions-by-issue and setup endpoints.
- `internal/webui/handlers/webhooks` — Package webhooks implements the inbound external-event ingestion surface for Loom's trigger-driven driver workflows.
- `internal/webui/handlers/workflows` — Package workflows serves the HTTP surface over internal/workflows and internal/driver: registering a workflow version from uploaded source, creating a driver run (fail-closed on an AI CLI backend preflight when an epic-runner run would execute locally), and reading or SSE-streaming that run's events.
- `internal/webui/handlers/workspace` — Package workspace serves the web UI's workspace management routes over service.WorkspaceService: list, create, rename, delete, reorder, default and active workspace selection, repo membership, async job status, and the per-workspace agent CLI backend and design-format settings.
- `internal/webui/hooks` — Package hooks implements the coordinator.LifecycleHook set the web UI runs when a workspace is registered or deregistered: the per-workspace fleet-db issue backend and fleet store, the mutation subscriber, the web-terminal PTY manager, and tmux session reaping.
- `internal/webui/issuetabs` — Package issuetabs provides Redis-backed persistence for issue-scoped tab state.
- `internal/webui/localredis` — Package localredis provides an in-process miniredis instance with JSON snapshot persistence.
- `internal/webui/log` — Package log owns the on-disk layout of agent and task logs under ~/.loom/logs/<workspace>/: path resolution, containment and symlink-safe opening, tail reads, and SSE streaming of newly appended bytes.
- `internal/webui/modbuilder` — Package modbuilder constructs workspace-scoped handler modules for the webui/app composition root.
- `internal/webui/server/dto` — Package dto defines Data Transfer Objects for the loomcli HTTP API.
- `internal/webui/server/handler` — Package handler provides HTTP request/response helpers for the webui handler layer.
- `internal/webui/server/middleware` — Package middleware holds the net/http request-path wrappers the web UI server composes around its routes: JWT/JWKS auth and user identity, workspace resolution into the request context, file-access RBAC, CORS, security headers, rate limiting, request logging, and panic recovery.
- `internal/webui/server/realtime` — Package realtime owns the web UI's server-to-browser push plumbing: the Hub that fans mutation events out over SSE (with Last-Event-ID catch-up), the short-lived HMAC tokens authorizing SSE and terminal WebSocket upgrades, and the PTY/tmux-to-WebSocket relay.
- `internal/webui/service` — Package service defines shared types for the webui service layer.
- `internal/webui/service/pathsec` — Package pathsec owns shared service-layer file path security validators for webui file and diff access.
- `internal/webui/sessionhistory` — Package sessionhistory provides Redis-backed persistence for terminal session audit records.
- `internal/webui/storeadapter` — Package storeadapter exposes ops.WorkspaceData-shaped views over a store.Store handle.
- `internal/webui/subscription` — Package subscription bridges fleet-db mutation events onto the web UI's realtime.Hub so browsers see issue-tracker changes over SSE: one long-polling subscriber per active workspace, activated and idle-reaped by MultiWorkspaceSubscriber, plus the SSE token-exchange handler and route module.
- `internal/webui/svcimpl` — Package svcimpl holds the concrete implementations behind the internal/webui/service interfaces: agent, session, diff, workspace job store, and the file service with its rooted, symlink-hardened file store.
- `internal/webui/tabmeta` — Package tabmeta provides Redis-backed persistence for terminal tab metadata.
- `internal/webui/terminal` — Package terminal owns the web UI's PTY layer: shell sessions keyed on (workspace, session) that outlive any single WebSocket (PTYManager / MultiPTYManager behind the PTYSource interface), the Redis-backed tab-metadata and UI-state TerminalService, and the session launch-command resolver — including ValidBackends, the accepted agent CLI backend names (claude, codex, ...) the terminal dropdown validates against.

### Actual cross-layer imports

Computed from the loaded import graph (git-tracked `internal/` only). For each
rule, the other rules whose packages its own packages actually import:

- `data-isolation` → `infra-isolation`, `sdk-leaf`
- `handler-layer-isolation` → `webui-isolation`
- `infra-isolation` → (ungoverned), `sdk-leaf`
- `sdk-leaf` → (none — imports no other governed layer)
- `webui-isolation` → (ungoverned), `handler-layer-isolation`, `infra-isolation`, `sdk-leaf`

**Forbidden-import check.**

No governed package imports a package its own rule denies — the real graph
agrees with the enforced boundaries.

### Gaps and drift

Findings where the enforced config and the real tree disagree.

**Layer definitions referenced outside the repository.** The depguard config
cites these absolute paths, which are outside the repository tree and therefore
not version-controlled with the rules they explain — the rationale for the
boundaries cannot be regenerated or reviewed from this repo:

- `/Users/tyson/.claude/plans/purrfect-weaving-stream.md`

**Ungoverned packages.** No rule's file globs match these 73 of 153 `internal/`
packages, so depguard checks none of their imports and the "enforced, not
advisory" guarantee above does not reach them. The depguard config asks that
new packages be added to the appropriate rule; these are the ones that were
not. A green lint run says nothing about any edge listed here:

- `internal/agentinbox`
- `internal/agentpolicy`
- `internal/backend/backendtest`
- `internal/backendnames`
- `internal/bootstrap`
- `internal/cli`
- `internal/cli/agent`
- `internal/cli/agent/lead`
- `internal/cli/agent/tsruntime`
- `internal/cli/agentdef`
- `internal/cli/automode`
- `internal/cli/backendcheck`
- `internal/cli/backends`
- `internal/cli/cleanup`
- `internal/cli/clitest`
- `internal/cli/cmdstore`
- `internal/cli/config`
- `internal/cli/connector`
- `internal/cli/daemon`
- `internal/cli/daemon/supervisor`
- `internal/cli/daemonregistry`
- `internal/cli/doctor`
- `internal/cli/driver`
- `internal/cli/envfilter`
- `internal/cli/epic`
- `internal/cli/git`
- `internal/cli/hooks`
- `internal/cli/local`
- `internal/cli/monitor`
- `internal/cli/repo`
- `internal/cli/role`
- `internal/cli/serve`
- `internal/cli/serve/daemonwire`
- `internal/cli/serve/install`
- `internal/cli/serve/logroutercmd`
- `internal/cli/serve/metricscmd`
- `internal/cli/serve/observability`
- `internal/cli/serve/opsimpl`
- `internal/cli/serve/serveadapter`
- `internal/cli/serve/usagecmd`
- `internal/cli/serve/worker`
- `internal/cli/serve/workspacemgr`
- `internal/cli/sessionfinalize`
- `internal/cli/stack`
- `internal/cli/trigger`
- `internal/cli/workflow`
- `internal/cli/workspace`
- `internal/connector`
- `internal/connector/providers`
- `internal/domain`
- `internal/driver`
- `internal/driver/runtypes`
- `internal/driver/sandbox`
- `internal/epicrunner`
- `internal/fleethttp`
- `internal/harness`
- `internal/harness/fakeharness/mock`
- `internal/infra/fleetdb`
- `internal/infra/memstore`
- `internal/leadcontrol`
- `internal/localsettings`
- `internal/localworkspace`
- `internal/netutil`
- `internal/observability/tracing`
- `internal/runtimectx`
- `internal/runtimepreflight`
- `internal/stacklineage`
- `internal/stackpublish`
- `internal/stackstore`
- `internal/store`
- `internal/store/storetest`
- `internal/trigger`
- `internal/workflows`

**Stale deny targets.** These import paths are denied by depguard rules, but no
git-tracked package provides them (removed, renamed, or not yet added), so the
deny entries protect nothing today:

- `internal/backend/beads` — referenced by `data-isolation`, `sdk-leaf`
- `internal/webui/store` — referenced by `handler-layer-isolation`

**Dead file globs.** These rule globs match no git-tracked package:

- `infra-isolation`: `**/internal/backend/beads/**`

**Governed but lint-excluded.** `linters.exclusions.paths` (`internal/backend/api/gen`, `third_party`) removes these
paths from every linter, depguard included. The packages below match a rule's
file globs — so they appear under a rule above — but depguard never actually
checks them, so that coverage is nominal:

- `internal/backend/api/gen`
