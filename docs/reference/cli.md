<!-- Code generated by scripts/loomdoc. DO NOT EDIT. -->
<!-- loom CLI command reference, from the assembled cobra command tree in internal/cli. -->
<!-- Regenerate with: make docs-gen -->

# Loom CLI Reference

`loom` is the agent-management CLI. This page is the complete command reference,
derived from the assembled cobra command tree so it cannot drift from the binary.
It lists every command — including the hidden, internal-only ones that
`loom --help` omits — with usage, aliases, descriptions, and flags.

**How to read this**

- The [Command Index](#command-index) is the whole tree; each entry links to that
  command's section.
- [Global Flags](#global-flags) are the root's persistent flags. Every command
  inherits them, so they are documented once and not repeated per command.
- Under each command, the **Flags** table lists only that command's own
  (non-inherited) flags. A persistent flag defined on an intermediate parent
  (for example `loom data`'s `--output`) is documented on the parent that
  defines it and inherited by its subcommands. A command that defines its *own*
  flag under a global's name shadows the global, so that flag stays in the
  command's table — `loom pull`'s `-W, --workspace` is the command's flag, not
  the root's.
- Hidden commands are for internal or test-only use; they are included here for
  completeness but are absent from `loom --help`.
- `loom help` and `loom completion` are cobra's built-ins, installed during
  `Execute` rather than at construction; the generator installs them the same
  way before walking the tree (`scripts/loomdoc/cli.go:88-89`). The one gap is
  cobra's hidden shell-completion transport, `__complete` (aliased
  `__completeNoDesc`) — cobra registers it from an unexported method, so the
  generator cannot reach it.

For the rationale behind these commands and the workflows they compose, see the
design docs under `docs/design/`. This page is the inventory of *what* exists;
those documents cover *why*.

## Command Index

199 commands (26 hidden). Global flags apply to every command; see [Global Flags](#global-flags).

- [`loom`](#loom) — Agent management CLI for parallel Claude Code workflows
  - [`loom agent`](#loom-agent) — Run a custom agent with a user-defined prompt
  - [`loom agentdef`](#loom-agentdef) — Manage agent assignments within the active workspace
    - [`loom agentdef add`](#loom-agentdef-add) — Register an agent assignment in the active workspace
    - [`loom agentdef list`](#loom-agentdef-list) — List agent assignments in the active workspace
    - [`loom agentdef remove`](#loom-agentdef-remove) — Delete an agent assignment from the active workspace
    - [`loom agentdef show`](#loom-agentdef-show) — Show agent details
    - [`loom agentdef start`](#loom-agentdef-start) — Request an agent assignment to start
    - [`loom agentdef stop`](#loom-agentdef-stop) — Request an agent assignment to stop
  - [`loom backend`](#loom-backend) — Manage AI backends
    - [`loom backend health`](#loom-backend-health) — Check health status of all backends
    - [`loom backend info`](#loom-backend-info) — Show detailed info for a specific backend
    - [`loom backend list`](#loom-backend-list) — List all registered backends
  - [`loom claim`](#loom-claim) — Update the lock file with the task being worked on
  - [`loom cleanup`](#loom-cleanup) — Purge old data from sessions, usage, and event stores
  - [`loom complete`](#loom-complete) — Signal task completion to auto mode
  - [`loom completion`](#loom-completion) — Generate the autocompletion script for the specified shell
    - [`loom completion bash`](#loom-completion-bash) — Generate the autocompletion script for bash
    - [`loom completion fish`](#loom-completion-fish) — Generate the autocompletion script for fish
    - [`loom completion powershell`](#loom-completion-powershell) — Generate the autocompletion script for powershell
    - [`loom completion zsh`](#loom-completion-zsh) — Generate the autocompletion script for zsh
  - [`loom connector`](#loom-connector) — Manage per-source connectors: sealed credentials, egress grants, and the call audit trail
    - [`loom connector audit`](#loom-connector-audit) — List the connector-call audit journal for a run or a binding
    - [`loom connector create`](#loom-connector-create) — Register a connector; secrets are read from stdin and sealed before the store write
    - [`loom connector grant`](#loom-connector-grant) — Manage deny-by-default egress grants (binding × action × resource)
      - [`loom connector grant create`](#loom-connector-grant-create) — Grant one {action, resource pattern} to a binding against a connector
      - [`loom connector grant list`](#loom-connector-grant-list) — List active grants for a binding or a connector (revoked grants are filtered)
      - [`loom connector grant revoke`](#loom-connector-grant-revoke) — Revoke a grant; revoked grants never authorize egress again
    - [`loom connector list`](#loom-connector-list) — List connectors (always redacted: secrets and sealed credentials never appear)
    - [`loom connector rotate`](#loom-connector-rotate) — Rotate the inbound secret (dual-secret window) and optionally re-seal the outbound credential
  - [`loom daemon`](#loom-daemon) — Manage the agent supervisor daemon
    - [`loom daemon config`](#loom-daemon-config) — Show effective running configuration
    - [`loom daemon logs`](#loom-daemon-logs) — View agent logs
    - [`loom daemon profile`](#loom-daemon-profile) — Manage per-workspace daemon profile in fleet-db
      - [`loom daemon profile set`](#loom-daemon-profile-set) — Set a single daemon-profile field
      - [`loom daemon profile show`](#loom-daemon-profile-show) — Show the active workspace's daemon profile
      - [`loom daemon profile unset`](#loom-daemon-profile-unset) — Clear a daemon-profile field back to its default
    - [`loom daemon queue`](#loom-daemon-queue) — Preview an agent's filtered work queue
    - [`loom daemon restart`](#loom-daemon-restart) — Restart a single agent with fresh counters
    - [`loom daemon seed-transcript`](#loom-daemon-seed-transcript) — TEST-ONLY: seed an agent session + transcript artifact + transcript_ref in fleet-db _(hidden)_
    - [`loom daemon start`](#loom-daemon-start) — Start a previously stopped agent
    - [`loom daemon status`](#loom-daemon-status) — Show daemon and agent status
    - [`loom daemon stop`](#loom-daemon-stop) — Stop the running daemon or a single agent
  - [`loom data`](#loom-data) — Data-only commands for local or remote loom backends
    - [`loom data agent`](#loom-data-agent) — Control a single agent on the loom server (stop/start/restart/yield)
      - [`loom data agent restart`](#loom-data-agent-restart) — Restart an agent
      - [`loom data agent start`](#loom-data-agent-start) — Start a stopped agent
      - [`loom data agent stop`](#loom-data-agent-stop) — Yield the agent (or force-stop with --force)
      - [`loom data agent yield`](#loom-data-agent-yield) — Request that the agent yield after its current task
    - [`loom data agents`](#loom-data-agents) — List agents managed by the loom server daemon (HTTP)
    - [`loom data blocked`](#loom-data-blocked) — List issues that are blocked by other open issues (HTTP)
    - [`loom data claim`](#loom-data-claim) — Atomically claim an issue
    - [`loom data close`](#loom-data-close) — Close an issue (HTTP)
    - [`loom data comment`](#loom-data-comment) — Add a comment to an issue (HTTP)
    - [`loom data create`](#loom-data-create) — Create an issue
    - [`loom data list`](#loom-data-list) — List issues (HTTP)
    - [`loom data monitor`](#loom-data-monitor) — Show monitor dashboard from a loom server (HTTP, single-shot)
    - [`loom data ready`](#loom-data-ready) — List issues that are ready to work on (HTTP)
    - [`loom data show`](#loom-data-show) — Show a single issue by ID (HTTP)
    - [`loom data update`](#loom-data-update) — Update issue fields
  - [`loom doctor`](#loom-doctor) — Check loom installation and configuration health
  - [`loom driver`](#loom-driver) — Register and run dynamic Loom drivers
    - [`loom driver active-task-runs`](#loom-driver-active-task-runs) — List active child task runs for a driver runtime _(hidden)_
    - [`loom driver agent-orchestration-session`](#loom-driver-agent-orchestration-session) — Resolve the active orchestration session for an agent _(hidden)_
    - [`loom driver claim-ready`](#loom-driver-claim-ready) — Claim one ready task for a driver runtime _(hidden)_
    - [`loom driver complete-task`](#loom-driver-complete-task) — Complete a claimed FleetDB task for a driver runtime _(hidden)_
    - [`loom driver deliver-agent-message`](#loom-driver-deliver-agent-message) — Queue or deliver a workflow message to an agent runtime _(hidden)_
    - [`loom driver deliver-lead-assignment`](#loom-driver-deliver-lead-assignment) — Deliver the current backend epic assignment to a lead runtime _(hidden)_
    - [`loom driver epic-get`](#loom-driver-epic-get) — Read epic issue detail for a driver runtime _(hidden)_
    - [`loom driver epic-snapshot`](#loom-driver-epic-snapshot) — Snapshot epic child task state for a driver runtime _(hidden)_
    - [`loom driver exec-task`](#loom-driver-exec-task) — Execute one child task run for a driver runtime _(hidden)_
    - [`loom driver list-agents`](#loom-driver-list-agents) — List agents for a driver runtime _(hidden)_
    - [`loom driver recover-stale-tasks`](#loom-driver-recover-stale-tasks) — Fail stale child task runs and release their task claims _(hidden)_
    - [`loom driver register`](#loom-driver-register) — Register a built native Flue driver artifact as an immutable DriverVersion
    - [`loom driver release-task`](#loom-driver-release-task) — Release a claimed FleetDB task for a driver runtime _(hidden)_
    - [`loom driver run`](#loom-driver-run) — Record a queued DriverRun for a published driver
    - [`loom driver update-agent-parent`](#loom-driver-update-agent-parent) — Update an agent parent for a driver runtime _(hidden)_
    - [`loom driver work-task-run`](#loom-driver-work-task-run) — Claim and execute one queued task run for a worker runtime _(hidden)_
  - [`loom epic`](#loom-epic) — Manage epic-scoped work
    - [`loom epic run`](#loom-epic-run) — Drain an epic by running the epic-runner workflow
  - [`loom help`](#loom-help) — Help about any command
  - [`loom hooks`](#loom-hooks) — Manage agent lifecycle hooks _(hidden)_
    - [`loom hooks claude-code`](#loom-hooks-claude-code) — Claude Code hook handlers _(hidden)_
      - [`loom hooks claude-code post-task`](#loom-hooks-claude-code-post-task) — Handle Claude Code PostToolUse[Task] hook _(hidden)_
      - [`loom hooks claude-code pre-task`](#loom-hooks-claude-code-pre-task) — Handle Claude Code PreToolUse[Task] hook _(hidden)_
      - [`loom hooks claude-code session-end`](#loom-hooks-claude-code-session-end) — Handle Claude Code SessionEnd hook _(hidden)_
      - [`loom hooks claude-code session-start`](#loom-hooks-claude-code-session-start) — Handle Claude Code SessionStart hook _(hidden)_
      - [`loom hooks claude-code stop`](#loom-hooks-claude-code-stop) — Handle Claude Code Stop hook _(hidden)_
      - [`loom hooks claude-code user-prompt-submit`](#loom-hooks-claude-code-user-prompt-submit) — Handle Claude Code UserPromptSubmit hook _(hidden)_
      - [`loom hooks claude-code yield-guard`](#loom-hooks-claude-code-yield-guard) — Check yield file and block tools if yield requested _(hidden)_
    - [`loom hooks dispatch`](#loom-hooks-dispatch) — Dispatch a harness lifecycle hook to harness-wrapper _(hidden)_
    - [`loom hooks install`](#loom-hooks-install) — Install loom hooks into Claude Code settings
    - [`loom hooks status`](#loom-hooks-status) — Show loom hooks installation status
    - [`loom hooks uninstall`](#loom-hooks-uninstall) — Remove loom hooks from Claude Code settings
  - [`loom init`](#loom-init) — Initialize loom with guided setup
  - [`loom install-service`](#loom-install-service) — Generate or install a platform-native service definition for loom serve
  - [`loom lead`](#loom-lead) — Run the interactive terminal-agent runtime
  - [`loom list`](#loom-list) — List all agents (worktrees)
  - [`loom local`](#loom-local) — Manage the local desktop runtime
    - [`loom local drain`](#loom-local-drain) — Mark the local runtime as draining
    - [`loom local install-service`](#loom-local-install-service) — Install the persistent local runtime service
    - [`loom local logs`](#loom-local-logs) — Print local runtime log paths
    - [`loom local restart`](#loom-local-restart) — Restart the local runtime service
    - [`loom local resume`](#loom-local-resume) — Resume local runtime task claims
    - [`loom local service`](#loom-local-service) — Run the local runtime service in the foreground
    - [`loom local start`](#loom-local-start) — Start the local runtime service in the background
    - [`loom local status`](#loom-local-status) — Show local runtime status
    - [`loom local stop`](#loom-local-stop) — Stop the local runtime service
    - [`loom local uninstall-service`](#loom-local-uninstall-service) — Uninstall the persistent local runtime service
  - [`loom log-router`](#loom-log-router) — Route stdin to agent and task log files (internal) _(hidden)_
  - [`loom monitor`](#loom-monitor) — Display comprehensive agent and task dashboard
  - [`loom plan`](#loom-plan) — Run a planning agent
  - [`loom pr`](#loom-pr) — Create GitHub PR from worktree branch
  - [`loom pull`](#loom-pull) — Pull branch into worktree
  - [`loom push`](#loom-push) — Push worktree branch to target branch
  - [`loom recover`](#loom-recover) — Recover from agent error state
  - [`loom repo`](#loom-repo) — Manage repos within the active workspace
    - [`loom repo add`](#loom-repo-add) — Register a repo in the active workspace
    - [`loom repo list`](#loom-repo-list) — List repos in the active workspace
    - [`loom repo remove`](#loom-repo-remove) — Delete a repo from the active workspace
    - [`loom repo show`](#loom-repo-show) — Show repo details
  - [`loom reset`](#loom-reset) — Hard reset worktree to a specific branch
  - [`loom role`](#loom-role) — Manage roles within the active workspace
    - [`loom role add`](#loom-role-add) — Create a role definition in the active workspace
    - [`loom role list`](#loom-role-list) — List roles in the active workspace
    - [`loom role remove`](#loom-role-remove) — Delete a role from the active workspace
    - [`loom role set`](#loom-role-set) — Set a single field on an existing role
    - [`loom role show`](#loom-role-show) — Show role details
    - [`loom role unset`](#loom-role-unset) — Clear a clearable role field back to its default
  - [`loom serve`](#loom-serve) — Start HTTP server for agent status API
  - [`loom sessions`](#loom-sessions) — Manage agent sessions
    - [`loom sessions clean`](#loom-sessions-clean) — Remove old session data
  - [`loom stack`](#loom-stack) — Manage stack lineage and publish stacked pull requests
    - [`loom stack add`](#loom-stack-add) — Register a task in a stack (appends to the tip; --after to chain, --root for a parallel chain)
    - [`loom stack init`](#loom-stack-init) — Create a stack (e.g. epic:EPIC-1) rooted on a base branch
    - [`loom stack list`](#loom-stack-list) — List stacks in the active workspace
    - [`loom stack move`](#loom-stack-move) — Reorder a unit to sit after another unit
    - [`loom stack publish`](#loom-stack-publish) — Publish the stack as stacked pull requests
    - [`loom stack remove`](#loom-stack-remove) — Remove a unit (children reparent onto its predecessor)
    - [`loom stack restack`](#loom-stack-restack) — Rebase descendants of merged units onto the live base, resolving conflicts with an agent
    - [`loom stack set-base`](#loom-stack-set-base) — Set a unit's predecessor ("" for root)
    - [`loom stack show`](#loom-stack-show) — Show a stack and its ordered units
    - [`loom stack status`](#loom-stack-status) — Show each unit's PR, state, and live health (checks/review/mergeable)
    - [`loom stack validate`](#loom-stack-validate) — Check the stack's lineage is linear and acyclic
  - [`loom status`](#loom-status) — Show system overview
  - [`loom sync`](#loom-sync) — Push completed work then pull latest into all worktrees
  - [`loom task`](#loom-task) — Run an implementation agent
  - [`loom trigger`](#loom-trigger) — Manage trigger bindings and inspect trigger events/deliveries
    - [`loom trigger bindings`](#loom-trigger-bindings) — Manage trigger bindings
      - [`loom trigger bindings create`](#loom-trigger-bindings-create) — Create a trigger binding that maps a route key to a pinned driver version
      - [`loom trigger bindings list`](#loom-trigger-bindings-list) — List trigger bindings
      - [`loom trigger bindings show`](#loom-trigger-bindings-show) — Show a single trigger binding
      - [`loom trigger bindings update`](#loom-trigger-bindings-update) — Update Router v2 fields on an existing trigger binding
    - [`loom trigger deliveries`](#loom-trigger-deliveries) — Inspect persisted trigger deliveries
      - [`loom trigger deliveries list`](#loom-trigger-deliveries-list) — List trigger deliveries
      - [`loom trigger deliveries show`](#loom-trigger-deliveries-show) — Show a single trigger delivery
    - [`loom trigger events`](#loom-trigger-events) — Inspect persisted trigger events
      - [`loom trigger events list`](#loom-trigger-events-list) — List trigger events
      - [`loom trigger events show`](#loom-trigger-events-show) — Show a single trigger event
  - [`loom usage`](#loom-usage) — Display token usage and cost summaries
  - [`loom worker`](#loom-worker) — Run a remote agent worker that connects to a control plane
    - [`loom worker profile`](#loom-worker-profile) — Manage worker profiles in the active workspace
      - [`loom worker profile add`](#loom-worker-profile-add) — Create a worker profile
      - [`loom worker profile list`](#loom-worker-profile-list) — List worker profiles
      - [`loom worker profile remove`](#loom-worker-profile-remove) — Delete a worker profile
      - [`loom worker profile set`](#loom-worker-profile-set) — Set a worker profile field
      - [`loom worker profile show`](#loom-worker-profile-show) — Show worker profile details
      - [`loom worker profile unset`](#loom-worker-profile-unset) — Clear a worker profile field
    - [`loom worker service`](#loom-worker-service) — Manage long-running agent services in the active workspace
      - [`loom worker service add`](#loom-worker-service-add) — Create an agent service
      - [`loom worker service list`](#loom-worker-service-list) — List agent services
      - [`loom worker service remove`](#loom-worker-service-remove) — Delete an agent service
      - [`loom worker service set`](#loom-worker-service-set) — Set an agent service field
      - [`loom worker service show`](#loom-worker-service-show) — Show agent service details
      - [`loom worker service unset`](#loom-worker-service-unset) — Clear an agent service field
  - [`loom workflow`](#loom-workflow) — Author, approve, activate, and run Flue workflows
    - [`loom workflow activate`](#loom-workflow-activate) — Make a workflow DriverVersion the active version
    - [`loom workflow approve`](#loom-workflow-approve) — Approve one workflow DriverVersion for local process execution
    - [`loom workflow build`](#loom-workflow-build) — Build local workflow TypeScript source and register a non-active DriverVersion
    - [`loom workflow clone`](#loom-workflow-clone) — Copy built-in workflow TypeScript source into a local source tree
    - [`loom workflow digest`](#loom-workflow-digest) — Print the canonical source digest of a built-in workflow
    - [`loom workflow list`](#loom-workflow-list) — List registered workflows
    - [`loom workflow readyz`](#loom-workflow-readyz) — Check local workflow authoring prerequisites
    - [`loom workflow run`](#loom-workflow-run) — Create a DriverRun for a workflow
    - [`loom workflow unapprove`](#loom-workflow-unapprove) — Remove approval from one workflow DriverVersion
    - [`loom workflow versions`](#loom-workflow-versions) — List registered workflow versions
  - [`loom workspace`](#loom-workspace) — Manage multi-repo workspaces
    - [`loom workspace add`](#loom-workspace-add) — Create a new workspace in fleet-db
    - [`loom workspace create`](#loom-workspace-create) — Create a new workspace with git worktrees
    - [`loom workspace list`](#loom-workspace-list) — List all configured workspaces
    - [`loom workspace ops`](#loom-workspace-ops) — Agent-safe workspace operations
      - [`loom workspace ops diagnose`](#loom-workspace-ops-diagnose) — Diagnose workspace runtime and agent configuration
      - [`loom workspace ops ensure-runtime`](#loom-workspace-ops-ensure-runtime) — Ensure the local runtime and workspace daemon are running
      - [`loom workspace ops status`](#loom-workspace-ops-status) — Show workspace runtime status
    - [`loom workspace remove`](#loom-workspace-remove) — Remove a workspace and its worktrees
    - [`loom workspace set`](#loom-workspace-set) — Update workspace settings in fleet-db
    - [`loom workspace show`](#loom-workspace-show) — Show workspace details (defaults to active)
    - [`loom workspace status`](#loom-workspace-status) — Show workspace lifecycle state
    - [`loom workspace use`](#loom-workspace-use) — Remember the last selected workspace for UI convenience

## Global Flags

Persistent flags defined on the root command; every subcommand inherits them.

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--backend` | — | `string` | — | AI backend CLI to use (codex, claude, opencode). Env: LOOM_BACKEND |
| `--log-format` | — | `string` | `text` | Log format (text\|json) |
| `--log-output` | — | `string` | `stderr` | Log output destination (stderr\|<filepath>) |
| `--server` | — | `string` | — | Remote loom server base URL. When set, CLI uses HTTP API backend instead of local FleetDB. Env: LOOM_SERVER_URL |
| `--workspace` | — | `string` | — | Workspace ID. Env: LOOM_WORKSPACE |

## Command Reference

### loom

Agent management CLI for parallel Claude Code workflows

**Usage:** `loom [flags]`

```
loom - Agent Management CLI

Manage Claude Code agents working in parallel across workspace repos.

GETTING STARTED
  1. Select a FleetDB-backed workspace with registered repos.

  2. Create tasks for agents to work on:
     loom task create --title="Add login feature" --type=feature --priority=2

  3. Run agents:
     loom plan falcon    # Creates design, sets status=review
     loom lead           # Review and approve plans
     loom task falcon    # Implements approved design

KEY CONCEPTS
  Workspaces   FleetDB-backed repo groups where agents work independently.

  Agents       Claude processes that work on tasks:
               - 'plan' agent: researches and creates designs
               - 'task' agent: implements approved designs

  FleetDB      Issue tracker. Tasks flow through states:
               open → in_progress → review → open → closed

  Auto Mode    --auto flag runs agents continuously, processing multiple
               tasks until stopped (Ctrl+C) or idle timeout.

COMMANDS
  plan         Run a planning agent (creates designs, marks for review)
  task         Run an implementation agent (implements approved tasks)
  lead         Interactive mode for reviewing plans and managing backlog
  monitor      Dashboard showing agent status and task progress
  recover      Recover agent from error state (clear stale locks, reset tasks)
  push         Push worktree branches to target with AI conflict resolution
  pull         Pull integration branch into worktrees with AI conflict resolution
  sync         Full sync: push all completed work, then pull into all worktrees
  reset        Hard reset worktrees to a specific branch
  list         List all agents and their status

GLOBAL FLAGS
      --backend          AI backend CLI (codex, claude, opencode). Env: LOOM_BACKEND

ENVIRONMENT VARIABLES
  LOOM_DEFAULT_BRANCH    Default integration branch (default: main)
  LOOM_BACKEND           AI backend CLI to use (default: codex)

EXAMPLES
  loom plan falcon              # Run planning agent in falcon worktree
  loom task falcon --auto       # Continuous implementation mode
  loom lead                     # Interactive backlog management
  loom monitor                  # Watch agent progress
  loom push --all               # Push all worktrees to main
  loom pull --all               # Pull main into all worktrees
  loom sync                     # Full sync: push all + pull all
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--version` | `-v` | `bool` | `false` | Print version information |

### loom agent

Run a custom agent with a user-defined prompt

**Usage:** `loom agent <worktree> --prompt <path> [flags]`

```
Run a custom agent with a user-defined prompt template.

The agent command allows you to define custom agent roles by providing a prompt
template file. This is useful for specialized workflows like code review,
documentation generation, or domain-specific tasks.

Arguments:
  worktree    Worktree/workspace name (e.g., falcon) or path (required)

Required Flags:
  -p, --prompt    Path to prompt template file

Optional Flags:
  -f, --task-filter   Task filter: needs_design, has_design, or any (default: any)
  -a, --auto          Enable continuous mode (process multiple tasks)
  -i, --interval      Polling interval in seconds when no tasks (default: 30)
  -m, --max-tasks     Maximum tasks to process before exiting (0 = unlimited)
  -t, --idle-timeout  Exit after N minutes with no available tasks (0 = none)

Template Variables:
  {{.AgentName}}      Agent name (derived from worktree)
  {{.WorktreeName}}   Worktree name
  {{.Role}}           Role name (always "custom" for this command)

Examples:
  loom agent falcon --prompt ./prompts/reviewer.txt
  loom agent falcon --prompt ./prompts/reviewer.txt --task-filter needs_design
  loom agent falcon --prompt ./prompts/reviewer.txt --auto --idle-timeout 30
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--auto` | `-a` | `bool` | `false` | Enable continuous mode (process multiple tasks) |
| `--daemon-mode` _(hidden)_ | — | `bool` | `false` | Internal: single task mode for daemon |
| `--idle-timeout` | `-t` | `int` | `0` | Exit after N minutes with no tasks (0 = none) |
| `--interval` | `-i` | `int` | `30` | Polling interval in seconds when no tasks available |
| `--max-tasks` | `-m` | `int` | `0` | Maximum tasks to process (0 = unlimited) |
| `--parent` | — | `string` | — | Filter tasks to descendants of this epic ID |
| `--prompt` | `-p` | `string` | — | Path to prompt template file |
| `--task-filter` | `-f` | `string` | `any` | Task filter: needs_design, has_design, or any |

### loom agentdef

Manage agent assignments within the active workspace

**Usage:** `loom agentdef [flags]`

```
Define long-lived agent assignments stored in fleet-db.

Distinct from 'loom agent <worktree>' which runs an actual agent process.
Phase 6 will unify these surfaces.
```

### loom agentdef add

Register an agent assignment in the active workspace

**Usage:** `loom agentdef add <NAME> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--auto` | — | `bool` | `false` | Auto-start on daemon up |
| `--backend` | — | `string` | — | AI backend override |
| `--budget-policy` | — | `string` | — | Budget/retry policy name |
| `--cross-repo` | — | `bool` | `false` | Allow tasks spanning repos |
| `--max-concurrency` | — | `int` | `0` | Maximum concurrent runs for orchestrator/service agents |
| `--mode` | — | `string` | — | Agent mode: ephemeral or service |
| `--orchestrator` | — | `string` | — | Parent lead/orchestrator session ID for the queued --task start command (overrides $LOOM_ORCHESTRATOR_SESSION_ID) |
| `--parent` | — | `string` | — | Epic ID to scope this agent to |
| `--repo-groups` | — | `stringSlice` | `[]` | Repo groups (comma-separated or repeat flag) |
| `--repos` | — | `stringSlice` | `[]` | Repo names (comma-separated or repeat flag) |
| `--role` | — | `string` | — | Role name (required) |
| `--task` | — | `string` | — | Pin this agent's first cycle to a specific task ID (claims that task instead of polling Ready) |
| `--task-filter` | — | `string` | — | Task filter for task-driven agents |

### loom agentdef list

List agent assignments in the active workspace

**Usage:** `loom agentdef list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom agentdef remove

Delete an agent assignment from the active workspace

**Usage:** `loom agentdef remove <NAME> [flags]`

### loom agentdef show

Show agent details

**Usage:** `loom agentdef show <NAME> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom agentdef start

Request an agent assignment to start

**Usage:** `loom agentdef start <NAME> [flags]`

### loom agentdef stop

Request an agent assignment to stop

**Usage:** `loom agentdef stop <NAME> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | — | `bool` | `false` | Stop without graceful yield when handled by a local daemon |

### loom backend

Manage AI backends

**Usage:** `loom backend [flags]`

```
Manage AI backend registrations and health status.

Subcommands:
  list     List all registered backends
  health   Check health status of all backends
  info     Show detailed info for a specific backend

Examples:
  loom backend list              # List all backends
  loom backend list --json       # JSON output for scripting
  loom backend health            # Check health of all backends
  loom backend info claude       # Show details for claude backend
```

### loom backend health

Check health status of all backends

**Usage:** `loom backend health [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | Output as JSON |

### loom backend info

Show detailed info for a specific backend

**Usage:** `loom backend info <name> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | Output as JSON |

### loom backend list

List all registered backends

**Usage:** `loom backend list [flags]`

**Aliases:** `ls`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | Output as JSON |

### loom claim

Update the lock file with the task being worked on

**Usage:** `loom claim <task-id> [flags]`

```
Update the agent lock file to record which task is being worked on.

This command is called after an agent claims a task through Loom. It updates
the lock file so that 'loom monitor' can show which task each agent is working on.
The lock-file update is bookkeeping for 'loom monitor'; failures do not affect
the actual task claim.

Arguments:
  task-id    The Loom task ID (e.g., loomcli-487 or loomcli-abc.1)

Examples:
  loom claim loomcli-487         # Record that we're working on loomcli-487
```

### loom cleanup

Purge old data from sessions, usage, and event stores

**Usage:** `loom cleanup [flags]`

```
Purge old data from all JSONL stores with configurable retention periods.

By default, data older than 30 days is removed. Each store has its own
retention flag. A dry-run mode previews what would be deleted.

Stores cleaned:
  - Sessions: old session directories + index.jsonl compaction
  - Usage:    old records from usage.jsonl
  - Events:   old day-based event JSONL files

Does NOT touch runtime-owned files (issues.jsonl, interactions.jsonl).
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--dry-run` | — | `bool` | `false` | Preview what would be deleted without modifying files |
| `--events-older-than` | — | `string` | `30d` | Purge event files older than this (e.g. 30d, 720h) |
| `--sessions-older-than` | — | `string` | `30d` | Purge sessions older than this (e.g. 30d, 720h) |
| `--usage-older-than` | — | `string` | `30d` | Purge usage records older than this (e.g. 30d, 720h) |

### loom complete

Signal task completion to auto mode

**Usage:** `loom complete [flags]`

```
Signal that the current task is complete.

This command is used by Claude agents to signal task completion to the
auto mode parent process. It writes a signal file to a temporary location
outside the git worktree, so it won't be affected by git clean operations.

Usage:
  loom complete    # Signal completion from current directory
```

### loom completion

Generate the autocompletion script for the specified shell

**Usage:** `loom completion [flags]`

```
Generate the autocompletion script for loom for the specified shell.
See each sub-command's help for details on how to use the generated script.
```

### loom completion bash

Generate the autocompletion script for bash

**Usage:** `loom completion bash`

```
Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(loom completion bash)

To load completions for every new session, execute once:

#### Linux:

	loom completion bash > /etc/bash_completion.d/loom

#### macOS:

	loom completion bash > $(brew --prefix)/etc/bash_completion.d/loom

You will need to start a new shell for this setup to take effect.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--no-descriptions` | — | `bool` | `false` | disable completion descriptions |

### loom completion fish

Generate the autocompletion script for fish

**Usage:** `loom completion fish [flags]`

```
Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

	loom completion fish | source

To load completions for every new session, execute once:

	loom completion fish > ~/.config/fish/completions/loom.fish

You will need to start a new shell for this setup to take effect.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--no-descriptions` | — | `bool` | `false` | disable completion descriptions |

### loom completion powershell

Generate the autocompletion script for powershell

**Usage:** `loom completion powershell [flags]`

```
Generate the autocompletion script for powershell.

To load completions in your current shell session:

	loom completion powershell | Out-String | Invoke-Expression

To load completions for every new session, add the output of the above command
to your powershell profile.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--no-descriptions` | — | `bool` | `false` | disable completion descriptions |

### loom completion zsh

Generate the autocompletion script for zsh

**Usage:** `loom completion zsh [flags]`

```
Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need
to enable it.  You can execute the following once:

	echo "autoload -U compinit; compinit" >> ~/.zshrc

To load completions in your current shell session:

	source <(loom completion zsh)

To load completions for every new session, execute once:

#### Linux:

	loom completion zsh > "${fpath[1]}/_loom"

#### macOS:

	loom completion zsh > $(brew --prefix)/share/zsh/site-functions/_loom

You will need to start a new shell for this setup to take effect.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--no-descriptions` | — | `bool` | `false` | disable completion descriptions |

### loom connector

Manage per-source connectors: sealed credentials, egress grants, and the call audit trail

**Usage:** `loom connector [flags]`

### loom connector audit

List the connector-call audit journal for a run or a binding

**Usage:** `loom connector audit [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--binding` | — | `string` | — | list calls for this binding id |
| `--decision` | — | `string` | — | filter by decision: granted\|denied\|stale_subject\|precondition_required\|upstream_error |
| `--json` | — | `bool` | `false` | JSON output |
| `--limit` | — | `int` | `0` | max results |
| `--run` | — | `string` | — | list calls for this driver run id |

### loom connector create

Register a connector; secrets are read from stdin and sealed before the store write

**Usage:** `loom connector create [flags]`

```
Registers a named connector for one source kind. The inbound webhook secret and the
outbound credential are read from stdin (first and second line respectively when both
flags are set) — never from argv, which leaks through process listings. The outbound
credential is sealed with the connector vault before the store ever sees it,
and the printed result is always redacted.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--credential-stdin` | — | `bool` | `false` | read the outbound credential from stdin (line after the inbound secret) and seal it |
| `--disabled` | — | `bool` | `false` | create the connector disabled |
| `--endpoint-path` | — | `string` | — | workspace-relative inbound webhook path (must start with /) |
| `--id` | — | `string` | — | connector id (default: the source kind, e.g. "github") |
| `--inbound-secret-stdin` | — | `bool` | `false` | read the inbound webhook secret from stdin (first line) |
| `--json` | — | `bool` | `false` | JSON output (always redacted) |
| `--name` | — | `string` | — | connector display name |
| `--source` | — | `string` | — | source kind: github\|slack\|datadog\|internal |

### loom connector grant

Manage deny-by-default egress grants (binding × action × resource)

**Usage:** `loom connector grant [flags]`

### loom connector grant create

Grant one {action, resource pattern} to a binding against a connector

**Usage:** `loom connector grant create [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--action` | — | `string` | — | dotted action verb, e.g. github.merge |
| `--binding` | — | `string` | — | trigger binding id the grant is scoped to |
| `--connector` | — | `string` | — | connector id the grant authorizes egress through |
| `--grant-id` | — | `string` | — | grant id (default: derived from binding and action) |
| `--json` | — | `bool` | `false` | JSON output |
| `--resource` | — | `string` | — | resource pattern, e.g. repo:octocat/hello |

### loom connector grant list

List active grants for a binding or a connector (revoked grants are filtered)

**Usage:** `loom connector grant list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--binding` | — | `string` | — | list active grants for this binding id |
| `--connector` | — | `string` | — | list active grants for this connector id |
| `--json` | — | `bool` | `false` | JSON output |

### loom connector grant revoke

Revoke a grant; revoked grants never authorize egress again

**Usage:** `loom connector grant revoke <grant-id> [flags]`

### loom connector list

List connectors (always redacted: secrets and sealed credentials never appear)

**Usage:** `loom connector list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output (always redacted) |
| `--source-kind` | — | `string` | — | filter by source kind |
| `--status` | — | `string` | — | filter by status: active\|disabled |

### loom connector rotate

Rotate the inbound secret (dual-secret window) and optionally re-seal the outbound credential

**Usage:** `loom connector rotate <connector-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--credential-stdin` | — | `bool` | `false` | read a new outbound credential from stdin (line after the secret) and re-seal it |
| `--inbound-secret-stdin` | — | `bool` | `false` | read the new inbound secret from stdin (first line; required) |
| `--inbound-window` | — | `duration` | `0s` | how long the previous inbound secret keeps verifying (default 15m, capped at 24h) |
| `--json` | — | `bool` | `false` | JSON output (always redacted) |

### loom daemon

Manage the agent supervisor daemon

**Usage:** `loom daemon [flags]`

```
Start, stop, and monitor the loom agent supervisor daemon.

The daemon reads FleetDB daemon profiles for the active workspace and
supervises the configured agents, automatically restarting them on failure
with exponential backoff.

Commands:
  loom daemon                      Start the supervisor in the foreground
  loom daemon status               Show daemon and agent status
  loom daemon logs                 View agent logs
  loom daemon stop                 Stop the running daemon
  loom daemon stop <agent>         Stop a single agent
  loom daemon start <agent>        Start a previously stopped agent
  loom daemon restart <agent>      Restart a single agent with fresh state
  loom daemon queue <agent>        Preview an agent's filtered work queue

Configuration is managed with FleetDB-backed daemon, role, and agent commands.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--dry-run` | — | `bool` | `false` | Validate config and print what would be started without actually starting |

### loom daemon config

Show effective running configuration

**Usage:** `loom daemon config [flags]`

```
Display the fully resolved FleetDB-backed daemon configuration as YAML.

Shows the active workspace daemon profile, roles, agents, and defaults.
Sensitive values (Redis URLs) are masked. The command works whether or not the
daemon is running.
```

### loom daemon logs

View agent logs

**Usage:** `loom daemon logs [agent-name] [flags]`

```
View agent logs without knowing the file path.

Examples:
  loom daemon logs              List available agents and their log paths
  loom daemon logs falcon       Show last 50 lines of falcon's log
  loom daemon logs falcon -n 100  Show last 100 lines
  loom daemon logs falcon -f    Follow/stream the log (like tail -f)
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--follow` | `-f` | `bool` | `false` | Follow log output (like tail -f) |
| `--lines` | `-n` | `int` | `50` | Number of lines to show |

### loom daemon profile

Manage per-workspace daemon profile in fleet-db

**Usage:** `loom daemon profile [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--workspace` | — | `string` | — | Workspace key (default: active) |

### loom daemon profile set

Set a single daemon-profile field

**Usage:** `loom daemon profile set <KEY> <VALUE> [flags]`

```
Set a daemon profile field. Supported keys:
  pid_file        string
  log_dir         string
  events_dir      string
  issue_backend   string (default: fleetdb)
  max_agents      integer
  startup_timeout integer (seconds)
```

### loom daemon profile show

Show the active workspace's daemon profile

**Usage:** `loom daemon profile show [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom daemon profile unset

Clear a daemon-profile field back to its default

**Usage:** `loom daemon profile unset <KEY> [flags]`

```
Revert a daemon profile field to its fleet-db default.
Same key set as 'set'; integer/pointer fields become nil, string
fields become empty.
```

### loom daemon queue

Preview an agent's filtered work queue

**Usage:** `loom daemon queue <agent-name> [flags]`

```
Show what tasks a specific agent would pick up next.

This command loads daemon configuration from FleetDB, resolves the named agent's role constraints
(task filter, skills, max priority, source repos), fetches ready issues,
scores them through the task router, and displays the results.

Works without the daemon running — pure CLI, no IPC/socket needed.

Examples:
  loom daemon queue spark       Show what spark would pick up
  loom daemon queue falcon      Show falcon's filtered queue
```

### loom daemon restart

Restart a single agent with fresh counters

**Usage:** `loom daemon restart <agent-name> [flags]`

### loom daemon seed-transcript

**Hidden command** — not shown in `loom --help`.

TEST-ONLY: seed an agent session + transcript artifact + transcript_ref in fleet-db

**Usage:** `loom daemon seed-transcript [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--backend` | — | `string` | `codex` | Backend label |
| `--content` | — | `string` | — | Canonical NDJSON transcript file (default: stdin) |
| `--session` | — | `string` | — | Agent session id (required) |
| `--task` | — | `string` | — | Task id the session belongs to (required) |
| `--workspace` | — | `string` | — | Workspace key (default: active) |

### loom daemon start

Start a previously stopped agent

**Usage:** `loom daemon start <agent-name> [flags]`

### loom daemon status

Show daemon and agent status

**Usage:** `loom daemon status [flags]`

### loom daemon stop

Stop the running daemon or a single agent

**Usage:** `loom daemon stop [agent-name] [flags]`

```
Stop the running daemon (no arguments) or stop a single agent by name.

Examples:
  loom daemon stop          Stop the entire daemon
  loom daemon stop falcon   Stop only the "falcon" agent
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | `-f` | `bool` | `false` | Skip graceful yield, send SIGTERM immediately (agent) or SIGKILL (daemon) |
| `--timeout` | `-t` | `int` | `0` | Yield timeout in seconds (default: 60; only for agent stop) |

### loom data

Data-only commands for local or remote loom backends

**Usage:** `loom data [flags]`

```
The 'loom data' subtree contains thin CLI commands that interact
with the configured loom issue backend. When --server or LOOM_SERVER_URL is
set, commands talk to that loom server over HTTP. Without a server, issue
commands use the local backend selected by the workspace configuration and
daemon IPC environment.

Use 'loom data' commands when:
  • You want a backend-aware command surface for agents and scripts.
  • You want to manage agents on a remote loom server.
  • You are scripting against a hosted loom deployment.

Examples:
  loom data show <id> --server http://localhost:8080
  loom data ready --limit 10
  loom data list --server http://localhost:8080 -o json | jq '.[].id'
  loom data monitor --server http://localhost:8080
  loom data agent stop falcon --server http://localhost:8080

Root-level --server / --workspace are mirrored into LOOM_SERVER_URL /
LOOM_WORKSPACE in PersistentPreRunE, so 'loom --server URL data show ID'
and 'loom data show ID --server URL' resolve identically. The data
subcommand reads from env, not from its own flag.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--output` | `-o` | `string` | `text` | Output format: text\|json |
| `--server` | — | `string` | — | Loom server base URL (or LOOM_SERVER_URL env var) |

### loom data agent

Control a single agent on the loom server (stop/start/restart/yield)

**Usage:** `loom data agent [flags]`

### loom data agent restart

Restart an agent

**Usage:** `loom data agent restart <agent-name> [flags]`

### loom data agent start

Start a stopped agent

**Usage:** `loom data agent start <agent-name> [flags]`

### loom data agent stop

Yield the agent (or force-stop with --force)

**Usage:** `loom data agent stop <agent-name> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | — | `bool` | `false` | Force-stop immediately instead of yielding |

### loom data agent yield

Request that the agent yield after its current task

**Usage:** `loom data agent yield <agent-name> [flags]`

### loom data agents

List agents managed by the loom server daemon (HTTP)

**Usage:** `loom data agents [flags]`

### loom data blocked

List issues that are blocked by other open issues (HTTP)

**Usage:** `loom data blocked [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--limit` | — | `int` | `0` | Maximum number of results (0 = server default) |
| `--parent` | — | `string` | — | Filter by parent issue ID |
| `--type` | — | `string` | — | Filter by issue type |

### loom data claim

Atomically claim an issue

**Usage:** `loom data claim <issue-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--actor` | — | `string` | — | Actor attempting the claim |

### loom data close

Close an issue (HTTP)

**Usage:** `loom data close <issue-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | — | `bool` | `false` | Force close even if blocked or dependencies open |
| `--reason` | — | `string` | — | Reason for closing |
| `--session` | — | `string` | — | Session identifier to attach to the close event |

### loom data comment

Add a comment to an issue (HTTP)

**Usage:** `loom data comment <issue-id> <text> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--author` | — | `string` | — | Comment author (defaults to server-side session user) |

### loom data create

Create an issue

**Usage:** `loom data create --title <title> [flags]`

```
Create an issue.

Idempotency: by default the create sends an X-Idempotency-Key derived from
the issue content and the current UTC date, so re-running the exact same
command (e.g. after a lost response) returns the already-created issue
instead of minting a duplicate. Use --idempotency-key to supply your own
key, or --no-idempotency to disable deduplication entirely.

--force only bypasses the server's soft-duplicate guard (same title+type
created moments ago); with an identical command the idempotency key still
returns the existing issue — combine with --no-idempotency to intentionally
create an identical duplicate.

Output: on success the last line of stdout is "CREATED <issue-id>" in text
mode. With -o json, stdout stays pure JSON (the created issue) and the
"CREATED <issue-id>" line goes to stderr. The exit code is non-zero only on
real failure, so success is checkable from the exit code alone.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--acceptance-criteria` | — | `string` | — | Acceptance criteria |
| `--assignee` | — | `string` | — | Assignee |
| `--created-by` | — | `string` | — | Creator |
| `--defer-until` | — | `string` | — | Defer until timestamp |
| `--depends-on` | — | `stringArray` | `[]` | Dependency issue ID (repeatable) |
| `--description` | — | `string` | — | Issue description |
| `--design` | — | `string` | — | Design notes |
| `--due-at` | — | `string` | — | Due timestamp |
| `--estimated-minutes` | — | `int` | `0` | Estimated effort in minutes |
| `--external-ref` | — | `string` | — | External reference |
| `--force` | — | `bool` | `false` | Bypass the soft-duplicate guard only; identical duplicates also need --no-idempotency |
| `--idempotency-key` | — | `string` | — | Explicit idempotency key (default: content hash of the create body + UTC date) |
| `--label` | — | `stringArray` | `[]` | Label to attach (repeatable) |
| `--no-idempotency` | — | `bool` | `false` | Disable idempotent create (always mint a new issue) |
| `--notes` | — | `string` | — | Internal notes |
| `--owner` | — | `string` | — | Owner |
| `--parent` | — | `string` | — | Parent issue ID |
| `--priority` | — | `int` | `2` | Priority (0 critical, 1 high, 2 medium, 3 low, 4 backlog) |
| `--source-repo` | — | `string` | — | Source repository ID |
| `--status` | — | `string` | — | Initial status |
| `--title` | — | `string` | — | Issue title |
| `--type` | — | `string` | `task` | Issue type (task\|bug\|feature\|epic\|chore) |

### loom data list

List issues (HTTP)

**Usage:** `loom data list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--limit` | — | `int` | `0` | Maximum number of results (0 = server default) |
| `--parent` | — | `string` | — | Filter by parent issue ID |
| `--priority` | — | `int` | `0` | Filter by priority (0-4) |
| `--status` | — | `string` | — | Filter by status (open\|in_progress\|review\|closed\|...) |
| `--type` | — | `string` | — | Filter by issue type (task\|bug\|feature\|epic\|...) |

### loom data monitor

Show monitor dashboard from a loom server (HTTP, single-shot)

**Usage:** `loom data monitor [flags]`

```
Fetch /api/monitor/status from the configured loom server and render
a simplified dashboard. This command is single-shot — it does not live-refresh
or draw terminal boxes.
```

### loom data ready

List issues that are ready to work on (HTTP)

**Usage:** `loom data ready [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--assignee` | — | `string` | — | Filter by assignee |
| `--limit` | — | `int` | `0` | Maximum number of results (0 = server default) |
| `--parent` | — | `string` | — | Filter by parent issue ID |
| `--type` | — | `string` | — | Filter by issue type |

### loom data show

Show a single issue by ID (HTTP)

**Usage:** `loom data show <issue-id> [flags]`

### loom data update

Update issue fields

**Usage:** `loom data update <issue-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--assignee` | — | `string` | — | Set assignee |
| `--depends-on` | — | `stringArray` | `[]` | Add dependency on issue ID (repeatable) |
| `--description` | — | `string` | — | Set description |
| `--description-from-file` | — | `string` | — | Read description from file (use - for stdin) |
| `--design` | — | `string` | — | Set design |
| `--design-format` | — | `string` | — | Set design format (markdown or html) |
| `--notes` | — | `string` | — | Set notes |
| `--priority` | — | `int` | `0` | Set priority |
| `--remove-depends-on` | — | `stringArray` | `[]` | Remove dependency on issue ID (repeatable) |
| `--status` | — | `string` | — | Set status |
| `--title` | — | `string` | — | Set title |

### loom doctor

Check loom installation and configuration health

**Usage:** `loom doctor [flags]`

```
Diagnose the health of your loom installation, configuration, and runtime.

Runs a series of checks covering prerequisite tools, daemon status,
configuration validity, worktree state, and connectivity. Reports
actionable pass/warn/fail results.

Examples:
  loom doctor              # Human-readable health report
  loom doctor --json       # Machine-readable JSON output
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--fix` | — | `bool` | `false` | Automatically fix issues where possible |
| `--json` | — | `bool` | `false` | Output in JSON format |

### loom driver

Register and run dynamic Loom drivers

**Usage:** `loom driver [flags]`

### loom driver active-task-runs

**Hidden command** — not shown in `loom --help`.

List active child task runs for a driver runtime

**Usage:** `loom driver active-task-runs [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--epic-id` | — | `string` | — | Epic ID metadata (default: parent DriverRun epic) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--limit` | — | `int` | `100` | Maximum active task runs to return |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver agent-orchestration-session

**Hidden command** — not shown in `loom --help`.

Resolve the active orchestration session for an agent

**Usage:** `loom driver agent-orchestration-session [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Agent name |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver claim-ready

**Hidden command** — not shown in `loom --help`.

Claim one ready task for a driver runtime

**Usage:** `loom driver claim-ready [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--actor` | — | `string` | — | Claim actor (default: driver-run:<driver-run-id>) |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--epic-id` | — | `string` | — | Epic ID to scope ready tasks (default: parent DriverRun epic) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--limit` | — | `int` | `100` | Maximum ready tasks to inspect |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver complete-task

**Hidden command** — not shown in `loom --help`.

Complete a claimed FleetDB task for a driver runtime

**Usage:** `loom driver complete-task [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--artifact-id` | — | `stringArray` | `[]` | Required artifact ID; may be repeated |
| `--artifacts-ref` | — | `string` | — | Artifact bundle/ref for CompleteRun |
| `--completion-id` | — | `string` | — | Completion idempotency key (default: complete-<task-run-id>) |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--lease-token` | — | `string` | — | TaskRun lease token (default: LOOM_TASK_RUN_LEASE_TOKEN or LOOM_RUNNER_LEASE_TOKEN) |
| `--logs-ref` | — | `string` | — | Logs artifact/ref for CompleteRun |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--reason` | — | `string` | — | Completion reason |
| `--task-id` | — | `string` | — | FleetDB task ID |
| `--task-run-id` | — | `string` | — | TaskRun ID to complete through FleetDB CompleteRun |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver deliver-agent-message

**Hidden command** — not shown in `loom --help`.

Queue or deliver a workflow message to an agent runtime

**Usage:** `loom driver deliver-agent-message [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Agent name |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--message` | — | `string` | — | Message text to deliver |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver deliver-lead-assignment

**Hidden command** — not shown in `loom --help`.

Deliver the current backend epic assignment to a lead runtime

**Usage:** `loom driver deliver-lead-assignment [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Lead agent name |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver epic-get

**Hidden command** — not shown in `loom --help`.

Read epic issue detail for a driver runtime

**Usage:** `loom driver epic-get [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--epic-id` | — | `string` | — | Epic ID to read (default: parent DriverRun epic) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver epic-snapshot

**Hidden command** — not shown in `loom --help`.

Snapshot epic child task state for a driver runtime

**Usage:** `loom driver epic-snapshot [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--epic-id` | — | `string` | — | Epic ID to snapshot (default: parent DriverRun epic) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver exec-task

**Hidden command** — not shown in `loom --help`.

Execute one child task run for a driver runtime

**Usage:** `loom driver exec-task [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--capability` | — | `stringSlice` | `[]` | Runner capability (repeatable) |
| `--defer-completion` | — | `bool` | `false` | Return execution result but leave successful TaskRun running for CompleteRun |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--driver-step-id` | — | `string` | — | DriverStep ID (default: LOOM_DRIVER_STEP_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Executor node ID |
| `--parent-session-id` | — | `string` | — | Parent AgentSession ID |
| `--provider-profile` | — | `string` | — | Provider profile requested by the driver |
| `--runner-id` | — | `string` | — | Runner ID |
| `--sandbox-cwd` | — | `string` | — | Sandbox working directory |
| `--sandbox-id` | — | `string` | — | Sandbox ID |
| `--sandbox-provider` | — | `string` | — | Sandbox provider |
| `--sandbox-repo-ref` | — | `string` | — | Sandbox repository ref |
| `--supported-provider` | — | `stringSlice` | `[]` | Supported task provider (repeatable) |
| `--task-id` | — | `string` | — | FleetDB task ID |
| `--task-run-id` | — | `string` | — | TaskRun ID (default: generated) |
| `--worker-profile-id` | — | `string` | — | Worker profile ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver list-agents

**Hidden command** — not shown in `loom --help`.

List agents for a driver runtime

**Usage:** `loom driver list-agents [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver recover-stale-tasks

**Hidden command** — not shown in `loom --help`.

Fail stale child task runs and release their task claims

**Usage:** `loom driver recover-stale-tasks [driver-run-id] [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID or positional argument) |
| `--error-class` | — | `string` | `stale_task_run` | Error class recorded on recovered task runs |
| `--error-message` | — | `string` | `task run heartbeat is stale` | Error message recorded on recovered task runs |
| `--json` | — | `bool` | `false` | JSON output |
| `--max-age-seconds` | — | `int64` | `300` | Recover task runs last observed more than this many seconds ago |
| `--stale-before` | — | `string` | — | Recover task runs last observed before this RFC3339 timestamp |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver register

Register a built native Flue driver artifact as an immutable DriverVersion

**Usage:** `loom driver register [flags]`

```
Register a built native Flue driver artifact as an immutable DriverVersion.

The artifact must already be built by Flue, for example:

  flue build --target node
  loom driver register --flue-dist ./dist --name epic-runner --activate

Loom stages the built dist directory and records the DriverVersion. It does not
generate a Flue project or adapter source.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--activate` | — | `bool` | `false` | Activate the registered version after validation |
| `--flue-dist` | — | `string` | — | Built Flue dist directory containing server.mjs |
| `--id` | — | `string` | — | Driver ID (default: slug of --name or manifest driver_name) |
| `--json` | — | `bool` | `false` | JSON output |
| `--manifest` | — | `string` | — | Optional native Flue driver manifest JSON (default: <flue-dist>/loom-driver.json if present) |
| `--name` | — | `string` | — | Driver name |
| `--source-digest` | — | `string` | — | Optional source digest recorded on the DriverVersion |
| `--source-ref` | — | `string` | — | Optional source/provenance ref recorded on the DriverVersion |
| `--trusted` | — | `bool` | `false` | Register as operator-trusted for local process execution |
| `--untrusted` | — | `bool` | `false` | Register as untrusted (default); requires an isolating launcher unless later approved |
| `--workflow` | — | `string` | — | Flue workflow name (default: driver ID or manifest workflow_name) |

### loom driver release-task

**Hidden command** — not shown in `loom --help`.

Release a claimed FleetDB task for a driver runtime

**Usage:** `loom driver release-task [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--actor` | — | `string` | — | Release actor (default: driver-run:<driver-run-id>) |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--task-id` | — | `string` | — | FleetDB task ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver run

Record a queued DriverRun for a published driver

**Usage:** `loom driver run <driver_id> [flags]`

```
Record a queued DriverRun for a published driver.

The run is pinned to the driver's active DriverVersion. Driver execution is
handled by a later runtime/executor slice; this command records durable work
without claiming or running it synchronously.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--entrypoint` | — | `string` | `run` | Driver entrypoint |
| `--epic` | — | `string` | — | Epic ID to pass as input.epicId |
| `--idempotency-key` | — | `string` | — | DriverRun admission idempotency key |
| `--input` | — | `stringArray` | `[]` | Input key=value (repeatable) |
| `--json` | — | `bool` | `false` | JSON output |
| `--run-id` | — | `string` | — | Run ID (default: generated) |

### loom driver update-agent-parent

**Hidden command** — not shown in `loom --help`.

Update an agent parent for a driver runtime

**Usage:** `loom driver update-agent-parent [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Agent name |
| `--driver-run-id` | — | `string` | — | Parent DriverRun ID (default: LOOM_DRIVER_RUN_ID) |
| `--expect-parent` | — | `string` | — | Expected current parent before update |
| `--fencing-token` | — | `int64` | `0` | Parent DriverRun fencing token |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | Parent DriverRun lease ID |
| `--node-id` | — | `string` | — | Parent DriverRun node ID |
| `--parent` | — | `string` | — | New parent epic ID |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_DRIVER_WORKSPACE or active workspace) |

### loom driver work-task-run

**Hidden command** — not shown in `loom --help`.

Claim and execute one queued task run for a worker runtime

**Usage:** `loom driver work-task-run [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--capability` | — | `stringSlice` | `[]` | Worker capability (repeatable) |
| `--defer-completion` | — | `bool` | `false` | Return execution result but leave successful TaskRun running for CompleteRun |
| `--json` | — | `bool` | `false` | JSON output |
| `--lease-id` | — | `string` | — | TaskRun lease ID (default: generated per claim) |
| `--lease-token` | — | `string` | — | TaskRun lease token (default: generated per claim) |
| `--node-id` | — | `string` | — | Worker node ID (default: LOOM_WORKER_NODE_ID, LOOM_DRIVER_NODE_ID, or host name) |
| `--runner-id` | — | `string` | — | Worker runner ID (default: LOOM_WORKER_RUNNER_ID or LOOM_DRIVER_RUNNER_ID) |
| `--runner-process-ref` | — | `string` | — | Runner process reference |
| `--runner-provider` | — | `string` | — | Runner placement provider |
| `--sandbox-cwd` | — | `string` | — | Sandbox working directory |
| `--sandbox-id` | — | `string` | — | Sandbox ID |
| `--sandbox-image` | — | `string` | — | Sandbox image or snapshot |
| `--sandbox-provider` | — | `string` | — | Sandbox provider |
| `--sandbox-repo-ref` | — | `string` | — | Sandbox repository ref |
| `--supported-provider` | — | `stringSlice` | `[]` | Supported task provider (repeatable) |
| `--task-run-id` | — | `string` | — | Specific queued TaskRun ID to claim |
| `--worker-profile-id` | — | `stringSlice` | `[]` | Worker profile ID this worker may claim (repeatable) |
| `--workspace-key` | — | `string` | — | Workspace key (default: LOOM_WORKER_WORKSPACE, LOOM_DRIVER_WORKSPACE, or active workspace) |

### loom epic

Manage epic-scoped work

**Usage:** `loom epic [flags]`

```
Group commands for working with an epic and its child tasks.

Today this is a single subcommand:
  loom epic run --parent <epic-id>   drain the epic by running the epic-runner workflow
```

### loom epic run

Drain an epic by running the epic-runner workflow

**Usage:** `loom epic run [flags]`

```
Queue or run the epic-runner workflow for an epic.

The command records a durable DriverRun for a workflow. Lead assignment,
preflight, and child-task orchestration are handled by the workflow itself. By
default the built-in epic-runner workflow is used; pass --workflow to run
another registered workflow with the same payload shape.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--base-branch` | — | `string` | — | Base branch passed to compatible task runners |
| `--detach` | — | `bool` | `false` | Queue the workflow run and return without executing it in this process |
| `--dry-run` | — | `bool` | `false` | Print what would be spawned but don't actually create agents |
| `--interval-seconds` | — | `int` | `5` | Seconds between reconcile passes |
| `--lead` | — | `string` | — | Lead agent running this epic (default: $LOOM_AGENT_NAME when set) |
| `--max-concurrency` | — | `int` | `2` | Maximum simultaneous workers spawned by this run |
| `--node-id` | — | `string` | — | Daemon node ID to run spawned workers on (default: the single active local node) |
| `--open-pull-request` | — | `bool` | `false` | Ask compatible task runners to open pull requests |
| `--parent` | — | `string` | — | Epic ID to drain (required) |
| `--repo-url` | — | `string` | — | Repository URL passed to compatible task runners |
| `--runner` | — | `string` | `local-task-runner` | Runner requested for child task runs |
| `--stacked-pull-requests` | — | `bool` | `false` | Ask compatible task runners to stack child pull requests |
| `--worker-prefix` | — | `string` | — | Optional worker profile prefix for spawned workers |
| `--workflow` | — | `string` | `epic-runner` | Workflow name to run |

### loom help

Help about any command

**Usage:** `loom help [command] [flags]`

```
Help provides help for any command in the application.
Simply type loom help [path to command] for full details.
```

### loom hooks

**Hidden command** — not shown in `loom --help`.

Manage agent lifecycle hooks

**Usage:** `loom hooks [flags]`

### loom hooks claude-code

**Hidden command** — not shown in `loom --help`.

Claude Code hook handlers

**Usage:** `loom hooks claude-code [flags]`

### loom hooks claude-code post-task

**Hidden command** — not shown in `loom --help`.

Handle Claude Code PostToolUse[Task] hook

**Usage:** `loom hooks claude-code post-task [flags]`

### loom hooks claude-code pre-task

**Hidden command** — not shown in `loom --help`.

Handle Claude Code PreToolUse[Task] hook

**Usage:** `loom hooks claude-code pre-task [flags]`

### loom hooks claude-code session-end

**Hidden command** — not shown in `loom --help`.

Handle Claude Code SessionEnd hook

**Usage:** `loom hooks claude-code session-end [flags]`

### loom hooks claude-code session-start

**Hidden command** — not shown in `loom --help`.

Handle Claude Code SessionStart hook

**Usage:** `loom hooks claude-code session-start [flags]`

### loom hooks claude-code stop

**Hidden command** — not shown in `loom --help`.

Handle Claude Code Stop hook

**Usage:** `loom hooks claude-code stop [flags]`

### loom hooks claude-code user-prompt-submit

**Hidden command** — not shown in `loom --help`.

Handle Claude Code UserPromptSubmit hook

**Usage:** `loom hooks claude-code user-prompt-submit [flags]`

### loom hooks claude-code yield-guard

**Hidden command** — not shown in `loom --help`.

Check yield file and block tools if yield requested

**Usage:** `loom hooks claude-code yield-guard [flags]`

### loom hooks dispatch

**Hidden command** — not shown in `loom --help`.

Dispatch a harness lifecycle hook to harness-wrapper

**Usage:** `loom hooks dispatch <harness> <event> [flags]`

### loom hooks install

Install loom hooks into Claude Code settings

**Usage:** `loom hooks install [worktree-path] [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | — | `bool` | `false` | Install hooks even when fleet mode is active |

### loom hooks status

Show loom hooks installation status

**Usage:** `loom hooks status [worktree-path] [flags]`

### loom hooks uninstall

Remove loom hooks from Claude Code settings

**Usage:** `loom hooks uninstall [worktree-path] [flags]`

### loom init

Initialize loom with guided setup

**Usage:** `loom init [flags]`

```
Initialize loom with a guided, interactive setup experience.

This command helps new users set up loom agents:
  - Checks prerequisites (git, fleet-db local mode)
  - Confirms fleet-db issue storage
  - Validates workspace setup

Flags:
  -y, --yes              Non-interactive mode with defaults
  --worktrees-dir DIR    Override worktrees directory
  --names NAMES          Comma-separated worktree names
  --workspace NAME       Initialize within an existing workspace

Examples:
  loom init                           # Interactive setup
  loom init --yes                     # Non-interactive with defaults
  loom init --workspace myws          # Workspace-aware setup
  loom init --workspace myws --yes    # Non-interactive workspace setup
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--names` | — | `string` | — | Comma-separated worktree names for non-interactive mode |
| `--workspace` | — | `string` | — | Workspace name for workspace-aware setup |
| `--worktrees-dir` | — | `string` | — | Worktrees directory (default: from LOOM_WORKTREES_DIR or 'worktrees') |
| `--yes` | `-y` | `bool` | `false` | Non-interactive mode with defaults |

### loom install-service

Generate or install a platform-native service definition for loom serve

**Usage:** `loom install-service [flags]`

```
Generate and optionally install a service definition to run loom serve
as a persistent background service.

On Linux, generates a systemd user unit file.
On macOS, generates a launchd user agent plist.

By default, prints the service definition to stdout for inspection.
Use --install to write it to the platform-specific location and enable it.
Use --uninstall to stop and remove a previously installed service.

The service runs loom serve with the specified flags and restarts on failure.
The working directory is set to the current directory.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--bind` | — | `string` | `127.0.0.1` | Bind address for loom serve |
| `--env` | — | `stringArray` | `[]` | Extra environment variables (KEY=VALUE, repeatable) |
| `--install` | — | `bool` | `false` | Write and enable the service (instead of printing to stdout) |
| `--name` | — | `string` | — | Service name suffix for multi-project setups (e.g. my-project) |
| `--port` | — | `int` | `8080` | Port for loom serve |
| `--uninstall` | — | `bool` | `false` | Stop and remove an installed service |

### loom lead

Run the interactive terminal-agent runtime

**Usage:** `loom lead [flags]`

```
Launch an interactive terminal-agent session.

Unlike 'plan' and 'task' (which are autonomous worker agents), 'lead' is the
interactive terminal-agent runtime. The default persona is lead/project
management mode, where the AI agent helps you:
  - Review and approve/reject plans from planning agents
  - Create new tickets (tasks, bugs, features, epics)
  - Triage and prioritize the backlog
  - Manage dependencies between tickets

Pass --prompt to replace the default lead prompt with a role prompt_file while
keeping terminal-agent guardrails and orchestration behavior.

This command does not require a worktree - it can run from the main
repository or any worktree.

Use --message to seed the session with an initial user request. The message
is appended to the lead system prompt, so the agent performs its normal
lead-mode startup and then addresses the request using lead-mode conventions.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--message` | — | `string` | — | Initial user request to address in lead mode |
| `--prompt` | — | `string` | — | Path to terminal-agent prompt template |

### loom list

List all agents (worktrees)

**Usage:** `loom list [flags]`

**Aliases:** `ls`

```
List all available agents (worktrees) and their status.

Shows:
  - Worktree name
  - Current branch
  - Status: running agent, dirty working tree, or clean

Examples:
  loom list                     # List all agents
  loom ls                       # Short alias
```

### loom local

Manage the local desktop runtime

**Usage:** `loom local [flags]`

```
Manage the local desktop runtime used by Loom.app.

The local runtime stores state under the app data directory, starts a local
Loom web/API server, and writes runtime metadata that the desktop shell can
read. On macOS the app should run 'loom local service' through a per-user
LaunchAgent.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--data-dir` | — | `string` | — | Local runtime data directory (default: macOS app data dir) |

### loom local drain

Mark the local runtime as draining

**Usage:** `loom local drain [flags]`

### loom local install-service

Install the persistent local runtime service

**Usage:** `loom local install-service [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--port` | — | `int` | `0` | Local web/API port (0 picks a free port) |

### loom local logs

Print local runtime log paths

**Usage:** `loom local logs [flags]`

### loom local restart

Restart the local runtime service

**Usage:** `loom local restart [flags]`

### loom local resume

Resume local runtime task claims

**Usage:** `loom local resume [flags]`

### loom local service

Run the local runtime service in the foreground

**Usage:** `loom local service [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--bind` | — | `string` | `127.0.0.1` | Local web/API bind address |
| `--port` | — | `int` | `0` | Local web/API port (0 picks a free port) |

### loom local start

Start the local runtime service in the background

**Usage:** `loom local start [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--port` | — | `int` | `0` | Local web/API port (0 picks a free port) |

### loom local status

Show local runtime status

**Usage:** `loom local status [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | Print JSON status |

### loom local stop

Stop the local runtime service

**Usage:** `loom local stop [flags]`

### loom local uninstall-service

Uninstall the persistent local runtime service

**Usage:** `loom local uninstall-service [flags]`

### loom log-router

**Hidden command** — not shown in `loom --help`.

Route stdin to agent and task log files (internal)

**Usage:** `loom log-router [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Agent name for naming the agent log file (required) |
| `--base-dir` | — | `string` | — | Base directory for log files (default: ~/.loom/logs) |
| `--lock-path` | — | `string` | — | Path to the .agent.lock file to watch for TaskID changes (required) |
| `--max-log-size` | — | `int` | `50` | Maximum log file size in MB before rotation (0 to disable) |
| `--workspace-id` | — | `string` | — | Workspace ID for log directory namespacing |

### loom monitor

Display comprehensive agent and task dashboard

**Usage:** `loom monitor [flags]`

**Aliases:** `mon`, `status`

```
Display a dashboard showing agents, tasks, sync status, and statistics.

Sections:
  AGENTS     - Worktree status (running/idle, branch, dirty/clean)
  TASKS      - Ready, in_progress, need review, backlog counts
  SYNC       - Database and git sync status
  STATS      - Overall issue counts and completion rate

Flags:
  -b, --branch      Integration branch to compare against
  -n, --no-watch    One-shot mode (disable auto-refresh)
  -i, --interval    Refresh interval in seconds (default: 5)

Examples:
  loom monitor              # Auto-refresh (default)
  loom monitor -n           # One-shot display
  loom monitor -i 10        # Refresh every 10 seconds
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--branch` | `-b` | `string` | — | Integration branch to compare against (default: LOOM_DEFAULT_BRANCH or main) |
| `--interval` | `-i` | `int` | `5` | Refresh interval in seconds |
| `--no-watch` | `-n` | `bool` | `false` | Disable auto-refresh (one-shot mode) |

### loom plan

Run a planning agent

**Usage:** `loom plan [worktree|workspace] [flags]`

```
Run a planning agent in the specified worktree or workspace.

The planning agent will:
  1. Pick the highest priority task (or one with needs-revision label)
  2. Research the codebase and create a detailed plan
  3. Save the plan to the task's --design field
  4. Set status to 'review' for human approval
  5. Exit after completing ONE task (unless --auto is enabled)

Arguments:
  worktree    Worktree/workspace name (e.g., falcon) or path
              In workspace mode, workspace names take priority over repo names.
              If omitted, runs in current directory (or workspace root in workspace mode).

Flags:
  -a, --auto          Enable continuous mode (process multiple tasks)
  -i, --interval      Polling interval in seconds when no tasks (default: 30)
  -m, --max-tasks     Maximum tasks to process before exiting (0 = unlimited)
  -t, --idle-timeout  Exit after N minutes with no available tasks (0 = none)

Examples:
  loom plan falcon              # Run in falcon worktree/workspace (single task)
  loom plan                     # Run in current directory
  loom plan falcon --auto       # Continuous mode until Ctrl+C
  loom plan falcon -a -m 5      # Process up to 5 tasks
  loom plan falcon -a -t 30     # Exit after 30 min idle
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--auto` | `-a` | `bool` | `false` | Enable continuous mode (process multiple tasks) |
| `--daemon-mode` _(hidden)_ | — | `bool` | `false` | Internal: single task mode for daemon |
| `--idle-timeout` | `-t` | `int` | `0` | Exit after N minutes with no tasks (0 = none) |
| `--interval` | `-i` | `int` | `30` | Polling interval in seconds when no tasks available |
| `--max-tasks` | `-m` | `int` | `0` | Maximum tasks to process (0 = unlimited) |
| `--parent` | — | `string` | — | Filter tasks to descendants of this epic ID |

### loom pr

Create GitHub PR from worktree branch

**Usage:** `loom pr [worktree] [target] [flags]`

```
Create a GitHub pull request from a worktree branch to a target branch.

Unlike 'loom push' which directly merges, 'loom pr' creates a PR for code
review before merging. Uses 'gh pr create' under the hood.

Arguments:
  worktree    Source worktree to create PR from (e.g., falcon)
  target      Target branch for the PR (default: main or per-repo default)

Flags:
  -a, --all          Create PRs for all worktree branches
  -W, --workspace    Workspace to operate on

Examples:
  loom pr falcon                        # Create PR from falcon to main
  loom pr falcon develop                # Create PR from falcon to develop
  loom pr --all                         # Create PRs for all worktrees
  loom pr --all main                    # Create PRs for all worktrees to main
  loom pr -W myworkspace falcon         # Create PR in specific workspace
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--all` | `-a` | `bool` | `false` | Create PRs for all worktree branches |
| `--workspace` | `-W` | `string` | — | Workspace to operate on |

### loom pull

Pull branch into worktree

**Usage:** `loom pull [worktree] [branch] [flags]`

```
Pull latest changes from a branch into worktree(s).

Merges the source branch (e.g., main) INTO the worktree branch, updating
the worktree with the latest changes. If conflicts occur, Claude
is launched to resolve them.

Arguments:
  worktree    Worktree name (e.g., falcon)
  branch      Source branch to pull from (default: main or per-repo default)

Flags:
  -a, --all          Pull into all worktrees
  -W, --workspace    Workspace to operate on

Examples:
  loom pull falcon                        # Pull main (or per-repo default) into falcon
  loom pull falcon main                   # Pull main into falcon explicitly
  loom pull --all                         # Pull into all worktrees from their defaults
  loom pull --all main                    # Pull main into all worktrees
  loom pull -W myworkspace falcon         # Pull in specific workspace
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--all` | `-a` | `bool` | `false` | Pull into all worktrees |
| `--workspace` | `-W` | `string` | — | Workspace to operate on |

### loom push

Push worktree branch to target branch

**Usage:** `loom push [worktree] [target] [flags]`

**Aliases:** `merge`

```
Push completed work from worktree branches to target branch.

Merges worktree branch INTO the target branch (e.g., main), pushing
completed work. If conflicts occur, Claude is launched to resolve them.

Arguments:
  worktree    Source worktree to push from (e.g., falcon)
  target      Target branch to push into (default: main or per-repo default)

Flags:
  -a, --all          Push all worktree branches to target
  -W, --workspace    Workspace to operate on

Examples:
  loom push falcon                        # Push falcon to main (or per-repo default)
  loom push falcon main                   # Push falcon to main explicitly
  loom push --all                         # Push all worktrees to their default targets
  loom push --all main                    # Push all worktrees to main
  loom push -W myworkspace falcon         # Push in specific workspace
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--all` | `-a` | `bool` | `false` | Push all worktree branches to target |
| `--workspace` | `-W` | `string` | — | Workspace to operate on |

### loom recover

Recover from agent error state

**Usage:** `loom recover <worktree> [flags]`

```
Recover a worktree from error state by clearing stale locks
and handling orphaned tasks intelligently.

This command will:
  1. Check if the agent process is still running
  2. If not running, clear the stale lock file
  3. Analyze the orphaned task using Claude to determine if it was completed
  4. Close completed tasks, or reset incomplete tasks to open status
  5. Clean up untracked files left by the crashed agent (with confirmation)

In workspace mode, recovery clears the shared workspace-level lock,
searches git logs across all repos for task completion evidence, and
cleans untracked files in all workspace repos.

Use this when 'loom monitor' shows an agent in error state.

Flags:
  --force        Kill running agent and clean untracked files without prompting
  --no-analyze   Skip Claude analysis, always reset task to open status

Examples:
  loom recover falcon              # Recover with task analysis (default)
  loom recover ember --no-analyze  # Skip analysis, always reset to open
  loom recover falcon --force      # Kill running agent and clean files without prompting
  loom recover myworkspace         # Recover workspace-level agent
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | — | `bool` | `false` | Skip all confirmation prompts (kill process, clean files) |
| `--no-analyze` | — | `bool` | `false` | Skip Claude analysis, always reset task to open status |

### loom repo

Manage repos within the active workspace

**Usage:** `loom repo [flags]`

```
Manage repositories tied to the active workspace.

The active workspace is resolved from the --workspace root flag or
LOOM_WORKSPACE env var. Runtime repo commands do not use
~/.loom/state.json's last_workspace.
```

### loom repo add

Register a repo in the active workspace

**Usage:** `loom repo add <NAME> <REMOTE_URL> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--branch` | — | `string` | — | Default branch (default: main) |
| `--groups` | — | `stringSlice` | `[]` | Logical groups (comma-separated or repeat flag) |
| `--remote` | — | `string` | — | Git remote name (default: origin) |
| `--source-repo-id` | — | `string` | — | Source repo ID for issue routing (default: NAME) |

### loom repo list

List repos in the active workspace

**Usage:** `loom repo list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom repo remove

Delete a repo from the active workspace

**Usage:** `loom repo remove <NAME> [flags]`

### loom repo show

Show repo details

**Usage:** `loom repo show <NAME> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom reset

Hard reset worktree to a specific branch

**Usage:** `loom reset <worktree> [branch] [flags]`

```
Hard reset worktree(s) to a specific branch.

WARNING: This discards ALL local changes!

This command will:
  1. Discard all local changes (git reset --hard, git clean -fd)
  2. Reset to the target branch (origin/branch)
  3. Force push only if --push is specified (local-only by default)

In workspace mode, --all resets all repos in the workspace. Each repo
resets to its own configured integration branch (DefaultBranch) unless
a target branch is explicitly provided.

Arguments:
  worktree    Worktree name (e.g., falcon)
  branch      Target branch to reset to (default: integration branch)

Flags:
  -a, --all      Reset all worktrees
  -p, --push     Force-push to remote after resetting locally
  -f, --force    Skip confirmation prompt, override lock protection,
                 and allow force-push to protected branches (main/master)

Safety:
  By default, only local state is reset. The remote branch is NOT updated.
  Use --push to force-push the reset to origin.
  Force-pushing to main/master requires both --push and --force.

Examples:
  loom reset falcon                        # Reset falcon locally (no push)
  loom reset falcon main                   # Reset falcon to main (local only)
  loom reset falcon --push                 # Reset falcon and force-push to origin
  loom reset --all                         # Reset all worktrees locally
  loom reset --all --push                  # Reset all worktrees and force-push
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--all` | `-a` | `bool` | `false` | Reset all worktrees |
| `--force` | `-f` | `bool` | `false` | Skip confirmation and allow force-push to protected branches (main/master) |
| `--push` | `-p` | `bool` | `false` | Force-push to remote after resetting locally |

### loom role

Manage roles within the active workspace

**Usage:** `loom role [flags]`

### loom role add

Create a role definition in the active workspace

**Usage:** `loom role add <NAME> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--backend` | — | `string` | — | AI backend (e.g., claude, codex) |
| `--description` | — | `string` | — | Description |
| `--effort` | — | `string` | — | Agent effort (low, medium, high, xhigh, max) |
| `--kind` | — | `string` | — | Role runtime kind (interactive or worker) |
| `--max-concurrency` | — | `int` | `0` | Max concurrent agents (0 = unlimited) |
| `--model` | — | `string` | — | Model identifier |
| `--prompt` | — | `string` | — | Inline prompt text |
| `--prompt-file` | — | `string` | — | Path to prompt file (relative to workspace) |
| `--read-only` | — | `bool` | `false` | Read-only role |
| `--skills` | — | `stringSlice` | `[]` | Skills (comma-separated or repeat flag) |

### loom role list

List roles in the active workspace

**Usage:** `loom role list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom role remove

Delete a role from the active workspace

**Usage:** `loom role remove <NAME> [flags]`

### loom role set

Set a single field on an existing role

**Usage:** `loom role set <NAME> <KEY> <VALUE> [flags]`

```
Set a role field by key. Supported keys:
  description     string
  kind            string (interactive/worker)
  prompt          inline prompt text
  prompt_file     string
  model           string
  task_filter     string
  backend         string
  effort          string (low/medium/high/xhigh/max)
  read_only       bool ("true"/"false")
  max_priority    integer
  max_concurrency integer
  max_budget_usd  float
  skills          comma-separated list
  path_patterns   comma-separated list
  allowed_tools   comma-separated list
  denied_tools    comma-separated list
```

### loom role show

Show role details

**Usage:** `loom role show <NAME> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom role unset

Clear a clearable role field back to its default

**Usage:** `loom role unset <NAME> <KEY> [flags]`

```
Revert a role field to nil/empty. Supported keys:
  max_priority    *int     (clear)
  max_concurrency *int     (clear)
  max_budget_usd  *float64 (clear)
  description / kind / prompt / prompt_file / model / task_filter / backend / effort  (set to "")
  skills / path_patterns / allowed_tools / denied_tools      (set to empty list)
  read_only                                                 (set to false)
```

### loom serve

Start HTTP server for agent status API

**Usage:** `loom serve [flags]`

```
Start a pure JSON API / SSE / WebSocket server.

The frontend is served externally (reverse proxy, CDN, Vite preview, etc.).
Use --frontend-url (repeatable) to allow cross-origin frontend deployments
via CORS.

Starts/opens fleet-db for issue and workspace data unless --no-daemon is set.

ENVIRONMENT VARIABLES
  LOOM_SERVER_PORT     Server port (default: 8080)
  LOOM_BIND_ADDR       Bind address (default: 127.0.0.1)
  LOOM_CORS_ORIGIN     CORS allowed origin
  LOOM_FRONTEND_URL    Allowed frontend origin(s) for CORS (comma-separated)
  LOOM_REDIS_PASSWORD  Redis password (avoids exposure in process list)
  LOOM_AUTH_URL         External auth service base URL (enables JWT auth)
  LOOM_AUTH_ISSUER      Expected JWT issuer (defaults to LOOM_AUTH_URL)
  LOOM_AUTH_AUDIENCE    Expected JWT audience (defaults to "loom")
  LOOM_DRIVER_EXECUTOR  DriverRun executor toggle (default: on; set 0/false/off/no to disable)
  LOOM_DRIVER_TASK_WORKER_CONCURRENCY  Local TaskRun worker loops (default: 2)
  LOOM_DRIVER_TASK_RUN_MAX_ATTEMPTS     TaskRun attempts before blocking failed (default: 2)
  LOOM_TRIGGER_CRON_INTERVAL            Cron trigger sweep interval in seconds (default: 30)
  LOOM_ISSUE_BRIDGE_INTERVAL            Issue-journal bridge poll interval in seconds (default: 2)
  LOOM_ISSUE_BRIDGE_DISABLED            Disable the issue-journal bridge loop (set 1/true)
  LOOM_ISSUE_BRIDGE_STATE_PATH          Bridge cursor state file (default: <state dir>/issue-bridge-cursor.json)
  LOOM_ISSUE_BRIDGE_REPLAY              Replay journal from zero on first observation (set 1/true)

EXAMPLES
  loom serve                                              # Default port 8080
  loom serve --bind 0.0.0.0 --auth-url https://auth.co   # Exposed with JWT auth
  loom serve --frontend-url https://app.example.com       # Cross-origin frontend
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--auth-allow-insecure` | — | `bool` | `false` | Allow HTTP for non-loopback --auth-url (INSECURE, for Docker internal networks only) |
| `--auth-audience` | — | `string` | — | Expected JWT audience (defaults to "loom") |
| `--auth-issuer` | — | `string` | — | Expected JWT issuer (defaults to --auth-url) |
| `--auth-url` | — | `string` | — | External auth service base URL (enables JWT auth) |
| `--bind` | — | `string` | `127.0.0.1` | Bind address (use 0.0.0.0 for all interfaces) |
| `--cors` | — | `string` | — | CORS allowed origin |
| `--fleet-api-key` | — | `string` | — | API key for fleet worker registration (required for fleet register endpoint) |
| `--fleet-mode` | — | `bool` | `false` | Enable fleet coordination features (stale detector, task claims, fleet routes). Default off for local dev. Env: LOOM_FLEET_MODE |
| `--frontend-dir` | — | `string` | — | Built web UI directory to serve for non-API routes. Env: LOOM_FRONTEND_DIR |
| `--frontend-url` | — | `stringSlice` | `[]` | Allowed frontend origin(s) for CORS. Repeatable or comma-separated. Env: LOOM_FRONTEND_URL |
| `--hsts` | — | `bool` | `false` | Enable HSTS header (use when behind TLS-terminating proxy) |
| `--no-daemon` | — | `bool` | `false` | Skip issue backend startup |
| `--port` | `-p` | `int` | `8080` | Server port |
| `--redis-addr` | — | `string` | — | Redis address for fleet coordination (enables stale detector) |
| `--redis-password` | — | `string` | — | Redis password (prefer LOOM_REDIS_PASSWORD env var to avoid leaking in process list) |
| `--sentry-dsn` | — | `string` | — | Sentry/GlitchTip DSN for error tracking (or LOOM_SENTRY_DSN) |
| `--webui-socket` | — | `string` | — | Daemon socket path for webui (auto-detect if empty) |

### loom sessions

Manage agent sessions

**Usage:** `loom sessions [flags]`

### loom sessions clean

Remove old session data

**Usage:** `loom sessions clean [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--older-than` | — | `string` | `30d` | Remove sessions older than this duration (e.g. 30d, 720h) |

### loom stack

Manage stack lineage and publish stacked pull requests

**Usage:** `loom stack [flags]`

### loom stack add

Register a task in a stack (appends to the tip; --after to chain, --root for a parallel chain)

**Usage:** `loom stack add <task-id> --stack <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--after` | — | `string` | — | predecessor task id (default: current tip) |
| `--commit-mode` | — | `string` | — | commit mode override |
| `--json` | — | `bool` | `false` | JSON output |
| `--root` | — | `bool` | `false` | start a new parallel chain off the stack base |
| `--stack` | — | `string` | — | stack id (required) |

### loom stack init

Create a stack (e.g. epic:EPIC-1) rooted on a base branch

**Usage:** `loom stack init <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--base` | — | `string` | — | root base branch (e.g. main) |
| `--commit-mode` | — | `string` | — | default commit mode: loom_commit\|agent_commit\|squash_on_publish |
| `--json` | — | `bool` | `false` | JSON output |
| `--repo` | — | `string` | — | workspace repo name |

### loom stack list

List stacks in the active workspace

**Usage:** `loom stack list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom stack move

Reorder a unit to sit after another unit

**Usage:** `loom stack move <task-id> --stack <stack-id> --after <task-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--after` | — | `string` | — | task id to move after (required) |
| `--stack` | — | `string` | — | stack id (required) |

### loom stack publish

Publish the stack as stacked pull requests

**Usage:** `loom stack publish <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--auto-rebase` | — | `bool` | `false` | rebase descendants of merged units onto the live base (agent resolves conflicts) instead of failing closed |
| `--dry-run` | — | `bool` | `false` | compute the plan without mutating GitHub |
| `--headless` | — | `bool` | `false` | with --auto-rebase, resolve conflicts headlessly (non-interactive) |
| `--json` | — | `bool` | `false` | JSON output |
| `--repo-path` | — | `string` | — | local checkout to push from (default: from loom state) |

### loom stack remove

Remove a unit (children reparent onto its predecessor)

**Usage:** `loom stack remove <task-id> --stack <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--stack` | — | `string` | — | stack id (required) |

### loom stack restack

Rebase descendants of merged units onto the live base, resolving conflicts with an agent

**Usage:** `loom stack restack <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--headless` | — | `bool` | `false` | resolve conflicts with a non-interactive (headless) agent |
| `--json` | — | `bool` | `false` | JSON output |
| `--repo-path` | — | `string` | — | local checkout to rebase in (default: from loom state) |

### loom stack set-base

Set a unit's predecessor ("" for root)

**Usage:** `loom stack set-base <task-id> --stack <stack-id> --base-task <task-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--base-task` | — | `string` | — | predecessor task id (empty = root) |
| `--stack` | — | `string` | — | stack id (required) |

### loom stack show

Show a stack and its ordered units

**Usage:** `loom stack show <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom stack status

Show each unit's PR, state, and live health (checks/review/mergeable)

**Usage:** `loom stack status <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--repo-path` | — | `string` | — | local checkout (enables live PR health) |

### loom stack validate

Check the stack's lineage is linear and acyclic

**Usage:** `loom stack validate <stack-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom status

Show system overview

**Usage:** `loom status [flags]`

```
Display a one-shot snapshot of the entire loom system.

Shows daemon health, backend config, worktree/agent state, task counts,
git sync status, Redis connectivity, and any detected issues.

Unlike 'loom monitor' (auto-refreshing dashboard), 'loom status' is a
static snapshot optimized for scripting and quick glances.

Examples:
  loom status              # Human-readable overview
  loom status --json       # Machine-readable JSON output
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--branch` | `-b` | `string` | — | Integration branch to compare against (default: LOOM_DEFAULT_BRANCH or main) |
| `--json` | — | `bool` | `false` | Output in JSON format |

### loom sync

Push completed work then pull latest into all worktrees

**Usage:** `loom sync [flags]`

```
Sync performs a full push + pull cycle for all worktrees.

This command:
1. Finds all worktrees with completed work (ready to push)
2. Pushes each to main (or per-repo default)
3. Pulls main into all worktrees

This is the recommended way to keep worktrees in sync with the main branch.

Flags:
  --push-only        Only push (skip pulling)
  --pull-only        Only pull (skip pushing)
  -W, --workspace    Workspace to operate on

Examples:
  loom sync                      # Full sync: push all ready + pull all
  loom sync --push-only          # Only push completed work
  loom sync --pull-only          # Only pull latest (same as pull --all)
  loom sync -W myworkspace       # Sync specific workspace
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--pull-only` | — | `bool` | `false` | Only pull (skip pushing) |
| `--push-only` | — | `bool` | `false` | Only push (skip pulling) |
| `--workspace` | `-W` | `string` | — | Workspace to operate on |

### loom task

Run an implementation agent

**Usage:** `loom task [worktree|workspace] [flags]`

```
Run an implementation agent in the specified worktree or workspace.

The implementation agent will:
  1. Pick the highest priority ready task (skipping tasks needing revision)
  2. Follow the --design plan if present, otherwise create a local plan
  3. Implement, test, and review the code
  4. Commit and push changes
  5. Close the task and exit after completing ONE task (unless --auto is enabled)

Arguments:
  worktree    Worktree/workspace name (e.g., falcon) or path
              In workspace mode, workspace names take priority over repo names.
              If omitted, runs in current directory (or workspace root in workspace mode).

Flags:
  -a, --auto          Enable continuous mode (process multiple tasks)
  -i, --interval      Polling interval in seconds when no tasks (default: 30)
  -m, --max-tasks     Maximum tasks to process before exiting (0 = unlimited)
  -t, --idle-timeout  Exit after N minutes with no available tasks (0 = none)

Examples:
  loom task falcon              # Run in falcon worktree/workspace (single task)
  loom task                     # Run in current directory
  loom task falcon --auto       # Continuous mode until Ctrl+C
  loom task falcon -a -m 5      # Process up to 5 tasks
  loom task falcon -a -t 30     # Exit after 30 min idle
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--auto` | `-a` | `bool` | `false` | Enable continuous mode (process multiple tasks) |
| `--daemon-mode` _(hidden)_ | — | `bool` | `false` | Internal: single task mode for daemon |
| `--idle-timeout` | `-t` | `int` | `0` | Exit after N minutes with no tasks (0 = none) |
| `--interval` | `-i` | `int` | `30` | Polling interval in seconds when no tasks available |
| `--max-tasks` | `-m` | `int` | `0` | Maximum tasks to process (0 = unlimited) |
| `--parent` | — | `string` | — | Filter tasks to descendants of this epic ID |

### loom trigger

Manage trigger bindings and inspect trigger events/deliveries

**Usage:** `loom trigger [flags]`

### loom trigger bindings

Manage trigger bindings

**Usage:** `loom trigger bindings [flags]`

### loom trigger bindings create

Create a trigger binding that maps a route key to a pinned driver version

**Usage:** `loom trigger bindings create [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--actor-filter-exclude` | — | `stringArray` | `[]` | actor kind(s) to exclude (repeatable); on update, pass a single empty value to clear the filter |
| `--binding-id` | — | `string` | — | binding id (default: derived from route key) |
| `--concurrency-policy` | — | `string` | — | concurrency policy: allow\|forbid\|replace\|queue\|one_active_per_epic |
| `--disabled` | — | `bool` | `false` | create the binding disabled |
| `--driver` | — | `string` | — | driver id or name (alternative to --workflow) |
| `--driver-version` | — | `string` | — | pin a specific driver version id (default: active version) |
| `--entrypoint` | — | `string` | — | target entrypoint (default: driver default) |
| `--event-pattern` | — | `stringArray` | `[]` | event type pattern(s), dot-segmented glob with * and {a,b} (repeatable) |
| `--json` | — | `bool` | `false` | JSON output |
| `--name` | — | `string` | — | binding display name (default: route key) |
| `--retry-backoff` | — | `int` | `0` | delivery retry base backoff in seconds, exponential with 1h cap (0 = server default 30) |
| `--retry-max-attempts` | — | `int` | `0` | delivery retry attempts (0 = server default 5) |
| `--route-key` | — | `string` | — | route key, e.g. github.pull_request.opened |
| `--schedule` | — | `string` | — | cron expression (standard 5-field or @descriptor); required when --source is cron |
| `--schedule-timezone` | — | `string` | — | IANA timezone the schedule is evaluated in (default UTC) |
| `--secret` | — | `string` | — | webhook HMAC secret for signature verification |
| `--source` | — | `string` | `github` | source kind |
| `--subject-key-template` | — | `string` | — | concurrency subject key template; tokens: {{subject_ref}}, {{event_type}}, {{attrs.<name>}} (empty = default key <binding-id>\|<subject-ref>) |
| `--workflow` | — | `string` | — | workflow name backing the driver |

### loom trigger bindings list

List trigger bindings

**Usage:** `loom trigger bindings list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--enabled` | — | `bool` | `false` | filter by enabled state (only applied when set) |
| `--json` | — | `bool` | `false` | JSON output |
| `--source-kind` | — | `string` | — | filter by source kind |

### loom trigger bindings show

Show a single trigger binding

**Usage:** `loom trigger bindings show <binding-id> [flags]`

**Aliases:** `get`

### loom trigger bindings update

Update Router v2 fields on an existing trigger binding

**Usage:** `loom trigger bindings update <binding-id> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--actor-filter-exclude` | — | `stringArray` | `[]` | actor kind(s) to exclude (repeatable); on update, pass a single empty value to clear the filter |
| `--concurrency-policy` | — | `string` | — | concurrency policy: allow\|forbid\|replace\|queue\|one_active_per_epic |
| `--event-pattern` | — | `stringArray` | `[]` | event type pattern(s), dot-segmented glob with * and {a,b} (repeatable) |
| `--json` | — | `bool` | `false` | JSON output |
| `--retry-backoff` | — | `int` | `0` | delivery retry base backoff in seconds, exponential with 1h cap (0 = server default 30) |
| `--retry-max-attempts` | — | `int` | `0` | delivery retry attempts (0 = server default 5) |
| `--schedule` | — | `string` | — | cron expression (standard 5-field or @descriptor); required when --source is cron |
| `--schedule-timezone` | — | `string` | — | IANA timezone the schedule is evaluated in (default UTC) |
| `--subject-key-template` | — | `string` | — | concurrency subject key template; tokens: {{subject_ref}}, {{event_type}}, {{attrs.<name>}} (empty = default key <binding-id>\|<subject-ref>) |

### loom trigger deliveries

Inspect persisted trigger deliveries

**Usage:** `loom trigger deliveries [flags]`

### loom trigger deliveries list

List trigger deliveries

**Usage:** `loom trigger deliveries list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--event` | — | `string` | — | filter by trigger event id |
| `--json` | — | `bool` | `false` | JSON output |
| `--limit` | — | `int` | `0` | max results |
| `--status` | — | `string` | — | filter by delivery status |

### loom trigger deliveries show

Show a single trigger delivery

**Usage:** `loom trigger deliveries show <delivery-id> [flags]`

### loom trigger events

Inspect persisted trigger events

**Usage:** `loom trigger events [flags]`

### loom trigger events list

List trigger events

**Usage:** `loom trigger events list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--limit` | — | `int` | `0` | max results |
| `--source-kind` | — | `string` | — | filter by source kind |

### loom trigger events show

Show a single trigger event

**Usage:** `loom trigger events show <event-id> [flags]`

### loom usage

Display token usage and cost summaries

**Usage:** `loom usage [flags]`

```
Display token usage and cost summaries from agent sessions.

Reads usage data from the local .loom/usage.jsonl file and displays
aggregated token consumption and cost breakdowns by agent and backend.

FLAGS
  --agent <name>      Filter by agent name
  --backend <name>    Filter by backend name
  --epic <id>         Filter by epic ID
  --since <date>      Start date (YYYY-MM-DD)
  --until <date>      End date (YYYY-MM-DD)
  --today             Show only today's usage
  --week              Show last 7 days
  --format <fmt>      Output format: table (default) or json
  --verbose           Show per-session detail

EXAMPLES
  loom usage                        # Show all usage
  loom usage --today                # Today's usage
  loom usage --week                 # Last 7 days
  loom usage --agent nova           # Filter by agent
  loom usage --format json          # JSON output
  loom usage --verbose              # Per-session detail
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Filter by agent name |
| `--backend` | — | `string` | — | Filter by backend name |
| `--epic` | — | `string` | — | Filter by epic ID |
| `--format` | — | `string` | `table` | Output format: table or json |
| `--since` | — | `string` | — | Start date (YYYY-MM-DD) |
| `--today` | — | `bool` | `false` | Show only today's usage |
| `--until` | — | `string` | — | End date (YYYY-MM-DD) |
| `--verbose` | — | `bool` | `false` | Show per-session detail |
| `--week` | — | `bool` | `false` | Show last 7 days |

### loom worker

Run a remote agent worker that connects to a control plane

**Usage:** `loom worker [flags]`

```
Run a remote agent worker that registers with a loom control plane
and executes tasks using HTTP-backed interfaces for lock files, events,
and log forwarding. Designed for containers or remote machines.

The worker:
  1. Registers with the control plane
  2. Sets up HTTP-backed lock bridge, event emitter, and log forwarder
  3. Runs the auto mode loop with remote interfaces
  4. Deregisters on shutdown

FLAGS
  --control-plane  Control plane URL (or LOOM_WORKER_CONTROL_PLANE)
  --workspace      Workspace name (or LOOM_WORKER_WORKSPACE)
  --agent          Agent name (or LOOM_WORKER_AGENT)
  --backend        AI backend to use (or LOOM_WORKER_BACKEND)

AUTHENTICATION
  Set LOOM_WORKER_TOKEN to the shared secret for control plane auth.

EXAMPLES
  loom worker \
    --control-plane http://loom.example.com:8080 \
    --workspace my-workspace \
    --agent agent-alpha \
    --backend claude
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--agent` | — | `string` | — | Agent name (env: LOOM_WORKER_AGENT) |
| `--backend` | — | `string` | — | AI backend (env: LOOM_WORKER_BACKEND) |
| `--control-plane` | — | `string` | — | Control plane URL (env: LOOM_WORKER_CONTROL_PLANE) |
| `--idle-timeout` | `-t` | `int` | `0` | Exit after N minutes with no tasks (0 = none) |
| `--interval` | `-i` | `int` | `30` | Polling interval in seconds when no tasks available |
| `--max-tasks` | `-m` | `int` | `0` | Maximum tasks to process (0 = unlimited) |
| `--parent` | — | `string` | — | Filter tasks to descendants of this epic ID |
| `--workspace` | — | `string` | — | Workspace UUID or name (env: LOOM_WORKER_WORKSPACE) |

### loom worker profile

Manage worker profiles in the active workspace

**Usage:** `loom worker profile [flags]`

### loom worker profile add

Create a worker profile

**Usage:** `loom worker profile add <PROFILE_ID> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--backend` | — | `string` | — | Preferred backend |
| `--capability` | — | `stringSlice` | `[]` | Capability (comma-separated or repeat flag) |
| `--disabled` | — | `bool` | `false` | Create disabled |
| `--label` | — | `stringSlice` | `[]` | Profile label (comma-separated or repeat flag) |
| `--max-priority` | — | `int` | `-1` | Maximum priority 0..4 (-1 = unset) |
| `--metadata` | — | `stringArray` | `[]` | Metadata key=value (repeatable) |
| `--name` | — | `string` | — | Display name (default: profile ID) |
| `--parent-epic` | — | `string` | — | Parent epic scope |
| `--repo` | — | `stringSlice` | `[]` | Repo scope (comma-separated or repeat flag) |
| `--role` | — | `string` | — | Worker role |

### loom worker profile list

List worker profiles

**Usage:** `loom worker profile list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--backend` | — | `string` | — | Filter by backend |
| `--enabled` | — | `string` | — | Filter by enabled true/false |
| `--json` | — | `bool` | `false` | JSON output |
| `--limit` | — | `int` | `0` | Maximum rows |
| `--role` | — | `string` | — | Filter by role |

### loom worker profile remove

Delete a worker profile

**Usage:** `loom worker profile remove <PROFILE_ID> [flags]`

### loom worker profile set

Set a worker profile field

**Usage:** `loom worker profile set <PROFILE_ID> <KEY> <VALUE> [flags]`

```
Set a worker profile field by key. Supported keys:
  name          string
  role          string
  backend       string
  parent_epic   string
  enabled       bool ("true"/"false")
  max_priority  integer 0..4
  repos         comma-separated list
  labels        comma-separated list
  capabilities  comma-separated list
  metadata      comma-separated key=value list
```

### loom worker profile show

Show worker profile details

**Usage:** `loom worker profile show <PROFILE_ID> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom worker profile unset

Clear a worker profile field

**Usage:** `loom worker profile unset <PROFILE_ID> <KEY> [flags]`

```
Clear a worker profile field. Supported keys:
  backend
  parent_epic
  max_priority
  repos
  labels
  capabilities
  metadata
```

### loom worker service

Manage long-running agent services in the active workspace

**Usage:** `loom worker service [flags]`

### loom worker service add

Create an agent service

**Usage:** `loom worker service add <SERVICE_ID> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--budget-policy` | — | `string` | — | Budget policy |
| `--desired-state` | — | `string` | — | Desired state running\|stopped\|paused (default: stopped) |
| `--event-source` | — | `stringSlice` | `[]` | Event source (comma-separated or repeat flag) |
| `--kind` | — | `string` | — | Service kind |
| `--lease-id` | — | `string` | — | Service lease identifier |
| `--max-instances` | — | `int` | `0` | Maximum instances (0 = default) |
| `--metadata` | — | `stringArray` | `[]` | Metadata key=value (repeatable) |
| `--name` | — | `string` | — | Display name (default: service ID) |
| `--permission` | — | `stringSlice` | `[]` | Permission (comma-separated or repeat flag) |
| `--placement-policy` | — | `string` | — | Placement policy |
| `--profile` | — | `string` | — | Worker profile name |
| `--restart-policy` | — | `string` | — | Restart policy |
| `--role` | — | `string` | — | Role name |
| `--schedule-id` | — | `string` | — | Schedule identifier |
| `--state-ref` | — | `string` | — | Persistent state reference |
| `--trigger-ref` | — | `stringSlice` | `[]` | Trigger binding reference (comma-separated or repeat flag) |

### loom worker service list

List agent services

**Usage:** `loom worker service list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--desired-state` | — | `string` | — | Filter by desired state |
| `--json` | — | `bool` | `false` | JSON output |
| `--kind` | — | `string` | — | Filter by service kind |
| `--limit` | — | `int` | `0` | Maximum rows |
| `--profile` | — | `string` | — | Filter by profile name |
| `--role` | — | `string` | — | Filter by role name |

### loom worker service remove

Delete an agent service

**Usage:** `loom worker service remove <SERVICE_ID> [flags]`

### loom worker service set

Set an agent service field

**Usage:** `loom worker service set <SERVICE_ID> <KEY> <VALUE> [flags]`

```
Set an agent service field by key. Supported keys:
  name              string
  kind              lead|support|triage|on_call|scheduled|maintenance|orchestrator|always_on|cron|event|campaign_orchestrator
  desired_state     running|stopped|paused
  role_name         string
  profile_name      string
  schedule_id       string
  placement_policy  string
  max_instances     positive integer
  lease_id          string
  restart_policy    string
  budget_policy     string
  state_ref         string
  event_sources     comma-separated list
  trigger_refs      comma-separated list
  permissions       comma-separated list
  metadata          comma-separated key=value list
```

### loom worker service show

Show agent service details

**Usage:** `loom worker service show <SERVICE_ID> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom worker service unset

Clear an agent service field

**Usage:** `loom worker service unset <SERVICE_ID> <KEY> [flags]`

```
Clear an agent service field. Supported keys:
  profile_name
  schedule_id
  placement_policy
  lease_id
  restart_policy
  budget_policy
  state_ref
  event_sources
  trigger_refs
  permissions
  metadata
```

### loom workflow

Author, approve, activate, and run Flue workflows

**Usage:** `loom workflow [flags]`

### loom workflow activate

Make a workflow DriverVersion the active version

**Usage:** `loom workflow activate <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--version` | — | `string` | — | DriverVersion id |

### loom workflow approve

Approve one workflow DriverVersion for local process execution

**Usage:** `loom workflow approve <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--version` | — | `string` | — | DriverVersion id |

### loom workflow build

Build local workflow TypeScript source and register a non-active DriverVersion

**Usage:** `loom workflow build <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--source` | — | `string` | — | Workflow source directory |

### loom workflow clone

Copy built-in workflow TypeScript source into a local source tree

**Usage:** `loom workflow clone <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--out` | — | `string` | — | Output source directory |

### loom workflow digest

Print the canonical source digest of a built-in workflow

**Usage:** `loom workflow digest <workflow> [flags]`

```
Print the canonical source digest (workflows.SourceDigest) of a built-in
workflow's source tree.

This is the SAME digest serve's builtin self-heal (EnsureBuiltinWorkflow)
computes, so out-of-band registrations that stamp it — e.g.
loom driver register --source-digest "$(loom workflow digest epic-runner)" —
hit the self-heal's exact-match fast path instead of logging digest drift.

Without --file the digest covers the sources embedded in THIS binary. A
registration should attest the bytes it actually STAGED, so register flows
pass --file <spec-key>=<path> for every source file in the bundle (the key
set must exactly match the workflow's source set); the digest then hashes
the staged contents under the canonical keys. Staged bytes identical to the
embedded sources produce the embedded digest (fast path); modified bytes
produce an honestly different digest (serve logs drift instead of silently
mislabeling the version). Purely local: no store or workspace is opened.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--file` | — | `stringArray` | `[]` | Staged source to hash as <spec-key>=<path> (repeatable; must cover the workflow's full source set) |
| `--json` | — | `bool` | `false` | JSON output |

### loom workflow list

List registered workflows

**Usage:** `loom workflow list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom workflow readyz

Check local workflow authoring prerequisites

**Usage:** `loom workflow readyz [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom workflow run

Create a DriverRun for a workflow

**Usage:** `loom workflow run <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--driver-version` | — | `string` | — | Preview run against a specific DriverVersion id |
| `--epic` | — | `string` | — | Epic ID to pass as input.epicId |
| `--input` | — | `stringArray` | `[]` | Input key=value (repeatable) |
| `--json` | — | `bool` | `false` | JSON output |

### loom workflow unapprove

Remove approval from one workflow DriverVersion

**Usage:** `loom workflow unapprove <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |
| `--version` | — | `string` | — | DriverVersion id |

### loom workflow versions

List registered workflow versions

**Usage:** `loom workflow versions <workflow> [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom workspace

Manage multi-repo workspaces

**Usage:** `loom workspace [flags]`

```
Manage loom workspaces that group multiple repositories together.

Subcommands:
  create       Create a new workspace with git worktrees
  list         List all configured workspaces
  remove       Remove a workspace and its worktrees

Examples:
  loom workspace create myws --repos /path/to/repo1,/path/to/repo2
  loom workspace list
  loom workspace remove myws
```

### loom workspace add

Create a new workspace in fleet-db

**Usage:** `loom workspace add <KEY> [flags]`

```
Create a new workspace. KEY must match fleet-db's key regex
(uppercase letters, digits, hyphens; 1-32 chars; starts with a letter).

Examples:
  loom workspace add MYPROJ --description "My project"
  loom workspace add ACME --branch main
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--branch` | — | `string` | — | Default branch (default: main) |
| `--description` | — | `string` | — | Optional description |
| `--design-format` | — | `string` | — | Planner design output format: markdown or html (default: markdown) |

### loom workspace create

Create a new workspace with git worktrees

**Usage:** `loom workspace create <name> [flags]`

```
Create a new workspace that groups multiple repositories together.

For each repo, a git worktree is created under the workspace directory.
The workspace is registered in FleetDB and local checkout paths are cached in
~/.loom/state.json.

Examples:
  loom workspace create myws --repos /path/to/frontend,/path/to/backend
  loom workspace create myws --repos /path/to/repo --branch feature-x
  loom workspace create myws --repos /path/to/repo --path /custom/path
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--branch` | — | `string` | — | Branch name for worktrees (default: workspace name) |
| `--default` | — | `bool` | `false` | Deprecated no-op; default workspace selection has been removed |
| `--path` | — | `string` | — | Workspace directory path (default: ~/.loom/workspaces/<name>) |
| `--repos` | — | `string` | — | Comma-separated list of repo paths (required) |

### loom workspace list

List all configured workspaces

**Usage:** `loom workspace list [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | Output as JSON |

### loom workspace ops

Agent-safe workspace operations

**Usage:** `loom workspace ops [flags]`

```
Agent-safe workspace operations expose one-shot JSON-friendly status,
diagnostics, and runtime repair for local/desktop workspaces.
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | Output JSON |

### loom workspace ops diagnose

Diagnose workspace runtime and agent configuration

**Usage:** `loom workspace ops diagnose [KEY] [flags]`

### loom workspace ops ensure-runtime

Ensure the local runtime and workspace daemon are running

**Usage:** `loom workspace ops ensure-runtime [KEY] [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--timeout` | — | `int` | `20` | Seconds to wait for runtime and daemon readiness |

### loom workspace ops status

Show workspace runtime status

**Usage:** `loom workspace ops status [KEY] [flags]`

### loom workspace remove

Remove a workspace and its worktrees

**Usage:** `loom workspace remove <name> [flags]`

```
Remove a workspace and optionally clean up its git worktrees.

By default, removes the workspace directory and runs git worktree remove
for each repo. Use --keep-worktrees to only remove from config.

Examples:
  loom workspace remove myws
  loom workspace remove myws --force
  loom workspace remove myws --keep-worktrees
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--force` | — | `bool` | `false` | Remove even if worktrees are dirty |
| `--keep-worktrees` | — | `bool` | `false` | Remove from config but don't delete git worktrees |

### loom workspace set

Update workspace settings in fleet-db

**Usage:** `loom workspace set <KEY> [flags]`

```
Update settings on an existing workspace.

Examples:
  loom workspace set MYPROJ --design-format html
  loom workspace set MYPROJ --design-format markdown
```

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--design-format` | — | `string` | — | Planner design output format: markdown or html (empty clears) |

### loom workspace show

Show workspace details (defaults to active)

**Usage:** `loom workspace show [KEY] [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom workspace status

Show workspace lifecycle state

**Usage:** `loom workspace status [KEY] [flags]`

**Flags:**

| Flag | Shorthand | Type | Default | Description |
|------|-----------|------|---------|-------------|
| `--json` | — | `bool` | `false` | JSON output |

### loom workspace use

Remember the last selected workspace for UI convenience

**Usage:** `loom workspace use <KEY> [flags]`

```
Persist KEY in ~/.loom/state.json as a UI selection hint.
Runtime commands no longer use this as an implicit default; set
LOOM_WORKSPACE or pass --workspace for command execution.
```
