# Native v5 selected history validation

The native bridge, recovery offers and browser preparation require fleet.issue-workspace.v5. All eleven fields are required, including history:null for an unselected response. Selected history is an exact request scope: issue_id, present, events and has_older. The browser's current HTTP/registry path remains unselected and rejects unsolicited selected history. FleetBackend.ReadIssueRecoveryForIssue supplies an explicit query-encoded issue ID and rejects missing or mismatched selections. This method is a prerequisite for binding browser selection ownership through the registry.

Each history event preserves all ten native fields, including empty before/after strings and metadata objects. Raw positive signed-64-bit sequence IDs ending in -0 must increase strictly; they identify history records and never replace the opaque through checkpoint. Fleet verifies the upper bound against its raw applied head. Loom checks the opaque checkpoint/source framing without decoding it. Validators enforce window size, has_older/full-window consistency, membership/absence, event workspace and entity scope, action/entity mapping, strict timestamps, string metadata and exact fields. Selected IDs retain exact strings and share Go whitespace and 1024 UTF-8 byte limits; invalid Unicode is rejected.

Preparation freezes complete selected records only when the caller supplies the expected selection. No timeline cast, UI selection binding, cache publication or reset acknowledgment is introduced. Next integration must bind issue selection plus generation (including A-to-B-to-A), adapt native timeline data, preserve has_older and fence ordinary requests before one recovery publication generation.

Validation: 131 shared Go/TypeScript corpus cases, all action/entity pairs, selected HTTP scope/escaping checks and affected backend/realtime/subscription races passed. Final Go selected HTTP/corpus tests passed in 1.455s; full affected races earlier passed Fleet1.669s, realtime3.738s and subscription3.738s. The full frontend gate passed 428 files/9420 tests; final selection-boundary and corpus tests additionally passed 159 cases. TypeScript, ESLint, formatting and Go lint passed. Production frontend build passed in 2.70s. Independent Go/frontend and paired Fleet production reviews found no blocker. These are local package/HTTP proofs; no deployed paired-browser or hosted CI result is claimed.
