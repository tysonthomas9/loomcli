# Block dependencies for implementation phase.
# Format: task-slug depends-on-slug
# Added by test script between Phase 2 (review) and Phase 3 (implementation)
# so that planning can parallelize all tasks.
#
# Ordering: scaffold → (7 components + quality) → (detail panels) → app → visual

# All components + quality depend on scaffold
data scaffold
header scaffold
board scaffold
column scaffold
card scaffold
agentlist scaffold
workqueue scaffold
quality scaffold

# Detail panels depend on the components they extend
agent-detail agentlist
agent-detail data
task-detail card
task-detail data

# App imports everything
app data
app header
app board
app column
app card
app agentlist
app workqueue
app agent-detail
app task-detail

# Visual tests need the full assembled app
visual app
