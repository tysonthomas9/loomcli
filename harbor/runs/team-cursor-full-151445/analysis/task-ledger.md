# Task ledger (fleet-db final-issues.json; the headless lead's transcript was not exported, so its seeding/approval work is visible only here and in lead-passes.log / daemon-filtered.log)

22 issues. id | status | type | priority | labels | owner | created | closed | title
MARATHON-1 | open | epic | 2 |  | marathon-harness | 22:16:35 |  | Huddle: Slack-like team chat cluster
MARATHON-2 | closed | task | 0 | backend | marathon-harness | 22:16:49 | 22:30:35 | Scaffold cluster: start.sh, 3 HTTP nodes, Redis
MARATHON-3 | closed | task | 0 | backend | marathon-harness | 22:16:49 | 22:44:22 | Auth API: register, login, me, bearer tokens
MARATHON-4 | closed | task | 1 | backend | marathon-harness | 22:16:49 | 22:59:54 | Profiles API: UserObj get and patch me
MARATHON-5 | closed | task | 0 | backend | marathon-harness | 22:16:49 | 23:11:15 | Workspaces: create, list, detail, auto #general
MARATHON-6 | closed | task | 1 | backend | marathon-harness | 22:17:01 | 23:43:57 | Workspace members, roles, ownership transfer
MARATHON-7 | closed | task | 0 | backend | marathon-harness | 22:17:01 | 00:15:38 | Channels: create, join, leave, topic, archive, pins
MARATHON-8 | closed | task | 0 | backend | marathon-harness | 22:17:01 | 01:01:20 | Messages, threads, reactions REST API
MARATHON-9 | closed | task | 2 | backend | marathon-harness | 22:17:16 | 01:08:35 | DMs: get-or-create DM channel
MARATHON-10 | review | task | 2 | architect,backend | marathon-harness | 22:17:16 |  | Files: upload, metadata, download, attach
MARATHON-11 | in_progress | task | 1 | backend | marathon-harness | 22:17:16 |  | Search, slash commands, mentions, read state
MARATHON-12 | closed | task | 1 | backend | marathon-harness | 22:17:16 | 00:28:53 | User groups and workspace invitations
MARATHON-13 | in_progress | task | 0 | backend | marathon-harness | 22:17:29 |  | WebSocket subscribe/resume and dense channel seq
MARATHON-14 | open | task | 0 | architect,backend | marathon-harness | 22:17:29 |  | Cross-node fan-out and Redis-outage resilience
MARATHON-15 | open | task | 1 | architect,backend | marathon-harness | 22:17:47 |  | IRC gateway on :6667 with bidirectional bridge
MARATHON-16 | closed | task | 1 | frontend | marathon-harness | 22:17:47 | 23:36:55 | SPA: auth, token storage, workspace shell
MARATHON-17 | review | task | 1 | architect,frontend | marathon-harness | 22:17:47 |  | SPA: channels, messages, threads, reactions
MARATHON-18 | closed | task | 2 | frontend | marathon-harness | 22:17:47 | 01:19:44 | SPA: workspace and channel settings UI
MARATHON-19 | closed | task | 1 | qa | marathon-harness | 22:37:07 | 22:45:55 | Verify: cluster boot health Redis and stub GET /
MARATHON-20 | closed | task | 1 | qa | marathon-harness | 22:37:07 | 22:47:14 | Verify: HTTP node and Redis SIGKILL respawn
MARATHON-21 | closed | task | 1 | qa | marathon-harness | 22:53:55 | 23:01:10 | Verify: auth register login me envelopes
MARATHON-22 | closed | task | 1 | qa | marathon-harness | 22:53:55 | 23:03:22 | Verify: auth token accepted on all HTTP nodes


# Notes and designs per task


## MARATHON-1 — Huddle: Slack-like team chat cluster [open] close_reason=

## MARATHON-2 — Scaffold cluster: start.sh, 3 HTTP nodes, Redis [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Implemented Python/FastAPI+Uvicorn cluster scaffold per design. start.sh supervises Redis :6379 + nodes 8000/8001/8002 with SIGKILL respawn. GET /api/health and stub GET / verified; unit tests in tests/test_health.py. SQLite WAL bootstrap at data/huddle.sqlite with schema_migrations.
DESIGN (markdown, 18024 chars): # MARATHON-2 — Scaffold cluster: start.sh, 3 HTTP nodes, Redis ## Summary This task boots the Huddle cluster so the verifier (and every later feature task) has a live, self-contained process tree: `/app/start.sh` stays in the foreground, supervises a local Redis on `127.0.0.1:6379` and three identical HTTP nodes on `127.0.0.1:8000/8001/8002`, and respawns any of those children after `SIGKILL`. Each node exposes a stub `GET /` and the contracted `GET /api/health`. The design also locks language/framework and the shared durable storage layout that Auth, messages, fan-out, IRC, and the SPA will all assume. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Language / HTTP framework | **Python 3.12 + FastAPI + Uvicorn** | | Shared durable store | **SQLite file** at `/app/data/huddle.sqlite` with **WAL** + busy timeout (shared by all nodes) | | Cross-node transport | **Local Redis** on `127.0.0.1:6379` (pub/sub / coordination only — **not** source of truth) | | Process supervisor | **`/app/start.sh`** (bash): create dirs, ensure venv+deps, start Redis + 3 Uvicorn workers, foreground wait loop with respawn | | Runtime root | Repo root **is** `/app` when the verifier runs | ### Why this stack - Epic/instruction leave language open; Python 3.12, `sqlite3`, and `redis-server` are already on the image. FastAPI covers REST + future WebSockets (Starlette) without a second runtime for the backend/IRC path. - Redis outage requirements (MARATHON-14: writes and dense `seq` continue while Redis is dead) **forbid** using Redis as the durable store. SQLite on a shared filesystem path is the simplest self-contained SoT that all three processes can open. - A shell supervisor matches the verifier contract (`./start.sh` foreground) and can respawn both HTTP and Redis without DinD or external orchestrators. ### Alternatives considered | Alternative | Rejected because | |------------|------------------| | Node.js + Express/Fastify | Equally viable for HTTP/WS; wou

## MARATHON-3 — Auth API: register, login, me, bearer tokens [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Auth API implemented: POST /api/auth/register|login, GET /api/auth/me; SQLite users+auth_tokens (migration v1); scrypt passwords; SHA-256 bearer tokens; error envelope; get_current_user dependency. Tests: tests/test_auth.py (33 suite total green). Cluster verified token across :8000/:8001/:8002. Logout/revoke out of scope per design.
DESIGN (markdown, 20666 chars): # MARATHON-3 — Auth API: register, login, me, bearer tokens ## Summary This task adds the first product API surface for Huddle: `POST /api/auth/register`, `POST /api/auth/login`, and `GET /api/auth/me`, backed by durable users and bearer tokens in the shared SQLite database so any of the three HTTP nodes accepts the same credential. It locks the canonical `UserObj` JSON shape (consumed by Profiles, Workspaces, and the SPA), the error envelope convention for all later REST routes, and a reusable FastAPI auth dependency that every authenticated endpoint will import. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Durable auth store | **SQLite** at `/app/data/huddle.sqlite` (shared by all nodes) — tables `users` + `auth_tokens` | | Token transport | Opaque bearer string in `Authorization: Bearer <token>` | | Token persistence | Store **SHA-256 hex** of the token; return plaintext only in register/login responses | | Password storage | **stdlib `hashlib.scrypt`** (salt per user); never store plaintext | | User / token ids | Stable strings: **UUID4** hex/`str` for `users.id`; tokens identified by hash (no separate public token id) | | Route layout | FastAPI `APIRouter` in `server/auth/routes.py` mounted from `server/app.py` under `/api/auth` | | Auth dependency | `server/auth/deps.py::get_current_user` for `/me` and for MARATHON-4+ | | Schema evolution | Extend MARATHON-2 `schema_migrations`: apply version **1** (users + auth_tokens) idempotently on node lifespan | ### Why - MARATHON-2 fixed SQLite as SoT and Redis as ephemeral fan-out only. Storing tokens in Redis would break “any node accepts the same token” after Redis `SIGKILL` and contradicts MARATHON-14. Shared SQLite is the only persistence already wired on every node. - Opaque bearer tokens match the epic (“REST + bearer auth”) and the SPA task (`localStorage` `huddle.token`) without JWT clock skew or shared signing secrets across processes. - Hashing stored tokens limits damage from

## MARATHON-4 — Profiles API: UserObj get and patch me [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Profiles GET /api/users/{id} + PATCH /api/users/me shipped. No migration (v1 users columns). publish_user_updated is a documented no-op (no channel_id/seq) for MARATHON-13/14. Broadened RequestValidationError envelope to /api/* except /api/health. Tests: tests/test_profiles.py; full suite 73 passed. Cluster boundary: PATCH :8000 visible on GET :8001/:8002.
DESIGN (markdown, 21150 chars): # MARATHON-4 — Profiles API: UserObj get and patch me ## Summary This task adds the Profiles REST surface on top of the Auth-owned `users` table: authenticated `GET /api/users/{id}` returns a canonical `UserObj` (or 404), and authenticated `PATCH /api/users/me` applies any subset of mutable profile fields with IANA timezone and length validation (400 on failure). It does **not** change the `UserObj` shape locked by MARATHON-3; it only mutates profile columns already present in migration v1 and leaves a clear, channel-less `user.updated` event contract so MARATHON-13/14 can fan profile changes over WebSocket later. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence | **Same** shared SQLite `users` row from MARATHON-3 (migration **v1**). **No new migration.** | | Route layout | FastAPI `APIRouter` prefix `/api/users` in `server/users/routes.py`, mounted from `server/app.py` | | Auth | Reuse `server.auth.deps.get_current_user` on **both** GET and PATCH | | `UserObj` mapping | Keep sole mapper in `server/auth/users.py` (`row_to_user_obj` / `USER_OBJ_KEYS`); Profiles calls into that module for reads/updates — **no second users table or second JSON shape** | | Timezone validation | stdlib `zoneinfo.ZoneInfo(name)` — accept iff construction succeeds; reject → 400 `validation_error` | | Length limits | Explicit max code-point lengths below (task requires “out-of-range lengths → 400”) | | Immutable fields | `id` and `username` are **not** mutable via PATCH; if present in body they are **ignored** (same forward-compat rule as unknown keys) | | WebSocket readiness | After a successful PATCH that changes ≥1 stored field, call `publish_user_updated(user)` with a documented payload that has **no** `channel_id` / `seq`. Implementation is a **local no-op hook** until MARATHON-13/14 wire Redis/WS fan-out | | Errors | Existing `{error:{code,message}}`; broaden `RequestValidationError` handling to all `/api/*` except `/api/health` (same rule as M

## MARATHON-5 — Workspaces: create, list, detail, auto #general [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Shipped workspaces create/list/detail per design. Migration v2 + routes. read_state uses full ReadStateEntry {channel_id,last_read_seq,unread_count,mention_count}; include_archived accepts 1 and case-insensitive true. errors.py already broadened by MARATHON-4. Tests: tests/test_workspaces.py; full unit suite green; cluster verified create:8000 list:8001 detail:8002.
DESIGN (markdown, 28664 chars): # MARATHON-5 — Workspaces: create, list, detail, auto #general ## Summary This task adds the workspace tenancy root for Huddle: authenticated callers can create a workspace (unique kebab `slug`, display `name`, creator as `owner`, default `join_mode=open`), which atomically provisions the public `#general` channel and membership rows; list workspaces they own or belong to; and load a workspace by slug with the visible channel list plus embedded `read_state`. It locks `WorkspaceObj`, the initial `ChannelObj` shape used by later channel APIs, the SQLite schema for workspaces/members/channels, and the REST contracts the SPA shell (MARATHON-16) and members/channels tasks (MARATHON-6/7) consume. **Revision (needs-revision feedback):** `read_state` entries use the **full** `ReadStateEntry` shape shared with `GET /api/channels/{id}/read` (MARATHON-11): `{channel_id, last_read_seq, unread_count, mention_count}`. Empty `[]` until messages/read APIs write rows is fine; when entries are returned, all four fields are present (counts default to `0`). Query `include_archived` treats both literal `1` and `true` (case-insensitive) as “include archived.” ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence | Shared SQLite (MARATHON-2/3 SoT); **migration version 2** additive tables | | Route layout | FastAPI `APIRouter` prefix `/api/workspaces` in `server/workspaces/routes.py`, mounted from `server/app.py` | | Auth | Reuse `server.auth.deps.get_current_user`; all three endpoints require Bearer | | Identity | UUID4 strings for `workspaces.id`, `channels.id`, membership PKs | | `#general` | On create, insert a non-private, non-DM, non-archived channel with `name="general"` (UI `#` is display-only); add creator as channel member | | Membership | Insert `workspace_members` row with `role="owner"` in the same transaction as workspace create (MARATHON-6 owns member list/PATCH/transfer APIs but **needs these rows now** for list/detail authz) | | Private-

## MARATHON-6 — Workspace members, roles, ownership transfer [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Shipped members/roles/transfer on v2 schema (no migration). GET members, PATCH workspace name/join_mode, PATCH member role matrix, POST transfer_ownership. Tests: tests/test_workspace_members.py; full suite 152 passed. Cluster boundary: PATCH :8000, members :8001, transfer :8002 shared DB OK.
DESIGN (markdown, 22541 chars): # MARATHON-6 — Workspace members, roles, ownership transfer ## Summary This task completes workspace tenancy beyond create/list/detail (MARATHON-5): authenticated **members** can list workspace membership; **admins/owners** can PATCH `name` / `join_mode`; **admins/owners** can change other members’ roles under a strict hierarchy; and the **owner** alone can transfer ownership to an existing **admin** (previous owner becomes admin). It locks `MemberObj`, the four REST seams SPA settings (MARATHON-18) and channel/invitation authz (MARATHON-7/12) depend on, and keeps `workspaces.owner_id` synchronized with `workspace_members.role='owner'`. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence | Existing migration **v2** tables only — **no new migration** | | Route layout | Extend `server/workspaces/routes.py` under `/api/workspaces` | | Auth | Reuse `get_current_user`; all endpoints require Bearer | | Workspace resolution | Resolve by `slug`; unknown slug **or** non-member → **404** `not_found` (same as MARATHON-5; do not leak invite-only existence) | | Privilege failures | When caller **is** a member but lacks permission → **403** `forbidden` (first use of the code reserved by MARATHON-5) | | Ownership SoT | Dual write: `workspaces.owner_id` **and** exactly one `workspace_members.role='owner'`; transfer updates both in one transaction | | Role changes | `PATCH .../members/{user_id}` never creates owners; only `POST .../transfer_ownership` promotes to owner | | Member list enrichment | Join `users` for `username` / `display_name` (SPA settings without N+1 `GET /api/users/{id}`) | | Events / Redis | No pub/sub in this task (MARATHON-13/14); no Redis SoT | ### Why - Schema already has `workspace_members.role` CHECK and `join_mode`; inventing a third ownership table would diverge from MARATHON-5. - Separating transfer from role PATCH prevents “set role to owner” shortcuts and matches the task’s 400/403 matrix. - Flat `MemberObj` with de

## MARATHON-7 — Channels: create, join, leave, topic, archive, pins [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Shipped channels create/join/leave/topic/archive + workspace join + pins. Migration v3 messages+channel_pins; assert_channel_writable → 423 archived for M8. Tests: test_channels.py + test_pins.py; full suite 175 passed. Cluster two-app shared data_dir covered.
DESIGN (markdown, 29753 chars): # MARATHON-7 — Channels: create, join, leave, topic, archive, pins ## Summary This task completes channel lifecycle for Huddle on top of the workspace tenancy shipped in MARATHON-5/6: authenticated callers can create public/private channels under a workspace slug, join and leave channels (with open-workspace auto-onboard and invite_only/guest/private gates), list channel members as `UserObj`, patch topic, archive/unarchive (owner-only), and pin/unpin messages. It also implements the SPA-locked workspace join seam (`POST /api/workspaces/{slug}/join`) that MARATHON-16 already calls. It locks the `/api/channels/*` and pin REST contracts, introduces migration **v3** (`messages` minimal + `channel_pins`), and exports a shared **archived-write guard** (`423`) that MARATHON-8/11 must reuse. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence | Shared SQLite SoT; **migration version 3** additive: minimal `messages` + `channel_pins` (pins need message rows; same “schema-ahead” pattern as M5 channels-before-CRUD) | | Route layout | (1) Extend `server/workspaces/routes.py`: `POST /{slug}/join`, `POST /{slug}/channels`. (2) New `server/channels/routes.py` prefix `/api/channels`: join, leave, PATCH, members, archive, unarchive, pins. (3) New `server/messages/routes.py` (or pin routes mounted under `/api/messages`) for `POST/DELETE /{id}/pin` | | Auth | Reuse `get_current_user`; all new endpoints require Bearer | | `ChannelObj` | **Unchanged** key set from MARATHON-5 (`CHANNEL_OBJ_KEYS`); this task owns create/mutate behavior | | Workspace join vs channel join | **Both**: SPA uses workspace join (M16); product grader uses channel join with open auto-onboard. Same membership insert helper | | Private “invite” | No private-invite table yet (MARATHON-12). “Without invite” ≡ caller lacks `channel_members` row → **403** on join. Creator is auto-member on create; tests may SQL-seed members until add-member API exists | | Pins without full Message A

## MARATHON-8 — Messages, threads, reactions REST API [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: IMPL: Messages/threads/reactions REST shipped. Schema is migration v5 (design text said v4; v4 already used by MARATHON-12 groups/invitations). Additive only — preserved v3 messages. file_ids non-empty → 400 until MARATHON-10; @username mentions only; slash bodies stored literally. Unit tests green (151 excl. live/cluster port-bound); port 8000 held by another agent so live cluster pytest left unrun here — cluster read-after-write covered via dual TestClient sharing sqlite.
DESIGN (markdown, 28212 chars): # MARATHON-8 — Messages, threads, reactions REST API ## Summary This task completes the core chat write/read path for Huddle on top of MARATHON-7 channels and the minimal `messages` / pin embed already shipped: authenticated channel members can post top-level messages and one-level thread replies, list channel history (newest-first, cursor pagination) and thread replies (oldest-first), edit their own messages, soft-delete as author or workspace owner, and add/remove emoji reactions. It locks the REST `MessageObj` / `ReactionObj` contracts the SPA (MARATHON-17) and downstream search/WS tasks consume, extends schema with reaction (and optional attachment join) tables while **preserving** the v3 `messages` table and pin routes, and leaves slash-command interception, full mention forms, file upload ownership, and dense `seq` fan-out to their owning tasks. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence | Shared SQLite SoT; **migration version 4** additive: `message_reactions`, optional `message_files`, optional `messages.blocks_json`; **do not** drop/recreate v3 `messages` | | Route layout | Channel-scoped on `server/channels/routes.py`: `POST`/`GET /api/channels/{id}/messages`. Message-scoped on existing `server/messages/routes.py`: `PATCH`/`DELETE /{id}`, `GET /{parent_id}/replies`, `POST`/`DELETE .../reactions`, keep existing pin routes | | Auth | Reuse `get_current_user`; all endpoints require Bearer | | `MessageObj` | **Same key set as MARATHON-7** (`MESSAGE_OBJ_KEYS`); enrich `reply_count`, `files`, `reactions`, `mentions` for real data; **no `seq`** on REST objects (product: seq lives on WS frames — MARATHON-13) | | Soft delete | Set `deleted_at`; **exclude** deleted rows from channel history and reply lists; DELETE idempotent; PATCH / react on deleted → **409** `conflict` | | Archived writes | Reuse `server.channels.store.assert_channel_writable` → **423** `archived` for POST message, PATCH, DELETE (mutating), POST/DELET

## MARATHON-9 — DMs: get-or-create DM channel [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Implemented POST /api/dms get-or-create: deterministic dm- name hash (no migration); is_dm+is_private; ensure both memberships; list predicate hides DMs from non-members; DM authz member-only (no owner bypass) on members/PATCH; pins/messages already member-only. Tests: tests/test_dms.py (12). Full suite 204 passed. Live boundary: A→B and B→A same channel_id on :8000.
DESIGN (markdown, 18939 chars): ## Summary This task adds authenticated **get-or-create** for 1:1 direct-message channels: `POST /api/dms` resolves a workspace-scoped DM between the caller and a peer `user_id`, returning the same `channel_id` for both directions and on every repeat call. The channel is persisted as a normal `channels` row with `is_dm=true` (and `is_private=true` for visibility), both users in `channel_members`, so existing channel/message authz (MARATHON-7/8) and the SPA `dms-list` split (MARATHON-16) work without a separate message transport. It does not build group DMs, DM UI, or message posting. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Route | Top-level `POST /api/dms` (task-mandated), not nested under `/api/workspaces/{slug}` | | Request body | `{ "user_id", "workspace_id" }` — both required UUID strings. Task text abbreviated `{user_id}`; **workspace is mandatory** because every channel row has non-null `workspace_id` (MARATHON-5) and the shell lists DMs per current workspace | | Response | Always **200** `{ "channel_id": "<uuid>" }` (not `201`, not a full `ChannelObj` wrapper) | | Pair identity | Canonical unordered pair: `user_lo, user_hi = sorted([caller_id, peer_id])` within a workspace | | Dedup without competing for migration v4 | **No new table.** Deterministic synthetic `channels.name` under existing `UNIQUE(workspace_id, name)`: `dm-` + first 29 hex chars of `SHA-256(workspace_id \|\| ":" \|\| user_lo \|\| ":" \|\| user_hi)` (ASCII, lower hex). Satisfies `CHANNEL_NAME_RE` (`^[a-z0-9-]{2,32}$`). Lookup = `SELECT` by `(workspace_id, name, is_dm=1)` | | Flags | Insert with `is_dm=1`, `is_private=1`, `topic=""`, `is_archived=0` | | Membership | On create: insert **both** users into `channel_members`. On get (existing row): **ensure** both memberships exist (re-add if a party previously `leave`d) | | Package | New `server/dms/` router + thin store orchestration; channel row helpers live in `server/channels/store.py` next to `create_cha

## MARATHON-10 — Files: upload, metadata, download, attach [review] close_reason=
NOTES: DESIGN: Files upload/metadata/download + activate M8 file_ids attach. Migration v7 files table (v6 reserved by M13); blobs in shared data/files/{id}; FileObj keys locked to M8 embed; 10MiB→413 payload_too_large; download authz uploader-or-channel-read. Label remains backend for implementer after architect approval.
DESIGN (markdown, 19556 chars): # MARATHON-10 — Files: upload, metadata, download, attach ## Summary This task completes the file attachment seam that MARATHON-8 already stubbed: authenticated clients can upload a binary via `POST /api/files` (multipart, hard cap **10 MiB** → **413**), fetch metadata via `GET /api/files/{id}`, and download bytes via `GET /api/files/{id}/download` with `Content-Type` and `Content-Disposition`. Uploads land in the shared `data/files/` tree reserved by MARATHON-2 and are recorded in a new SQLite `files` table so any HTTP node can serve the same blob. Once a file exists, existing `POST /api/channels/{id}/messages` `file_ids` validation (caller-owned, not yet attached) becomes satisfiable and `MessageObj.files` returns real `FileObj` embeds — no MessageObj key changes. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence metadata | Shared SQLite table `files` — **migration version 7** (additive). **v6 is reserved by MARATHON-13** (`channel_seq` / `channel_events`); do not renumber. | | Blob storage | Shared filesystem under `{HUDDLE_DATA_DIR}/files/{file_id}` (opaque path = UUID string, no extension). Same SoT model as SQLite: all nodes share `HUDDLE_DATA_DIR`. | | Route package | New `server/files/` (`routes.py`, `store.py`, `__init__.py`) mounted from `server/app.py` under `/api/files` | | Auth | Reuse `get_current_user`; all three endpoints require Bearer | | `FileObj` keys | Lock exactly `{id, uploader_id, filename, content_type, size, created_at}` — matches MARATHON-8 embed contract and `list_message_files` SELECT | | Multipart field | Single required form part named **`file`** (`UploadFile`); other parts ignored | | Size limit | **10 MiB = 10 × 1024 × 1024 bytes**. Count bytes while streaming to disk; on overflow delete partial blob, abort txn → **413** `payload_too_large` | | Attach seam | **Do not reimplement** message create. Keep / rely on existing `messages.store.validate_file_ids` + `message_files` insert + `list_message

## MARATHON-11 — Search, slash commands, mentions, read state [in_progress] close_reason=
NOTES: Design complete for search, slash commands, advanced mentions, and read_state. Grounded in MARATHON-5 ReadStateEntry, MARATHON-7 topic/archive authz, MARATHON-8 MessageObj + composer POST, MARATHON-12 group handles, MARATHON-13 last_read_seq=dense event seq (soft dep for mid-cursor recompute). No new migration (avoids v6 clash). Label remains backend for implementer after architect approval.
DESIGN (markdown, 27763 chars): # MARATHON-11 — Search, slash commands, mentions, read state ## Summary This task completes the discovery and attention seams for Huddle on top of MARATHON-5/7/8/12: authenticated workspace members can full-text search messages they can see (`GET /api/search`), post slash commands through the existing message composer path (`POST /api/channels/{id}/messages`) with message-producing transforms (`/me`, `/shrug`, `//…` escape) and side-effect commands that return `message: null` (`/topic`, `/invite`, `/archive`, `/unarchive`), resolve advanced mentions (`@username`, `@group-handle`, `@channel`, `@here`) into the existing `MessageObj.mentions` user-id array without self-inclusion, and read/advance per-channel `read_state` via `GET/POST /api/channels/{id}/read` using the same `ReadStateEntry` shape already embedded on workspace detail. It does not build SPA search UI, WS badge push, file-content search, or IRC command bridging. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Search route | New package `server/search/` with `GET /api/search?q=&workspace_id=&limit=` mounted from `server/app.py` | | Search SoT | Shared SQLite; **case-insensitive substring** match on non-deleted `messages.body` (`LIKE` + `LOWER`); no FTS migration in this task | | Slash interception | **Before** `create_message` inside `POST /api/channels/{id}/messages` (and only there — not on PATCH). Parser in `server/messages/slash.py` | | Slash escape | Body starting with `//` → strip **one** leading `/`, treat remainder as normal message body (literal slash text) | | Side-effect response | Always HTTP **201** with `message: null` plus `channel` and/or `invited_user` as applicable; never insert a message row | | Mentions | Extend `resolve_mentions` in `server/messages/store.py`; keep `mentions: string[]` of user ids on `MessageObj` (no new JSON keys) | | `@channel` / `@here` | Reserved tokens; never resolved as usernames even if such users exist | | Username vs group collisi

## MARATHON-12 — User groups and workspace invitations [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: Shipped groups CRUD + workspace invitations (v4 migration + accept on-ramp). GroupObj/InvitationObj exact keys; invite_only join remains 403 without accept; accept adds workspace member + #general channel_members. Tests: test_groups.py + test_invitations.py; full suite 185 passed. Migration v4 apply/reverse verified.
DESIGN (markdown, 23513 chars): # MARATHON-12 — User groups and workspace invitations ## Summary This task adds workspace **user groups** and **invitation codes** so invite-only workspaces become joinable and @group mentions (MARATHON-11) have a durable handle→members map. It locks `GroupObj` / `InvitationObj`, the REST seams under `/api/workspaces/{slug}/groups`, `/api/workspaces/{slug}/invitations`, and `/api/invitations/{code}`, and the accept path that inserts `workspace_members` (+ `#general` channel membership) so MARATHON-7’s invite_only join gates and the SPA settings UI (MARATHON-18) have a complete onboarding loop. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Persistence | Shared SQLite SoT; **migration version 4** (additive). MARATHON-7 owns **v3** (`messages` + `channel_pins`); this task must not take v3. | | Groups package | `server/groups/` — `store.py` + `routes.py` mounted under workspace slug paths (or nested from workspaces router) | | Invitations package | `server/invitations/` — `store.py` + `routes.py`: workspace-scoped create/list + global accept/revoke by code | | Auth | Reuse `get_current_user`; all endpoints require Bearer | | Group identity | UUID4 `id`; workspace-scoped unique `handle` (mention key); display `name`; members as `member_user_ids: string[]` | | Request bodies | Create/PATCH use **`user_ids`** (task); responses always expose **`member_user_ids`** | | Invitation identity | Opaque unguessable `code` (URL-safe); UUID4 row `id` internal; list/accept return public fields below | | Targeted invites | `invited_user_id` and/or `invited_username` → resolve to one user; **force `max_uses=1`**; wrong bearer on accept → **403** `forbidden` | | Open invites | No target; `max_uses` default **1**; optional `expires_in_seconds` | | Accept effect | In one transaction: validate invite → insert `workspace_members` role=`member` (idempotent if already member) → ensure `#general` `channel_members` row → increment `use_count` only when membership 

## MARATHON-13 — WebSocket subscribe/resume and dense channel seq [in_progress] close_reason=
NOTES: Design complete for WS subscribe/resume + dense channel seq. Key decisions: SQLite channel_seq/channel_events (v6) as SoT; seq allocated in same txn as mutation; /api/ws?token=; subscribe=live only, resume=replay or resume.gap; user.updated global no seq; Redis fan-out deferred to MARATHON-14 (local hub + catch-up on any node via resume). Label remains backend for implementer after architect approval.
DESIGN (markdown, 27570 chars): # MARATHON-13 — WebSocket subscribe/resume and dense channel seq ## Summary This task adds the real-time seam for Huddle: an authenticated WebSocket at `GET /api/ws?token=…`, client `subscribe` / `resume` control frames, and a **dense per-channel `seq`** assigned in shared SQLite so every durable channel mutation (messages including thread replies, reactions, membership, channel mutations, pins) gets a gap-free monotonic frame `{type, seq, channel_id, …}`. Profile changes keep the existing channel-less `user.updated` shape (no `seq`). Same `seq` / event rows are visible from every HTTP node via the shared DB; **live cross-node push** remains MARATHON-14 (this task delivers live frames to connections on the writing node and makes catch-up via `resume` work on any node). ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Seq SoT | **Shared SQLite** counters + durable `channel_events` log (not Redis). Allocation is in the **same transaction** as the HTTP mutation so rollbacks never burn a seq. | | Dense rule | Per `channel_id`, `seq` starts at **0** (empty) and first durable event is **1**, then `+1` per committed durable event. No gaps, no duplicates, no reuse. | | WS transport | FastAPI/Starlette WebSocket on **`/api/ws`**, auth via query `token` (raw bearer, same value as `Authorization: Bearer`). | | Control protocol | Client JSON: `subscribe`, `resume` (with `since_seq`). Server JSON: `subscribed`, `resumed`, `resume.gap`, plus durable/live event frames and `user.updated`. | | History vs live | `subscribe` = watch from **now** (ack `head_seq`, no backlog). `resume` = authorize + **replay** `(since_seq+1)…head` then live, or `resume.gap` if `since_seq` is older than the retained tail. Channel history UI still uses REST (MARATHON-8). | | Retention | Keep the last **`CHANNEL_EVENT_RETAIN = 10_000`** events per channel (constant in code). After append, prune `seq < head_seq - RETAIN + 1`. Tail = minimum retained `seq` (or `1` if none pruned 

## MARATHON-14 — Cross-node fan-out and Redis-outage resilience [open] close_reason=

## MARATHON-15 — IRC gateway on :6667 with bidirectional bridge [open] close_reason=

## MARATHON-16 — SPA: auth, token storage, workspace shell [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: IMPL: Zero-build SPA at GET / (static/). Token write huddle.token; read fallbacks huddle_token/token. Workspace restore via huddle.workspaceSlug across logout. Join form POSTs /api/workspaces/{slug}/join (server still MARATHON-7). Verified via pytest + Playwright at 1280x900 (register/login/create/logout/restore; thread panel hidden).
DESIGN (markdown, 23433 chars): # MARATHON-16 — SPA: auth, token storage, workspace shell ## Summary Replace the stub `GET /` HTML with a zero-build Huddle SPA served identically from every HTTP node. The SPA owns browser session lifecycle (register → auto-login, login, logout), durable bearer storage under `localStorage` key `huddle.token` (with documented read fallbacks), restoration of the last workspace after logout/login, and the three-pane workspace shell (coloured sidebar + main message pane + on-demand thread panel) with the epic’s stable `data-testid` selectors so CUA/Playwright can drive auth and workspace onboarding at 1280×900. Messaging, channel CRUD UI, and settings modals remain later SPA tasks; this task only mounts their shell slots and wires the seams that already exist (Auth + Workspaces APIs). ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Delivery | **Static SPA** under `/app/static/` served by FastAPI; `GET /` returns `static/index.html` on every node | | Frontend stack | **Vanilla HTML + CSS + ES modules** — no Node, no bundler, no React | | API access | Same-origin relative `/api/...` (works on `:8000`/`:8001`/`:8002` without CORS or hard-coded ports) | | Auth header | `Authorization: Bearer <token>` exactly as MARATHON-3 | | Token storage | Write **`localStorage["huddle.token"]`**; read primary then accepted fallbacks (below) | | Workspace restore | Persist **`localStorage["huddle.workspaceSlug"]`** across logout; re-select after next login | | Routing | **Hash routes** (`#/`, `#/w/{slug}`) — no server path rewrite; refresh-safe on any node | | Selectors | Stable **`data-testid="<name>"`** attributes matching the task list exactly | | Shell layout | Fixed three-pane: sidebar (coloured) \| message pane \| thread panel (hidden until opened) | ### Why - Epic requires browser UI at `GET /` with stable selectors; MARATHON-2 explicitly deferred SPA to this task and left a stub. - Zero-build static assets keep `start.sh` / Python-only ops unchanged 

## MARATHON-17 — SPA: channels, messages, threads, reactions [review] close_reason=
NOTES: DESIGN: SPA chat UX for channels/messages/threads/reactions on M16 shell + M7/M8 REST. Zero-build chat_ui.js; REST-only (no WS/M13); normative channel-*/message-*/thread-*/reaction-*/emoji-* testids + create-channel-modal; emoji grid not prompt(); coexist with M18 via channel-settings-btn + huddle:open-channel-settings. Label remains frontend for implementer after architect approval.
DESIGN (markdown, 27259 chars): # MARATHON-17 — SPA: channels, messages, threads, reactions ## Summary Fill the MARATHON-16 workspace shell with the core chat UX: channel list selection and create-channel modal (duplicate-name validation, `#`-prefixed display names, `data-channel-id` / `data-channel-name` on rows), channel header (name + topic) with non-blank empty-channel copy, message history with composer (post / hover-toolbar edit+delete, edited indicator, empty-body validation, hard-reload survival via REST), message rows (avatar + author + human-readable timestamp + body), right-hand thread panel that bumps reply counts, and reactions via an in-DOM emoji-picker grid (never `prompt()`) with count badges. This task is **frontend-only** against already-shipped MARATHON-7/8 REST; it locks the epic’s `channel-*` / `message-*` / `thread-*` / `reaction-*` / `emoji-*` / `create-channel-modal` selector contract so CUA/Playwright can drive chat at 1280×900. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Delivery | Extend the **zero-build vanilla SPA** (`static/` ES modules) — same stack as MARATHON-16; no bundler | | Module split | New `static/js/chat_ui.js` owns channel switch, create modal, message pane, thread panel, reactions; `shell.js` exposes active workspace/channel + list refresh hooks; reuse `api.js` / `storage.js` | | Data path | **REST only** for this task (list/post/patch/delete/replies/reactions). No WebSocket live fan-out (MARATHON-13). After each successful mutation, update local DOM from the response envelope; on channel select, `GET` history | | Channel create | Modal `create-channel-modal` → `POST /api/workspaces/{slug}/channels`; append sidebar row on 201; 409 → inline duplicate-name error | | Channel switch | Click sidebar row → set `activeChannel`, render header + load messages; keep thread panel closed unless opened on a message | | Empty states | Distinct non-blank copy for (a) no channel selected, (b) channel selected with zero messages | | Messa

## MARATHON-18 — SPA: workspace and channel settings UI [closed] close_reason=integrated into /app by harness gate (attempt=2)
NOTES: REBASE: cherry-picked prior IMPL 6c6170f onto HEAD with M8/M9 (messages + /api/dms + schema v5) preserved; conflict resolved in server/channels/routes.py keeping PostMessageBody and AddChannelMemberBody. 220 tests green.
DESIGN (markdown, 28511 chars): # MARATHON-18 — SPA: workspace and channel settings UI ## Summary This task adds the Huddle SPA settings surface on top of the MARATHON-16 shell: a **workspace settings modal** (tabs General / Members / Invitations) and a **channel settings modal** (members list/add, topic, archive/unarchive). It wires those UIs to the already-shipped REST seams from MARATHON-6/7/12 (`PATCH` workspace, members/roles/transfer, invitations create/list/revoke/accept, channel members/topic/archive) and locks the epic’s stable DOM ids plus `settings-*` / `invitation-*` / role / channel-member `data-testid`s so CUA/Playwright can drive admin flows at 1280×900. It also specifies the missing **channel add-member** REST seam that MARATHON-7 deferred, because the channel-settings “add” control cannot work against join alone for private channels. ## Technical Approach ### Decision | Concern | Choice | |--------|--------| | Delivery | Extend the **zero-build vanilla SPA** (`static/` ES modules) — same stack as MARATHON-16; no bundler | | Module split | New `static/js/settings_ui.js` owns both modals; `shell.js` exposes open triggers + active workspace/channel; reuse `api.js` / `storage.js` | | Modal pattern | Match `auth-modal`: overlay + panel, `hidden` attribute, `role="dialog"`, Escape/close dismiss; **one settings modal open at a time** | | Workspace entry | `data-testid="workspace-settings-btn"` in `workspace-header` (visible whenever shell shows a workspace) | | Channel entry | Selecting a non-DM channel in `channel-list` sets active channel; `data-testid="channel-settings-btn"` in message-pane header chrome (or beside channel list) opens channel settings for that id — **does not** require MARATHON-17 message composer | | Tabs | Workspace modal: exactly three tabs — `general`, `members`, `invitations`; only one panel visible | | Authz UX | Client **hides/disables** privileged controls from cached caller role; server remains SoT (403/404 still shown as `error.message`) | | Invitation redee

## MARATHON-19 — Verify: cluster boot health Redis and stub GET / [closed] close_reason=integrated into /app by harness gate (attempt=1)

## MARATHON-20 — Verify: HTTP node and Redis SIGKILL respawn [closed] close_reason=integrated into /app by harness gate (attempt=1)

## MARATHON-21 — Verify: auth register login me envelopes [closed] close_reason=integrated into /app by harness gate (attempt=1)
NOTES: QA PASS: Auth envelopes match design on integrated head 00d49c6. Unit+live 38 passed (register 201 UserObj+token>=16 no email; dup 409; space/short pw 400; login 200; wrong pw 401 not 404; me bearer 200; missing/invalid 401; token cross :8000→:8001/:8002). defects filed: none.

## MARATHON-22 — Verify: auth token accepted on all HTTP nodes [closed] close_reason=integrated into /app by harness gate (attempt=1)