# Provider plugins and local state never belong in the repo.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Real values; terraform.tfvars.example is the tracked template.
terraform.tfvars
*.auto.tfvars
