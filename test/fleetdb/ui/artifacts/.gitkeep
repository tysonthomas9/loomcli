# Keep this directory in git. Actual artifacts are .gitignore'd — see
# test/fleetdb/ui/.gitignore. This file exists only so the artifacts tree
# (reports/, screenshots/, network-traces/, forensics/) has a landing place
# when the suite first runs.
