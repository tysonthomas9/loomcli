{
	"name": "@loom/workflow-sdk",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Run-scoped FleetDB client, idempotent task starts, and effectively-once actions for Loom dynamic workflows.",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"check": "tsc -p tsconfig.json --noEmit"
	},
	"devDependencies": {
		"@types/node": "^24.13.1",
		"typescript": "^5.7.0"
	}
}
